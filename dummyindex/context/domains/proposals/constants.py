"""Package-private constants for `context/proposals/`.

Kept out of `enums.py` because schema version is a tunable,
not a closed-alphabet enum.
"""
from __future__ import annotations

SCHEMA_VERSION = 1
