# Now

## 2026-08-21 — handoff

Correction: the gate must never self-judge triggers — observations always come from the host skill as data. Learned that scoring partial observation sets silently deflates recall; score_run raises instead, keep it that way.
