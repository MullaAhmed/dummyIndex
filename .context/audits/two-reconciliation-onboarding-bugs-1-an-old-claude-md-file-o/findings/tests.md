## tests findings

### Bug 1 — old CLAUDE.md / `.claude/` left dangling (migration)

- `dummyindex/cli/migrate.py:73-139` — **critical** — `migrate_claude_md_location` has ZERO direct tests (no test module imports `dummyindex.cli.migrate` at all) — `grep -rn 'migrate_claude\|migrate_legacy\|from dummyindex.cli.migrate' tests/` returns nothing; the whole relocate/strip/delete logic is unexercised — add a unit test module hitting all three exits.
  — [round1: defend] status=confirmed: re-ran `grep -rn 'migrate_claude\|migrate_legacy\|from dummyindex.cli.migrate' tests/` → still zero hits. The whole `migrate.py:73-139` body is untested. Corroborates correctness C2 (`migrate.py:108-117`) and architecture A3 (`migrate.py:73-138`) — the misplaced/incomplete logic has no guard rail.
- `dummyindex/cli/migrate.py:108-120` — **critical** — the `legacy_had_managed_block == False` branch (pre-existing root `CLAUDE.md` with USER content but NO managed block) is untested; this branch returns early after bootstrapping `.claude/CLAUDE.md` and LEAVES the root file untouched — exactly the "dangling" scenario the user reports — assert root `CLAUDE.md` is preserved verbatim and `.claude/CLAUDE.md` is created, and decide whether that is intended.
  — [round1: defend] status=confirmed: this is the exact branch correctness C2 (`migrate.py:108-117`) flags as a real medium bug. A test asserting the no-managed-block root file is folded (not abandoned) WOULD have caught C2 — high-value gap. The branch ships with no assertion either way.
- `dummyindex/cli/migrate.py:124-130` — **high** — the "user content preserved" path (legacy block present + non-empty residue → rewrite root file with `legacy_residue + "\n"`) has no test proving the user's surrounding prose survives the strip — assert residue is exactly the pre/post-marker text, stripped.
  — [round1: concur] status=confirmed: aligns with correctness C4 (info, `migrate.py:124-130`) on residue `.strip()` collapsing blank lines — a residue test pins both the preservation and the cosmetic loss.
- `dummyindex/cli/migrate.py:131-136` — **high** — the "removed empty root file" path (legacy block present + empty residue → `legacy_path.unlink()`) is untested — assert the root `CLAUDE.md` is deleted only when residue is whitespace-only, and that whitespace-only-but-present content (e.g. `"\n\n"`) deletes (not preserves).
  — [round1: defend] status=confirmed: still no test touches the unlink path; unchanged from round 0.
- `dummyindex/cli/migrate.py:97-100` — **medium** — the `OSError` on reading an unreadable legacy `CLAUDE.md` (early `return` with a stderr warning) is untested — no assertion the function degrades silently instead of crashing the refresh.
  — [round1: concur] status=confirmed: relates to architecture A4 (`migrate.py:53-66,100,116`) — the stderr-only failure channel is both untested AND unstructured.
- `dummyindex/cli/migrate.py:13-71` — **high** — `migrate_legacy_layout` (the only caller of `migrate_claude_md_location`, wired into `refresh-indexes` at `dummyindex/cli/refresh.py:32`) is untested; `tests/context/output/test_refresh_indexes.py:1-101` never creates a legacy `graph/` dir or a root `CLAUDE.md`, so the legacy→`features/` move and the CLAUDE relocation never run in any integration test — add a refresh test seeded with a pre-v0.10 layout.
  — [round1: defend] status=confirmed: re-checked `tests/context/output/test_refresh_indexes.py` — no legacy `graph/` seeding, no root `CLAUDE.md` seeding; the only integration entry into the migrator is dead in tests.
- `dummyindex/installer/install.py:271`, `dummyindex/context/build/runner.py:263` — **high** — on a FRESH install, migration is never invoked: both paths call `bootstrap_claude_md(.../.claude/CLAUDE.md)` directly and never look at a pre-existing root `CLAUDE.md`. No test covers "fresh install on a repo that already has a root `CLAUDE.md`" — `tests/test_install.py:377-392` only asserts `.claude/CLAUDE.md` exists, never seeds/asserts a root file — add a test seeding root `CLAUDE.md` (with and without a managed block) before `install`/`build_all` and assert the expected end state. This is the likeliest reproduction of the user's dangling bug.
  — [round1: defend] status=confirmed: directly corroborates correctness C1 (`install.py:262-263`/`runner.py:262-263`) and architecture A1 (`install.py:271,236`) + A2 (`runner.py:262-263`). Both peers rate the dangling-on-fresh-install bug HIGH and real. A test seeding a root `CLAUDE.md` before `build_all`/`install` and asserting it is folded WOULD have caught C1/A1/A2 — this absence is strong corroboration of the bug. Highest-value Bug-1 gap on the panel.
- `tests/context/output/test_bootstrap.py:1-141` — **low** — `bootstrap_claude_md` is thoroughly covered (create/append/replace/unbalanced/atomic), but no test exercises a pre-existing `.claude/CLAUDE.md` that already holds USER content alongside the managed block during the migrate flow — the append/preserve interaction across the two files is only proven for one file in isolation — suggested fix: cross-file integration test (covered by the migrate tests above).
  — [round1: concur] status=confirmed: low; subsumed by the migrate-test recommendations above.

### Bug 2 — no reconcile after Stop hook (gate)

- `dummyindex/context/hooks.py:115-128,163` + `dummyindex/cli/reconcile_gate.py:15-35` — **info** — the user's framing is contradicted by code AND tests: the Stop hook IS registered and `tests/context/test_hooks.py:127-128,805,224,914` assert `reconcile-gate` appears in the canonical Stop command; the wire layer is covered by `tests/cli/test_reconcile_gate_cli.py` and the subprocess e2e in `tests/cli/test_reconcile_gate_e2e.py:50-110` — no gap here, the trigger exists and is tested.
  — [round1: defend (refined)] status=confirmed: the TRIGGER exists and is tested — I defend that. But correctness C6 / architecture A5 (both HIGH) show the trigger is INCOMPLETE on an anchored repo: it fires only on `unassigned`/`awaiting`/anchor-less mtime, never on committed modifications to owned source. So "the gate is wired and tested" is true; "the gate catches the common drift case" is false. Refined framing: trigger present and covered; the gate's *relevance test* has an untested blind spot (see T-new below). The user's literal claim ("nothing checks after Stop") stays refuted; their underlying symptom (drift escapes the gate) is real via C6/A5.
- `dummyindex/context/reconcile_gate.py:284-295,229-238` — **critical (new, round 1)** — NO test asserts the gate BLOCKS when a session modifies-and-commits a feature-owned source file on an ANCHORED repo. Every blocking `decide_block` test escapes the C6/A5 path: the `patched` fixture (`tests/context/test_reconcile_gate.py:96-99`) hands `compute_drift` a report with `unassigned_new_files=("new/x.py",)`, so `_gate_relevant` (`reconcile_gate.py:291`) returns True on the unassigned signal — never on `rows`-with-anchor. `test_silent_when_only_mtime_drift_and_anchor_present` (`:255-265`) uses rows-only WITH a live anchor and asserts SILENCE (the C6/A5 behaviour, locked in as correct). `test_mtime_drift_blocks_when_no_anchor` (`:268-278`) blocks but only because `_has_live_anchor` is monkeypatched False. The e2e `test_cli_emits_block_for_drifted_repo` (`tests/cli/test_reconcile_gate_e2e.py:50-67`) blocks via mtime — but `_make_drifted_repo` (`:11-23`) never stamps an anchor, so `compute_reconcile_report` early-returns `indexed_commit=None` (`build/reconcile.py:128-134`) → `_has_live_anchor` False → it's the anchor-LESS branch, not the anchored modification path. There is no test where (a) an anchor is live AND (b) the only drift is a committed modification to an owned file AND (c) a block is asserted — because the gate CANNOT produce that block (it never reads `compute_reconcile_report().drifted_features`, `build/reconcile.py:187`). The absence is structural and confirms C6/A5 as a real high-value gap. — suggested fix: add a `decide_block` test with a real anchored `.context/` + a committed edit to an owned file asserting a block; it will fail today, proving C6/A5.
- `dummyindex/context/reconcile_gate.py:257-258` — **high** — the `subagent_file_count > 0` fast-path of `_session_drifted_source` (the branch the docstring calls "the highest-drift workflows" — `/dummyindex-build` whose edits live inside subagents) is NOT directly tested for the gate; `_session_drifted_source` has exactly one direct test and it is the negative `.claude-design` case (`tests/context/test_reconcile_gate.py:505-508`). The `subagent_file_count` blocks in `tests/context/domains/memory/test_nudge.py:25,107,145` cover `is_significant`/nudge, not the gate's source-attribution branch — add `assert rg._session_drifted_source((), base, subagent_file_count=1) is True` and a `decide_block` test where the only drift signal is subagent file count.
  — [round1: defend] status=confirmed: re-checked — the only direct `_session_drifted_source` assertion is the negative `.claude-design` case at `:505-508`; the `subagent_file_count > 0` positive branch (`reconcile_gate.py:257-258`) has no direct test. Corroborates correctness C10 (`reconcile_gate.py:241-270`): the over-counting of research-only subagents is exactly this untested branch.
- `dummyindex/context/reconcile_gate.py:259-270` — **medium** — `_session_drifted_source`'s positive main-thread branch is covered only end-to-end via `decide_block` (`test_blocks_when_session_edited_real_source`); the `_rel_under_base` returns-None path (edit outside the project tree → skip, lines 262-264) and an edit equal to a prefix root (`rel == prefix`, line 266) have no direct assertion — add unit cases for an absolute path outside `base` and for an edit at exactly `.context` / `.claude`.
  — [round1: concur] status=confirmed: unchanged; `_rel_under_base`-None (`:262-264`) and `rel == prefix` (`:266`) still have no direct case.
- `dummyindex/context/reconcile_gate.py:317,341-345` — **medium** — block-once is tested at the memo level (`test_blocks_once_then_allows_same_session`, `test_different_session_blocks_again`) but the EMPTY-`session_id` fallback (memo never persists → `already_blocked` returns False at line 207-208, so block-once then rests solely on `stop_hook_active`) has no test — a session with no id could re-block on every non-reentrant stop; add a `decide_block(..., session_id="")` repeat-call test asserting the documented fallback behaviour.
  — [round1: defend] status=confirmed: directly the gap behind correctness C7 (`reconcile_gate.py:315-316`) and architecture A6 (`:315-317`). A `session_id=""` repeat-call test asserting the documented fallback WOULD have caught the C7 short-circuit ambiguity — high-value. No such test exists.
- `dummyindex/context/reconcile_gate.py:229-238` — **medium** — `_has_live_anchor` is always monkeypatched in the gate tests (`tests/context/test_reconcile_gate.py:112,274`), so the real `compute_reconcile_report`-based anchor detection (anchor present / `anchor_broken` / `indexed_commit is None`) is never exercised through the gate — the assertions test the mock, not the function — add a unit test that builds a real anchored vs anchor-less `.context/` and asserts the three return values.
  — [round1: defend] status=confirmed: re-verified `_has_live_anchor` is monkeypatched at `:112` (fixture) and `:274`. This is WHY C6/A5 is invisible to the suite: with `_has_live_anchor` always mocked, no test ever runs the real anchored→`drifted_features` path. The mock masks the bug. Strong corroboration of C6/A5.
- `dummyindex/context/reconcile_gate.py:191-199,219-226` — **low** — `_load_gate_state` malformed/`OSError` fallback (returns `{}`) and `mark_blocked`'s 100-entry pruning (lines 219-225) are untested — a corrupt or oversized memo could change block behaviour silently — add a unit test writing junk to `reconcile-gate-state.json` and one writing 101 entries.
  — [round1: defend] status=confirmed: unchanged; no malformed-state or pruning test exists.
- `dummyindex/context/reconcile_gate.py:127-134` — **low** — `_root_label`'s fallback-to-absolute-path branch (`ctx_root` not under `base` → `ValueError`) is exercised only through `render_multi_block` (`test_render_multi_block_root_outside_base_uses_absolute_path`), never directly — acceptable, but the `"."` → `"(session root)"` mapping has no isolated assertion — suggested fix: none (covered transitively).
  — [round1: concede] status=withdrawn: the `(session root)` mapping IS asserted transitively in `test_render_multi_block_root_outside_base_uses_absolute_path` (`:472-483`) and `test_blocks_when_both_root_and_submodule_stale` (`:387`). Coverage is adequate; withdrawing as a gap.

## tests — round 1 positions

### On correctness findings
- **C1** (`install.py:262-263` / `runner.py:262-263`, high) — concur, confirmed: no test seeds a root `CLAUDE.md` before fresh install/build; `tests/test_install.py:377-392` only asserts `.claude/CLAUDE.md`. A red-before-green test here would catch it. Maps to my T7.
- **C2** (`migrate.py:108-117`, medium) — concur, confirmed: the no-managed-block early-return branch (`migrate.py:108-120`) is untested (my T2). A test asserting the user's root file is folded WOULD have caught C2 — high-value gap.
- **C3** (`install.py:167-181`, low) — concur, status=open→confirmed (test-gap): the substring `"dummyindex" in content` idempotency probe has no test asserting a false-positive skip (e.g. a global `CLAUDE.md` merely mentioning "dummyindex"). `grep -rn "dummyindex.*in content\|already.*registered" tests/` finds no such case. A test would catch it.
- **C4** (`migrate.py:124-130`, info) — concur: my T3 residue test pins the blank-line collapse; cosmetic.
- **C5** (gate exists, refuted) — concur, refuted: matches my T9 — Stop hook registered + tested (`tests/context/test_hooks.py`, `tests/cli/test_reconcile_gate_e2e.py`).
- **C6** (`reconcile_gate.py:229-238,293-295`, HIGH) — **concur, confirmed — firm verdict: NO test would catch this.** The gate's blocking tests all ride `unassigned_new_files` (`tests/context/test_reconcile_gate.py:96-99`) or a monkeypatched `_has_live_anchor=False` (`:274`) or an anchor-less e2e repo (`tests/cli/test_reconcile_gate_e2e.py:11-23`). The one rows-only + live-anchor test asserts SILENCE (`:255-265`), and `drifted_features` (`build/reconcile.py:187`) is never read by the gate. A committed modification to an owned file on an anchored repo cannot produce a block, and nothing tests for one. Filed as my new critical T-finding.
- **C7** (`reconcile_gate.py:315-316`, medium) — concur, confirmed: empty-`session_id` short-circuit untested (my T12).
- **C8** (`reconcile_gate.py:341-346`, medium→info) — concur with downgrade: single-memo-per-session is asserted by `test_blocks_once_then_allows_same_session`/`test_different_session_blocks_again` (`:213-249`); the per-root re-block risk is latent, not a tested-behaviour gap. Status=confirmed-as-info.
- **C9** (`reconcile_gate.py:332-333`, medium) — concur, confirmed (test-gap): `test_silent_when_no_transcript` (`:168-171`) asserts the missing-transcript SILENT allow — so the *current* behaviour is locked in, but there is NO test asserting the conservative-block-on-unresolvable-transcript that C9 argues for. The defect (silent drift on transcript-resolution failure) is untested as a defect.
- **C10** (`reconcile_gate.py:241-270`, low) — concur, confirmed: the `subagent_file_count>0` over-count branch (`:257-258`) is untested (my T10) — a test feeding research-only subagents would expose the spurious block.
- **C11** (`build/reconcile.py:277-283`, info) — concur: `stamp_reconciled` deliberately ignores `drifted_features` (`reconcile.py:257,277`); no test asserts a drift-only stamp advancing past unreconciled edits. Compounds C6.

### On architecture findings
- **A1** (`install.py:271,236`, high) / **A2** (`runner.py:262-263`, high) — concur, confirmed: same uncovered seam as C1/T7. A first-build test seeding a root `CLAUDE.md` would catch both.
- **A3** (`migrate.py:73-138`, medium) — concur: the misplaced helper is also the zero-coverage one (my T1); extracting it to `context/output/` should ship with the unit tests T1–T5.
- **A4** (`migrate.py:53-66,100,116`, low) — concur: stderr-only emission is untestable as a structured result today (my T5 OSError gap is a symptom).
- **A5** (`reconcile_gate.py:284-295,229-238`, HIGH) — **concur, confirmed:** same root cause as C6. `_has_live_anchor` is monkeypatched in every gate test (`:112,:274`), so the freshly-installed/anchored blind spot is never exercised. The "first session that edits source produces drift the gate cannot see" claim is corroborated by the absence of any anchored-modification block test.
- **A6** (`reconcile_gate.py:315-317,202-209`, medium) — concur, confirmed: the memo-set-before-reconcile dismissal path is untested; my T12 (empty-session_id) plus a "dismissed-but-still-stale re-arm" test would both fail today.
- **A7** (`reconcile_gate.py:241-270,39`, medium) — concur: the duplicated `_NON_SOURCE_PREFIXES` is held together ONLY by `test_gate_non_source_covers_reconcile_tool_paths` (`:486-502`) — a test standing in for a shared constant, exactly as A7 says. The invariant is tested; the structural seam is not.
- **A8** (`reconcile_gate.py:332-340`, low) — concur: the gate's coupling to `is_significant`/`read_session_signal` is real and only indirectly tested (fixture monkeypatches `read_session_signal` at `:109`); a memory-heuristic change would silently shift gating with no gate-side test catching it.
