## correctness findings

> Scope: the plan/spec/checklist under `.context/proposals/outstanding-audit-fixes/` vs. the CURRENT post-`c574d41`/`d7d2e74` source. The code wins. Every bullet cites a `path:range`. Severity = impact on plan/audit quality.

### HIGH — the plan re-proposes work the codebase author explicitly REJECTED with a rationale

- `dummyindex/context/domains/audit/log.py:166-181` + `dummyindex/context/domains/council.py:332-350` — **high** — Task 9 (extract `latest_status` into `context/domains/log_scan.py`) directly contradicts a deliberate, documented decision in the current code. Both `latest_status` bodies now carry a long NOTE (added by `c574d41`) arguing the two are *"deliberately **not** extracted into a shared helper"* because *"sharing would create a cross-domain dependency that `conventions/folder-organization.md` warns against — and the loop is too small to justify a new cross-cutting module"*; the council copy adds *"This copy is load-bearing for resumption; keep its semantics exact."* The plan invokes the SAME convention (`folder-organization.md:54`, verified — "Filenames never carry a leading underscore") to justify the `log_scan.py` name, yet never acknowledges or refutes the in-code argument against the extraction itself. This is the audit report's M5, which was the *lowest-confidence* finding ("Fix (optional): extract a shared `last_matching` helper") and was subsequently argued down in the code. — Fix: the plan must either (a) drop Task 9, or (b) explicitly quote and rebut the two NOTE blocks, justifying why a new `context/domains/log_scan.py` peer module does NOT violate the cross-domain-dependency rule the code cites (e.g. that `domains/atomic_io.py` already establishes a shared-domain-helper precedent — `atomic_io.py:1-5` says exactly that). As written, Task 9 reopens a closed decision.

### MEDIUM — stale premises / location mismatches that change a task's meaning

- `dummyindex/cli/query.py:60-68` — **medium** — Task 8's "trailing `--top-k`/`--budget`" premise is STALE. `c574d41` already added the trailing-flag guard (`elif a in ("--top-k", "--budget"): … return 2`), so `query "x" --top-k` (trailing) already exits 2. The spec's acceptance bullet (spec.md:74) and checklist item (checklist.md:33) name BOTH `--top-k` (trailing) and `--bogus` (unknown) as if both are broken; only the **unknown-flag** half (`query.py:69-71` `else: leftover.append(a)`) is genuinely outstanding. The audit report's C4 (report.md:59) was likewise already fixed for the trailing case. — Fix: reword Task 8 / spec.md:74 / checklist.md:33 to scope the work to unknown-flag rejection + `usage_error` routing + USAGE doc only; the trailing case is a no-op (write a regression test, not a fix).

- `dummyindex/pipeline/extract/__init__.py:268-293` — **medium** — Task 4(a) location mismatch. It says "containment-check leaf targets with `resolve_under_root` **at read time** (just before `read_bytes`/`_read_text_safely`)" but cites `:268-293`, which is `collect_files` (the directory WALK), not the read. The actual leaf read happens in `extract()` at `:167` (`result = extractor(path)` → `generic.py:42 source = path.read_bytes()`) and in `references.py:79` (`_read_text_safely`, defined `:164-176`). A check inside `collect_files:268-293` is at walk time, not read time, and would not be "robust to TOCTOU and Task-12's caching" as the task claims. — Fix: cite the read sites (`extract/__init__.py:167`, `generic.py:42`, `references.py:79/164-176`) and decide whether the guard goes in the walk (cheap, TOCTOU-racy) or at every read site (matches the stated intent). The concerns source (`extraction-pipeline/concerns.md:16`) names both `:268-293` AND `references.py:164-176` — the plan dropped the read-site half.

- `dummyindex/cli/reality_check.py:65-69` — **medium** — Task 11's premise ("the demote/promote return value … [is] in scope" at the CLI, so the CLI can print "demoted X→AMBIGUOUS / restored …→Y / unchanged") is only partly constructible from the current return type. `demote_feature_on_contradiction` / `promote_feature_on_clean` return a bare `bool` (`confidence.py:26,61,64,94`), and the CLI currently **discards** it (`reality_check.py:67,69` call with no assignment). A bare bool distinguishes "touched" from "not touched" but CANNOT distinguish the three transition strings the spec demands (spec.md:71) — e.g. "unchanged" because already-AMBIGUOUS-with-no-stash (`confidence.py:47-48` early return) vs. a real "demoted X→AMBIGUOUS" both return a value the CLI can't tell apart, and the prior value `X` is never returned at all. — Fix: Task 11 must widen the return type (e.g. return `(transition, from_value, to_value) | None`) or read feature.json in the CLI; "emit at the CLI from the existing return value" understates the change. This is a real implementability gap, not a nit.

- `dummyindex/context/domains/reality_check/__init__.py:74` — **medium** — Task 2 (retire `render.py:_atomic_write`) is under-specified: the symbol is **re-exported** at `__init__.py:74` (`from .render import _atomic_write, render_report_md, write_report`). Neither plan.md Task 2 nor spec.md's "Reused symbols" para mentions updating this re-export, and `confidence.py:16` imports it via `from .render import _atomic_write`. A clean retirement must touch three sites (`render.py` def, `confidence.py:16` import, `__init__.py:74` re-export), not the two the task names. — Fix: add `__init__.py:74` to Task 2's edit list (or confirm whether any external caller imports `reality_check._atomic_write` before deleting).

### LOW / INFO — citations that resolve but with minor drift; confirmations

- `dummyindex/analysis/cluster.py:10-11,109` vs `3d7d539` ("mark C2/C3 resolved") — **low (NOT a contradiction; GATE is correct)** — The Task-6 GATE is NOT in conflict with the "C2/C3 resolved" commit. Verified: Leiden IS seeded (`cluster.py:42 leiden(G, random_seed=_RANDOM_SEED)`), Louvain IS seeded (`:52`), and the tiebreak IS present (`:109 key=lambda c: (-len(c), sorted(c)[0])`) — that is the per-backend determinism C2/C3 resolved. The GATE targets a *different, deeper* issue the seed does not solve: graspologic-Leiden vs NetworkX-Louvain produce **different partitions**, so `community` is only cross-*machine* stable when backend presence is constant (`extraction-pipeline/concerns.md:6,31,52`). The docstring `cluster.py:10-11` even over-claims "identical across the Leiden … and Louvain … paths," which is false and is exactly what the GATE must resolve. The spec's Open-question (spec.md:83) states this accurately ("no injectable backend seam today"). Plan is correct here; recorded so the panel does not mis-flag it.

- `dummyindex/export/graph.py:36,40-41` — **info (premise CONFIRMED)** — Task 5 is valid. `data["hyperedges"]` (`:36`) is assigned by reference from `G.graph["hyperedges"]` and is NOT sorted; `nodes`/`links` ARE sorted (`:40-41`) and the dump already passes `sort_keys=True` (`:43`). `c574d41` sorted nodes/links but left hyperedges. Task 5's claim ("nodes/links already sorted at `:40-41`, only hyperedges need sorting") is exactly right.

- `dummyindex/pipeline/build/references.py:164-176` — **low (report citation now stale; plan OK)** — The audit report's P2 cites `references.py:99-111` / `read_text` at `:109` (report.md:54,55); after `c574d41`'s rewrite the read site moved to `_read_text_safely` (`:164-176`, called `:79`). The rewrite was P1-style (single combined regex matcher, `_build_matcher:106-125`) and did **NOT** thread bytes — it still does an independent `path.read_text` (`:174`). So P2/Task 12 is **genuinely outstanding** (good), but any reader cross-checking the report's `:99-111` against current code will miss. Plan Task 12 cites no line for references.py, so the plan itself is fine; flag the report drift only.

- `dummyindex/context/domains/reality_check/confidence.py:90-93` — **low** — Task 10's "mirror INDEX before popping the stash (`:91-93`)" cites the right region but is imprecise: the stash pop is `del payload[DEMOTED_FROM_KEY]` at `:90` (before `:91`), the feature.json write is `:91`, the INDEX mirror call is `:93`. The concern is real (promote pops the stash and writes feature.json BEFORE mirroring INDEX, so a crash after `:91` loses the stash with INDEX still AMBIGUOUS — `reality-check/concerns.md:4`). The fix region is `:88-93`, not `:91-93`. Meaning unchanged; cite `:88-93`.

- `dummyindex/cli/help.py:201-206` — **info (premise CONFIRMED)** — Task 8(c) doc-gap is real: the `query` USAGE block does not mention the no-match exit-1 anywhere (grep for "exit"/"no match" in help.py returns only other subcommands). `query.py:102` does `return 0 if result.matches else 1`. Valid work.

- `.context/proposals/outstanding-audit-fixes/spec.md:19-20` — **low** — the spec calls the six pre-existing guards "inline `x.resolve().relative_to(root)` guards," but two of the six do NOT `.resolve()` the candidate: `cli/common.py:42` (`scope_resolved.relative_to(cwd)`) and `reconcile_gate.py:66` (`sub.relative_to(root)`) operate on already-resolved paths. The other four (`incremental.py:110`, `manifest.py:105`, `check.py:78`, `discover.py:257`) do `.resolve().relative_to()`. All six citations resolve to real guards; the generalization is just slightly loose. Note for whoever migrates them (Task 1 only migrates `incremental.py`): `resolve_under_root` must handle both already-resolved and not-yet-resolved candidates uniformly.

### Citations verified accurate (spot-check pass — no finding, recorded for the panel)

- `atomic_io.py:11` `write_text_atomic` ✓; `confidence.py:16` `from .render import _atomic_write` ✓; `render.py:62-66` defines `_atomic_write` (plan Task 11 cites `render.py:16` for the `_reality-check.json` write — that line IS the JSON `_atomic_write` call ✓); `cli/common.py:47` `usage_error` ✓; `cache.py:51` env read ✓, `:60-77` `load_cached` ✓, `:10-17` `_body_content` ✓, `:97` `save_cached` tmp write ✓; `extract/__init__.py:163,169` `cache_root` threading ✓, `:216-221` `global_label_to_nid` ✓, `:190-197` `id_remap` in-place mutation ✓; `verify.py:157-202` `_resolve_cited_path` ✓ (every return branch — `:180,183,185,193,196` — is unconfined, Task 7a target real), `:376-384` `_repo_root_from_meta` ✓ (returns raw `meta["root"]`, consumed unconfined at `:63`), `:143-146` line-count `open` ✓, `:30` feat_dir construction ✓; `cli/reality_check.py:39` `feature_id = parsed.get("feature")` ✓ (flows to `verify.py:30` AND CLI write `:62` — Task 7c write-primitive claim real); `generic.py:42` `path.read_bytes()` ✓; `incremental.py:330-344` `_hash_files`/`file_hash` ✓, `:108-112` doc-path guard ✓; `pipeline/io/__init__.py:15,28` already exports `is_git_repo`/`resolve_git_dir` + has `__all__` (Task 1 re-export surface real) ✓; `context/__init__.py:64 from dummyindex.pipeline.io import is_git_repo` ✓ (the `context → pipeline.io` arrow Task 1 relies on is real and live). `log_scan.py`/`paths.py`/`last_matching` do NOT exist yet — Tasks 1/9 are genuinely net-new.

### Internal-consistency check (plan ↔ spec ↔ checklist)

- `.context/proposals/outstanding-audit-fixes/plan.md:45` vs `:35` — **low** — the Wave-3 rationale says "Task 12 shares the `extract/__init__.py` read seam (Task 4)," but Task 12's own file list (`plan.md:35`) is `references.py`, `generic.py`, `incremental.py` — `extract/__init__.py` is NOT in it. The serialization rationale is defensible (Task 4 changes the symlink/read seam in `extract/__init__.py:167` that Task 12's byte-threading must respect, and `generic.py:42` — Task 12's seam — is invoked from `extract/__init__.py:167`), but the prose overstates direct file overlap. Reconcile: either add `extract/__init__.py` to Task 12's list or reword to "depends on Task 4's read-seam contract" rather than "shares the file."
- Task/wave counts are consistent: 13 tasks, 4 waves across plan.md, spec.md acceptance, and checklist.md. Wave-2 file-disjointness (plan.md:44) holds — `cache.py` / `extract/__init__.py` / `graph.py` / `cluster.py` / `verify.py`+`cli/reality_check.py` / `query.py`+`help.py` / `log.py`+`council.py`+`log_scan.py` do not collide. The Task-12-vs-Task-4 `extract/__init__.py` adjacency is the only seam the disjointness prose glosses (covered above).

## Round 1 — rebuttals (correctness)

> Positions taken after re-reading the four peer files and re-verifying every load-bearing claim against the post-`d7d2e74` source. The code wins; each position cites `path:range`. Final severities for my own round-0 findings are listed at the end.

### Item 1 — my Task-9 HIGH (re-proposes a rejected in-code decision) → **CONVERGE to MEDIUM**

**Position: SOFTEN my own HIGH to MEDIUM**, on the strength of evidence I myself introduced and which Architecture/Tests corroborate.

The decision Task 9 reverses is real and explicit — verified verbatim: `audit/log.py:169-176` and `council.py:337-344` both carry the NOTE that the two `latest_status` bodies are *"deliberately **not** extracted into a shared helper"* because *"sharing would create a cross-domain dependency that `conventions/folder-organization.md` warns against,"* and the council copy adds *"This copy is load-bearing for resumption; keep its semantics exact."* That is a documented, in-code rejection of exactly audit-report M5. Task 9 silently reverses it. **That much I hold** — a plan must not silently reverse a load-bearing in-code NOTE.

But the **HIGH severity does not survive** scrutiny on two grounds the panel converged on:

1. **The precedent defeats the cross-domain objection.** `context/domains/atomic_io.py:1-5` is a real, shipping shared-domain helper whose own docstring states it is *"Kept in the `domains` package so any domain store can import without creating cross-domain dependencies."* A new `context/domains/log_scan.py` peer is structurally identical — a leaf helper under `domains/` that two sibling stores import, creating no *upward* or *sideways-between-stores* arrow (both depend **down** on `log_scan`, not on each other). So the NOTE's stated rationale ("no common parent module") is now **false**: `atomic_io.py` already establishes the common-parent pattern. The dedup is genuinely safe and has precedent.
2. **`folder-organization.md` does not actually forbid it.** Re-read against the live doc: the cross-cutting rule (`folder-organization.md:69-73`) says a helper that "a sibling domain with the same need would want … goes top-level" — `log_scan.py` *is* that top-level-of-`domains/` placement, so the plan's naming choice (`log_scan.py`, no leading underscore — `folder-organization.md:54`) is **convention-compliant**, not a violation. The convention the NOTE invokes therefore does not bar the extraction; it arguably *endorses* the `log_scan.py` location.

So the substantive defect is **not** "the extraction is wrong/dangerous" (it isn't — Tests confirms the two wrappers keep incompatible signatures `(features_dir, feature_id, stage, agent)` vs `(workspace, round_num, persona)` and read different logs, so resumption is untouched). The defect is **purely procedural**: the plan must *quote and rebut* the two NOTE blocks — citing `atomic_io.py` as the precedent and noting the NOTE's "no common parent" premise is stale — rather than reverse a documented decision in silence. That is a MEDIUM ("the plan reopens a closed decision without acknowledging it"), not a HIGH ("the work itself is unsound"). **Final severity: MEDIUM.** Tests' framing ("safe but overrides an explicit in-code decision") and Architecture's implicit clearance both land here; I converge with them.

### Item 2 — Task 12 scope: my LOW vs Architecture's HIGH → **CONCEDE toward MEDIUM** (Architecture's HIGH overstates; my LOW understates)

**Position: my round-0 LOW was too soft; I raise it to MEDIUM. I do NOT fully concede Architecture's HIGH** — the "must edit two out-of-scope files, no alternative" framing is one valid implementation, but not the only one, and the real defect is the spec's *headline mechanism* being unbuildable within the named files, which is a scope/spec gap, not a wave-collision that "stalls parallel work."

Decisive code trace (all re-verified):
- `extract()` returns `{"nodes", "edges", "input_tokens", "output_tokens"}` and **nothing else** — `extract/__init__.py:244-249`. The extractor's per-file bytes (`generic.py:42 source = path.read_bytes()`) are consumed inside the extractor and discarded; they never leave `extract()`.
- The textual-reference pass is a **separate pass in a different package**: `build_structure(extraction: dict, code_files: list[Path], root, ...)` (`structure.py:52-58`) receives only the `{nodes,edges,...}` dict + the path list, and at `structure.py:120` calls `_derive_textual_references(effective_files, root_abs, ...)` — passing **paths**, not bytes.
- `references.py` does its **own independent read**: `_read_text_safely(path)` at `:79`, defined `:164-176`, doing a fresh `path.read_text(...)` at `:174`. It does **not** import `pipeline.extract` (grep clean) and receives no extractor bytes.
- Orchestration confirms the two passes are wired in series with only the dict between them: `runner.py:114 extraction = extract(...)` → `runner.py:118 structure = build_structure(extraction, code_files, ...)`.

So the plan's headline verb — *"thread the bytes the extractor already read into the textual-reference pass"* (plan.md:35, checklist.md:24) — **cannot be implemented within `references.py`+`generic.py`+`incremental.py`**: to carry the extractor's bytes into `references.py`, they must be (a) added to `extract()`'s return (`extract/__init__.py:244`) and (b) threaded through `build_structure`'s signature + the `:120` call (`structure.py:52,120`). **Both files are outside Task 12's list.** Architecture is correct on this mechanism.

**Why I defend against the full HIGH, and land at MEDIUM:** the spec's *invariant* (spec.md:37 — "within one `build_all`, each file read once, all passes see the same bytes") is satisfiable by a **second** implementation that does NOT thread from `extract()`: a path-keyed read-through cache (`read_source_bytes(path)`) that both the extractor and the references pass call, so the second touch is a cache hit. This is exactly the seam Tests' HIGH on `spec.md:77` proposes ("a single named read seam all three consumers call"). **But** — and this is why it's still a real gap, not a LOW — even that cache, to dedup the *extractor's* read against the *references* read, must live at a layer **both** `generic.py` (in `pipeline/extract`) and `references.py` (in `pipeline/build`) can import; the lowest such layer is `pipeline/io` (where Task 1 is already adding `paths.py`) or the `extract`/`build_structure` orchestration seam — **again outside the three named files**. If the dedup is confined to `references.py`+`incremental.py` only (both already call `read_bytes`/`read_text`), it dedups those two passes against each other but leaves the extractor's read uncounted — which does **not** deliver the spec's headline and would make the read-counter acceptance (≤2/file across *all* read sites, spec.md:77) fail because the extractor read is a third touch.

Conclusion: there is no implementation that (i) honours the spec's stated headline ("thread the **extractor's** bytes") AND (ii) hits the ≤2-reads-across-every-site acceptance AND (iii) stays within `references.py`+`generic.py`+`incremental.py`. The scope is genuinely incomplete — **MEDIUM**. The fix is the plan's choice: either add `extract/__init__.py`+`structure.py` to Task 12 (Architecture's remedy) **or** re-scope the spec headline+acceptance to a `pipeline/io` read-through cache (then `paths.py`/Task 1 is the natural home and the three-file list grows by one io module). I decline HIGH only because the wave-gating still physically serializes Task 12 after Task 4 (no two agents hold a file simultaneously — the collision Architecture fears is a *missing-file* defect, not a *concurrent-write* defect), so "stalls parallel work" overstates the runtime risk; the defect is "as-written the task can't meet its own acceptance," which is squarely MEDIUM. **My internal-consistency LOW (plan.md:45 vs :35) is subsumed by and upgraded to this MEDIUM.**

### Item 3 — Security's "vacuous proof strings" HIGH → **CONCUR (strongly), with one amplification**

**Position: CONCUR.** Verified empirically, not just by reading the grammar. `_FILE_LINE_RE = r"`([\w./\-]+\.[A-Za-z0-9]{1,6}):(\d+)`"` (`extract.py:32-34`) requires a literal `.ext` (1–6 alnum) immediately before `:line`. I ran it against the spec's strings:

```
REJECT  `../../../../etc/passwd:1`   (no extension → no match)
REJECT  `/etc/passwd:1`              (no extension → no match)
MATCH   `../../secrets.env:1`        → ../../secrets.env   (reaches _resolve_cited_path)
MATCH   `/tmp/x.json:1`              → /tmp/x.json         (reaches _resolve_cited_path)
MATCH   `/etc/cron.d/evil.sh:1`      → /etc/cron.d/evil.sh (reaches _resolve_cited_path)
```

The match group from `_FILE_LINE_RE.finditer` (`extract.py:67`) is the only producer of `kind="file:line"` claims, and only those claims drive `_resolve_cited_path` → the `:144` `resolved.open("rb")` read. So a `file:line` claim that doesn't match the regex **is never created**, never resolved, never opened. The spec.md:65 acceptance — "spy `Path.open`, assert never invoked for `` `../../../../etc/passwd:1` ``" — is therefore **satisfied by the current UNPATCHED code** (the string can't even become a claim), a textbook false pass. This compounds my own round-0 confirmation that all five `_resolve_cited_path` return branches (`verify.py:180,183,185,193,196`) are unconfined: the branches **are** vulnerable, but the headline test doesn't exercise them.

**Amplification Security can fold in:** the spec names *three* example strings at spec.md:65 — `` `../../../../etc/passwd:1` ``, "an **absolute** `/etc/passwd:1`", and "an in-repo symlink escaping root." The **first two are both extensionless and both grammar-rejected** — so two of the three headline proofs are vacuous, not one. Only the symlink case (which can point at an extension-bearing in-repo name) and extension-bearing escapes (`../../secrets.env:1`, `/tmp/x.json:1`) are the genuinely reachable attack surface. The fix is exactly as Security states: re-specify the acceptance proof strings to **extension-bearing** escapes, and keep an extensionless control *documented as grammar-rejected (not confinement-rejected)* so the test records *why* it's safe. I dispute nothing here.

### Item 4 — Task 4(a) seam (me + Architecture + Security agree it's mis-located) → **endorse the `collect_files` leaf-emission gate (Architecture option a); it stays in-scope**

**Position: I endorse gating at `collect_files` leaf emission (Architecture's option a), NOT Security's "before `load_cached` at `:163`."** Reasoning from the verified read topology:

- The module `extract/__init__.py` reads **zero file bytes** itself — confirmed: grep for `read_bytes|read_text|open(` in that file returns nothing; the only bytes-reads on the leaf path are `generic.py:42` (extractor) and `cache.py:38` (via `file_hash`→`load_cached` at `extract/__init__.py:163`).
- Security's "gate before `load_cached` at `:163`" is correct that the cache-hash read at `:163` is the *first* touch of the out-of-tree file, so a gate must precede it. **But** putting the gate at `:163` only covers the `extract()` walk's own read sites; it does **not** cover `_read_text_safely` in the *separate* `references.py` pass (`:79`), which re-walks `effective_files` independently. A single per-leaf containment filter at enumeration time covers **both** consumers at once.
- `collect_files(target, *, follow_symlinks=False, root=None)` (`extract/__init__.py:252-293`) is **entirely in Task 4's in-scope file** and is the single chokepoint where every leaf path is produced (`:289-293` for the `follow_symlinks=True` branch — the only branch that chases symlinked dirs; the default `rglob` branch at `:268-276` already excludes dotfiles and never follows symlinked dirs). Rejecting a leaf whose `os.path.realpath` escapes `root.resolve()` right there (the `for fname in filenames` loop, `:289-292`) means the out-of-tree path is **never emitted**, so neither the cache-hash read (`:163`) nor the extractor read (`generic.py:42`) nor the references read (`references.py:79`) ever receives it. **This forces no out-of-scope edit** — `collect_files` is in `extract/__init__.py`, Task 4's only named file.

**The honest caveat (which I keep from round 0, and Security raised too):** a `collect_files` filter is **walk-time, not read-time**, so it is TOCTOU-weaker than a check at the moment of `open` — a symlink swapped *after* enumeration but *before* the read could still redirect. The plan's own words promise "at read time … robust to TOCTOU" (plan.md:18). So endorsing option (a) means the plan must **drop the "read time / TOCTOU-robust" claim** and state the weaker-but-in-scope guarantee honestly ("rejected at enumeration; a post-walk symlink swap is out of scope for a local single-user tool"). The genuinely read-time alternative (gate inside `generic.py:42`) lands the guard in **Task 12's file** (`generic.py`) — the exact collision Architecture flags — and still misses `references.py`'s independent read. Given the trust model (local tool, untrusted-repo-content adversary, no concurrent attacker swapping symlinks mid-build), the walk-time `collect_files` gate is the right cost/coverage trade and the only one that (i) stays in Task 4's file and (ii) covers both read passes with one check. **Endorsed: option (a), with the corrected (non-TOCTOU) wording.**

### Status of my round-0 findings

- **Task 9 / re-proposes rejected decision** — **softened-to-MEDIUM** (held the "silent reversal" defect; conceded severity: `atomic_io.py:1-5` precedent + `folder-organization.md:69-73` defeat the cross-domain objection, so the dedup is safe — the fix is "quote-and-rebut the NOTE," not "drop the task").
- **Task 8 / stale trailing-flag premise** — **confirmed** (MEDIUM). Unchallenged by peers; Architecture independently corroborates (`c574d41` already shipped the trailing guard; only unknown-flag + `usage_error` routing + USAGE doc is net-new).
- **Task 4(a) / read-time location mismatch** — **confirmed** (MEDIUM), and reinforced by Architecture's HIGH + Security's MEDIUM. Resolution: gate at `collect_files` leaf emission (in-scope), drop the "read-time/TOCTOU-robust" wording.
- **Task 11 / bare-bool return can't express three transitions** — **confirmed** (MEDIUM). Un-disputed; the bare `bool` from `confidence.py` cannot distinguish demoted/restored/unchanged nor carry the prior value `X`, so "emit at the CLI from the existing return value" understates the change.
- **Task 2 / `__init__.py:74` re-export omitted** — **confirmed** (MEDIUM). Complements Architecture's note that `render.py:16-17`'s in-file call site must also be repointed; clean retirement touches `render.py` def + `confidence.py:16` import + `__init__.py:74` re-export + `render.py:16-17` self-use.
- **Task 6 / GATE not in conflict with C2/C3-resolved** — **confirmed** (LOW/INFO, no finding). Architecture and data-integrity both independently verify the GATE targets the still-open cross-backend divergence, not the resolved single-backend seed/tiebreak; `cluster.py:10-11` docstring over-claim stands.
- **Task 5 / hyperedges unsorted premise** — **confirmed** (INFO). Corroborated by architecture + data-integrity against `c574d41~1`. (Data-integrity adds a worthwhile strengthening — the key must be *total*, e.g. `json.dumps(h, sort_keys=True)`; I concur that's a real acceptance tightening, not a contradiction of my finding.)
- **Task 12 / internal-consistency LOW (plan.md:45 vs :35)** — **softened-to-MEDIUM and merged into Item 2 above** (the file-list omission is not cosmetic prose drift; the named headline mechanism is unbuildable within the three listed files).
- **Task 10 / cite `:88-93` not `:91-93`** — **confirmed** (LOW). Tests and architecture both land on the same ~1-line drift; meaning unchanged.
- **Task 1 / "inline guards all `.resolve()`" loose generalization** — **confirmed** (LOW). Architecture's 6-guard enumeration verifies each site; the generalization is loose (two sites operate on already-resolved paths) but every citation resolves.
- **Spot-check citation pass + report-drift notes (P2 `:99-111`, help.py USAGE)** — **confirmed** (INFO). No peer disputed; all spot-checked symbols verified.
