## data-integrity findings

> Lane: on-disk write atomicity, serialization determinism, persistence consistency for the JSON artifacts this tool commits (`symbol-graph.json`, `map/*.json`, reality-check `feature.json`/`INDEX.json`, the content-addressed extraction cache). Every bullet verified against REAL source under `dummyindex/` and against git history (`c574d41`, `3d7d539`, `d7d2e74`). Severity = impact ON PLAN QUALITY.

### Determinism — committed-artifact byte-reproducibility

- `export/graph.py:36` — **info** (confirmation) — Task 5 (sort `hyperedges`) is genuinely outstanding and correctly scoped to hyperedges only. Evidence: `c574d41` added `data["nodes"].sort(...)` + `data["links"].sort(...)` + `sort_keys=True` (now at `graph.py:40-41,43`), but left `data["hyperedges"] = getattr(G,"graph",{}).get("hyperedges", [])` (`:36`) un-sorted. `git show c574d41~1:export/graph.py` confirms nodes/links sort + sort_keys did NOT exist before. Plan's line cites (`graph.py:40-41` nodes/links, `:36` hyperedges) match the real file exactly. Suggested fix: none — plan is accurate.

- `export/graph.py:36` — **low** — the hyperedge sort key must be **total**, or Task 5 still leaves a nondeterminism window for duplicate/partial-key hyperedges. Evidence: the committed `symbol-graph.json` currently has `hyperedges: []` (verified by loading the committed file — 0 entries today), so the bug is latent now; but `pipeline/build/__init__.py:122-126` accumulates hyperedges via `.extend(ext.get("hyperedges", []))` across extractor outputs in file-iteration order, and `build/validate.py:94-96` shows hyperedges are dicts with arbitrary extra fields (schema 1.2 flow hyperedges). Suggested fix: Task 5 should sort on a canonical total key (e.g. `json.dumps(h, sort_keys=True)`) — a single named field is insufficient given the open schema. Plan says "stable key" without naming one; the acceptance bullet (`spec.md:58`) should pin "total key" to be testable.

- `analysis/cluster.py:11,42,52,109` — **info** (confirmation) — the spec's framing of the GATE is accurate: `c574d41` added `_RANDOM_SEED=42` (`:11`), passed it to `leiden(G, random_seed=_RANDOM_SEED)` (`:42`) and `louvain_communities(..., seed=_RANDOM_SEED)` (`:52`), and added the content tiebreak `final_communities.sort(key=lambda c:(-len(c), sorted(c)[0]))` (`:109`). `git show c574d41~1:analysis/cluster.py` confirms bare `leiden(G)` with no seed and no tiebreak before. This fixes **single-backend** determinism only. Suggested fix: none.

- `analysis/cluster.py:34-47` — **high** — CROSS-backend nondeterminism is REAL and remains OPEN despite `3d7d539` "mark C2/C3 resolved"; the plan is correct to keep Task 6 a GATE. Evidence: `_partition` does `from graspologic.partition import leiden` inside `try/except ImportError` (`:34-47`) — Leiden when graspologic is installed, NetworkX Louvain otherwise. The two algorithms produce **different `community` partitions** for the same graph; `_RANDOM_SEED` only stabilizes each backend against itself, not Leiden-vs-Louvain. So committed `community` bytes differ by which backend is present on the building machine — the exact cross-machine reproducibility invariant `spec.md:35` / `conventions/data-access.md` demand. The spec's claim "no injectable backend seam today" (`spec.md:83`) is accurate: there is no parameter or dependency-injection point; the backend is chosen by import success inside the function. Suggested fix: none — this is the correct reason the GATE blocks; flag only that `3d7d539`'s commit message overstates resolution (C2 is single-backend-resolved, cross-backend-open).

- `cluster.py:42,120` / `features/builder.py:76` — **high** — GATE strategy **(c) "exclude `community` from committed bytes" is a breaking contract change** — there IS a live consumer of the node `community` field, so the spec's "confirm no consumer reads node `community` before choosing (c)" (`spec.md:83`) resolves to FALSE for (c) as a drop-in. Evidence: `context/domains/features/builder.py:76` does `by_community[n.get("community", -1)].append(n["id"])` to group nodes into features, and its docstring (`builder.py:45-46`) states `graph_data` is "the same dict already written to `.context/graph/graph.json`" — i.e. the committed graph's `community` node field drives feature scaffolding. The committed `symbol-graph.json` today carries `community` on all 4212 nodes (verified). Suggested fix: the GATE decision record must note (c) requires re-routing `builder.py` to an on-demand cluster call (not the committed field) — it is not a pure omission. Strategies (a)/(b) keep the field and are safer for this consumer.

- `cluster.py:113-128` — **medium** — `_split_community`'s recursive second Leiden pass is seeded (it calls the same `_partition`, `:120`), but the GATE-determinism argument must account for it: an oversized community triggers a *second* backend-dependent partition (`:101-102` → `_split_community`), so cross-backend divergence compounds, not just the top-level call. Evidence: `cluster.py:98-104` splits when `len(nodes) > max_size` via `_split_community` → `_partition(subgraph)` (`:120`). Single-backend determinism holds (same seed); cross-backend does not, same as the primary call. Suggested fix: none for code; the GATE acceptance (strategy a/b) test should exercise a graph large enough to hit the split path so the second `_partition` is covered, else the test under-verifies.

- `spec.md:35` / `plan.md:39` / `checklist.md:28` — **info** (confirmation) — the plan correctly flags that Tasks 5 & 7 cause a ONE-TIME committed-bytes change, so Wave-4 verification compares **two POST-change rebuilds**, not vs the pre-change committed file. Evidence: `spec.md:35` ("Tasks 5 (community) and 7 (hyperedge sort) cause a one-time committed-bytes change… expected, not a regression"), Task 13 `plan.md:39` ("byte-identical across two post-change rebuilds (not vs the pre-change committed file, which Tasks 5/5 intentionally change once)"), `checklist.md:28` mirrors it. Note: `plan.md:39` mislabels the pair as "Tasks 5/5" (typo — should read Tasks 5/7 per the community+hyperedge split; community is the GATE/Task 6, hyperedge is Task 5). Cosmetic, not an integrity gap. Suggested fix: correct "5/5"→ the intended task numbers in `plan.md:39`.

### Atomic confidence mirror (Task 10)

- `confidence.py:56-60,91-93` — **high** — the two-file confidence write is currently NON-atomic-together: `feature.json` and `INDEX.json` are written by two **independent** `_atomic_write` calls, so a crash/exception between them leaves `feature.json` flipped to AMBIGUOUS while `INDEX.json` still shows the old value (or the inverse on promote). Evidence: demote path `_atomic_write(feature_json, …)` (`:56`) then a separate `_mirror_confidence_to_index(…)` whose own `_atomic_write(index_path, …)` is at `:111`; promote path identically at `:91` then `:93`→`:111`. Each call is individually atomic (tmp+`replace`, `render.py:62-66`) but there is no cross-file ordering guarantee. Task 10 correctly targets this. Suggested fix: none — plan's "stage both `.tmp`, replace back-to-back" narrows the window correctly.

- `confidence.py:89-93` — **high** — the plan's "mirror INDEX **before** popping the stash" is the correct failure-safe order, and the current code is WRONG (it pops first). Evidence: `promote_feature_on_clean` does `del payload[DEMOTED_FROM_KEY]` (`:90`) and `_atomic_write(feature_json, …)` (`:91`) — committing the stash-pop to disk — **before** `_mirror_confidence_to_index` (`:93`). If the mirror then fails, the stash is already gone from `feature.json`, so a retry can no longer restore the prior confidence (the restore guard `:85-87` requires the stash present). This is a real data-loss window (the demoted-from value is unrecoverable). Suggested fix: none — Task 10's mirror-before-pop ordering fixes exactly this; the fault-injection acceptance (`spec.md:70`, raise before first replace leaves both unchanged) covers it.

- `confidence.py:108-111` — **medium** — the "no INDEX entry matched" silent no-op is REAL: `_mirror_confidence_to_index` loops `idx.get("features", [])` and only mutates entries whose `feature_id` matches (`:108-110`), then unconditionally re-writes `INDEX.json` (`:111`). If NO entry matches the `feature_id`, the function rewrites INDEX unchanged and returns with no signal — `feature.json` now says AMBIGUOUS but INDEX has no corresponding row, a silent persistence inconsistency. Evidence: no return value, no flag, no log on the zero-match path. Task 10 ("surface a 'no INDEX entry matched' signal instead of a silent no-op") correctly targets it. Suggested fix: none.

- `confidence.py` (Task 10 scope) — **medium** — `sort_keys` is currently MISSING on BOTH confidence dumps, a determinism gap for committed `feature.json`/`INDEX.json`. Evidence: `json.dumps(payload, indent=2)` (`:56`, `:91`) and `json.dumps(idx, indent=2)` (`:111`) — no `sort_keys=True`. Key order is therefore insertion-dependent; `INDEX.json`/`feature.json` are committed artifacts, so non-sorted keys can churn bytes across writers. Task 10 ("`sort_keys=True` on every persisted dump") covers it. Suggested fix: none.

- `confidence.py` / Task 10 honesty — **info** (confirmation) — the plan is honest that the fix is **best-effort, single-replace window**, not crash-atomic. Evidence: `plan.md:31` ("document the single-replace window as best-effort"), `spec.md:70` ("residual single-replace window is documented as best-effort (INDEX may lag, reconciled next run) — not claimed crash-atomic"), `spec.md:111` deliberately-left item 2 (no journal/sentinel). This is the correct scope for two independent `replace`s without a journal. Suggested fix: none.

### Cache accept-superset schema + save determinism (Task 3)

- `cache.py:97` — **high** — `sort_keys=True` is currently MISSING on `save_cached`, a real determinism gap on the cache artifacts. Evidence: `tmp.write_text(json.dumps(result), encoding="utf-8")` (`:97`) — no `indent`, no `sort_keys`. The `{hash}.json` cache entries are content-addressed by *source* hash but their own serialized key order is insertion-dependent, so two runs that re-extract the same file can write byte-different cache JSON. The cache dir is gitignored (not a committed artifact), so impact is lower than `symbol-graph.json`, but Task 3d correctly adds `sort_keys=True`. Suggested fix: none — confirm Task 3d also fixes the determinism, not just schema.

- `cache.py:60-77` — **medium** — the proposed minimal accept-superset schema (payload is a dict; `nodes`+`edges` are lists; every node has a string `id`; unknown keys ignored) would re-validate EXISTING valid entries WITHOUT mass invalidation. Evidence: `load_cached` currently returns `json.loads(...)` with NO shape check (`:75`) — any JSON parses as a hit; the consumer `extract/__init__.py:177` does `result.get("nodes", [])` / `result.get("edges", [])`, and `save_cached` (`cache.py:80-83` docstring) already writes "a dict with 'nodes' and 'edges' lists". So a current well-formed entry has exactly the shape the new schema accepts; the "ignore unknown keys" clause preserves entries carrying extra fields (e.g. `raw_calls`, `input_tokens`). The acceptance (`spec.md:49`: three malformed shapes miss + one real entry hits) is the right test. Suggested fix: none — but the schema check must NOT require `id` on EDGES (edges use `source`/`target`, not `id`); plan correctly scopes the `id`-string check to nodes only (`plan.md:15`).

- `cache.py:10-17` — **high** — the `_body_content` `.md` frontmatter bug is REAL and worse than a horizontal-rule truncation: `text.find("\n---", 3)` is a **loose substring** match, not line-anchored, so it closes the frontmatter at the first occurrence of `\n---` *even when that is not a fence on its own line*. Verified by executing the current code: input with a fake fence `---hack` (a `---` NOT on its own line) is matched and the real body below it is dropped from the hash; an in-body `\n---\n` horizontal rule after a *legitimate* close is handled fine only because the real close comes first, but any file whose YAML body/value contains an earlier `\n---` substring truncates at the wrong point. Consequence: the cache key is computed over the WRONG content → a real content change below the spurious cut does not invalidate the cache (stale extraction served). Anchoring to `^---\s*$` fixes it, and I verified the whole-file fallback when there is no closing fence is preserved (`return content`, `:17`). Suggested fix: none — Task 3c is correct; add a regression case for the `---hack` (non-bare `---`) shape, not only the horizontal-rule shape, since that is the sharper bug.

### Cached-dict immutability (Task 4c)

- `extract/__init__.py:189-197` — **high** — `id_remap` currently MUTATES IN PLACE the node/edge dicts that originate from `load_cached`, violating the immutability convention (`conventions/coding-practices.md`) AND creating an intra-build consistency hazard. Evidence: `load_cached` returns a fresh `json.loads` dict (`cache.py:75`), but the build loop does `per_file.append(cached)` (`:165`) and `all_nodes.extend(result.get("nodes", []))` (`:177`) — `all_nodes` holds the **same node-dict references** as the cached results in `per_file` (no copy). Then `for n in all_nodes: … n["id"] = id_remap[n["id"]]` (`:190-192`) mutates those shared dicts. Crucially `per_file` is consumed AGAIN afterward — the cross-file import passes (`py_results`/`java_results` derived from `per_file`, `:201,210`) and the `raw_calls` resolver (`for result in per_file: for rc in result.get("raw_calls", …)`, `:224-225`) — so a later pass observes post-remap ids while `global_label_to_nid` (`:216-221`) was built from the same now-mutated `all_nodes`. The mutation is intra-process only (the on-disk cache file is not rewritten — `save_cached` only fires on a miss, `:168-169`), so it does not corrupt the persisted cache, but it does mean the "immutable-by-default" invariant (`spec.md:38`) is currently broken on the cache-hit path. Suggested fix: none — Task 4c's `{**n, "id": …}` rebuild is correct; the acceptance (`spec.md:55`: object returned by `load_cached` is unmutated after `extract`) only fully proves it if the test holds a reference to the cached dict BEFORE it enters the build (since `load_cached` already returns a fresh copy, asserting on a re-`load_cached` would not catch the aliasing — assert on the object captured from the first call). Strengthen the acceptance to name that.

### Cross-cutting

- `render.py:62-66` vs `atomic_io.py:11-24` — **low** — Wave 1 Task 2 (retire `render.py:_atomic_write`, point both `render.py` and `confidence.py` at `write_text_atomic`) is correct and removes a real duplication, but note the two writers are **behaviourally identical** today (both tmp + `replace`), so the unification is a safety/maintainability win (one stable seam before Tasks 10/11 fan out), not a bug fix — no integrity defect exists in the current `render.py:_atomic_write`. Evidence: both do `path.parent.mkdir(...)`, `tmp = path.with_suffix(path.suffix + ".tmp")`, `tmp.write_text(...)`, `tmp.replace(path)`. Suggested fix: none.

- `verify.py:144` — **info** (out-of-lane note for the panel) — the reality-check line-count read `resolved.open("rb")` (`:144`) is the read-oracle site Task 7d gates behind `is_safe_read_target`; from a pure data-integrity stance it is a READ (no write, no serialization), so no atomicity/determinism consequence — it belongs to the security/trust-boundary lane, flagged here only to disclaim it is not a persistence gap. Suggested fix: none (defer to security auditor).

### Verdict summary

- Determinism work from `c574d41` is **partially** done and the plan's scoping is accurate: nodes/links sort + `sort_keys` (C3) and single-backend seed+tiebreak (C2) landed; **hyperedge sort (Task 5)** and **cross-backend `community` (Task 6 GATE)** remain genuinely open. `3d7d539`'s "C2/C3 resolved" message overstates C2 (single-backend only).
- The atomic confidence mirror (Task 10) targets THREE real defects — non-atomic-together two-file write, pop-before-mirror data-loss ordering (`confidence.py:90-93`), and the silent no-INDEX-match no-op — plus the missing `sort_keys`. Plan is honest about best-effort scope.
- Cache: `sort_keys` missing on save (real determinism gap), the accept-superset schema will not mass-invalidate (correct), and `_body_content` loose-match is a real stale-cache hazard sharper than the plan's horizontal-rule framing.
- `id_remap` in-place mutation of cache-returned dicts is real and creates an intra-build ordering hazard; Task 4c fixes it.
- No finding rises to *critical for plan quality*: every defect I confirmed is correctly addressed by an outstanding task; the highs are "the gap is real and the fix as-specified closes it" — strengthen two acceptance bullets (total hyperedge sort key; capture the cached-dict reference before build for the immutability test).

## Round 1 — rebuttals (data-integrity)

> I re-verified every load-bearing claim below against REAL source this round (not plan.md): `confidence.py:1-112` (full), `features/builder.py:46-76`, `context/build/common.py:130-141`, `pipeline/io/cache.py:43-77`, `pipeline/extract/__init__.py:163-232`, `pipeline/build/__init__.py:105-126`, `pipeline/build/validate.py:81-96`, `export/graph.py:30-43`, and the committed `.context/features/symbol-graph.json` (loaded: 4212 nodes, **all 4212 carry `community`**, **hyperedges = empty list**). The code wins; positions are grounded in it.

### 1. GATE strategy (c) is a breaking contract change — **DEFEND (reconfirmed, HARDENED)**

I **defend** my round-0 HIGH, now with the consumer re-proven live and the spec's own precondition shown to resolve FALSE.

- **The consumer is live and reads the committed field.** `features/builder.py:76` — `by_community[n.get("community", -1)].append(n["id"])` — is the feature-grouping pass; it buckets every node by its `community` value to form one feature per community. Its docstring is explicit that the input is the committed graph dict: `builder.py:46` — "Caller owns `graph_data` (the same dict already written to `.context/graph/graph.json`)" — and `:50-51` confirms it consumes the graph's `community`/`source_file` node fields directly. The committed `symbol-graph.json` carries `community` on **all 4212 nodes** (re-verified by load). So the field is not vestigial; it is the primary key of feature scaffolding.
- **The spec's precondition for (c) resolves to FALSE — verbatim.** `spec.md:83` requires, before choosing (c): "confirm no consumer (`tree-enrich`, **feature-grouping**, `features/graph.json`) reads the node `community` field." `builder.py:76` IS feature-grouping and IS that reader. The precondition is therefore violated; (c) cannot be taken as a drop-in omission.
- **Consequence (the GATE-decision input):** strategy (c) ("exclude `community` from committed bytes") is **not a pure omission** — it requires **re-routing `builder.py` to an on-demand cluster call** (compute partitions at scaffold time instead of reading the committed node field), an unscoped code change to `scaffold_features` that the plan does not list. Choosing (c) without that re-route silently breaks feature grouping: every node would fall to the `-1` default bucket (`builder.py:76`), collapsing all features into one. This is a strong reason for the GATE to prefer (a) or (b).
- **Do (a)/(b) keep the field safe for this consumer? YES.** Both (a) "require graspologic / always-Leiden" and (b) "always-Louvain for committed bytes" **retain `community` on the committed nodes** — they change only *which backend* computes the partition, not whether the field exists. `builder.py:76` reads whatever value is present and is agnostic to the producing algorithm. So (a)/(b) are drop-in safe for the `builder.py` consumer; only (c) is breaking. Net: the GATE should weigh that (c) carries an extra, unscoped `builder.py` re-route that (a)/(b) do not.
- **Cross-backend determinism (the reason the GATE exists) still holds as round-0 stated.** `cluster.py` chooses Leiden-vs-Louvain by import success with no injectable seam (`spec.md:83` is accurate). Note this is *why* (c) is even on the table — but (c)'s consumer cost is the part the spec under-specifies, and that is my defended point. (Also re-affirming round-0: `3d7d539`'s "C2/C3 resolved" overstates C2 — single-backend only.)

### 2. Task 3a cache-dir confinement — data-integrity angle — **CONCUR with Security's corrected mechanism; one integrity consequence to record**

I **concur** with Security's BLOCK→HIGH: the plan's trust-channel model is **inverted**, verified against source.

- **Mechanism, re-proven:** `cache_dir_override` sets the **env var**, not the threaded channel — `common.py:134`: `os.environ[key] = str(target.resolve())` (key = `DUMMYINDEX_CACHE_DIR`). And `cache_dir()` reads that env var **first and short-circuits**: `cache.py:51-53` — `override = os.environ.get(...)`; `if override: d = Path(override).resolve()` returns before `root` is ever consulted (`:54-55` else-branch is dead whenever the var is set). So the plan's claim (`spec.md:30`: "`cache_dir_override` keeps working by passing its target through the trusted `cache_root` channel") is false in both directions — the override IS the env-var channel the plan wants to confine, and `cache_root` never carries the dir decision when the var is set. Security and Architecture both land this; I confirm it independently.
- **Data-integrity consequence of getting the confinement wrong:** there IS a real integrity angle beyond access-control, and it is a **cache-correctness / cross-repo-bleed** risk, distinct from Security's read-oracle framing:
  - If confinement is implemented as the plan literally describes (confine the env-var read, trust `cache_root`), then because the env var short-circuits `cache_dir()` at `cache.py:51-53`, **confining it rejects the env value and falls back to `<root>/.context/cache/`** — which silently **relocates where cache entries are read/written** versus where they were last written under an override. The two real override callers point at `<out_root>/.context/cache/` (in-repo per Security's `runner.py:109`/`enriched_refresh.py:100` trace), so a fallback to `<root>/.context/cache/` can land in a **different directory than the prior write**, producing cache **misses that mass-re-extract** (not corruption, but a determinism/perf regression and a churned cache dir).
  - The sharper latent integrity hazard the confinement is actually *protecting against*: a **shared/cross-repo `DUMMYINDEX_CACHE_DIR`**. Cache entries are content-addressed by **source-hash only** (`cache.py:60-77`, `file_hash` over file bytes), with **no repo identity in the key**. So if two different repos point `DUMMYINDEX_CACHE_DIR` at the same dir and contain two different files with identical content-hash-but-different-extraction-context, one repo's `{hash}.json` is served to the other — **cross-repo cache bleed → wrong nodes/edges injected into the consuming repo's `symbol-graph.json`**. This is the genuine data-integrity payoff of confinement (keep the cache in-repo), and it is the same self-/cross-poisoning surface Security rates MEDIUM. So: confining the cache dir to in-repo is **integrity-positive** (prevents cross-repo bleed + mass invalidation); confining it *the way the plan describes* is **integrity-negative for the legitimate override path** (rejects the real callers' target and forces re-extraction).
  - **The corrected mechanism is integrity-safe:** Security's fix (i) "confine the env-var value AND silently fall back, never raise" — combined with Tests' `test_cache_env_var_is_restored` regression note (`tests.md:23`) — yields the right behavior: an out-of-repo env value is ignored (no bleed), the in-repo default is used (entries still found where the trusted callers wrote them, since those callers target `<out_root>/.context/cache/` which IS in-repo). **Concur** with that mechanism; with it, there is **no net integrity regression** and a real cross-repo-bleed mitigation gained.
- **Bottom line:** beyond what Security/Architecture cover (inverted model, broken escape hatch), the data-integrity-specific note is: the confinement's *purpose* is cache-correctness (no cross-repo bleed via the repo-identity-free key) and its *failure mode if mis-implemented* is mass cache invalidation / wrong-dir reads — so it MUST silently fall back, not raise, and confine to the in-repo default. Add that "silently fall back + repo-local default" requirement to Task 3a alongside Security's re-derivation of the `spec.md:48` acceptance bullet (whose premise is the inverted model).

### 3. Atomic-mirror findings (`confidence.py`) — **DEFEND (all confirmed against full-file read)**

I **hold** all four round-0 findings; re-verified line-exact against the full `confidence.py` (1-112) this round. Security and Tests concur the fix is honestly best-effort, and I agree it is correctly bounded.

- **Non-atomic two-file write — CONFIRMED.** Demote: `_atomic_write(feature_json, …)` at `:56`, then a *separate* `_mirror_confidence_to_index(...)` at `:58` whose own `_atomic_write(index_path, …)` is at `:111`. Promote: `:91` then `:93`→`:111`. Each call is individually atomic but there is **no cross-file ordering guarantee** — a raise between them leaves `feature.json` flipped while `INDEX.json` lags. Task 10's "stage both, replace back-to-back" narrows this correctly.
- **Pop-before-mirror data-loss ordering (`:90-93`) — CONFIRMED, and it is the sharpest defect.** `promote_feature_on_clean` does `del payload[DEMOTED_FROM_KEY]` at `:90` and writes `feature.json` at `:91` — **committing the stash-pop to disk** — *before* `_mirror_confidence_to_index` at `:93`. The restore guard `:85-87` requires the stash present; so if the mirror fails after `:91`, the stash is gone and the demoted-from value is **unrecoverable on retry**. This is a true data-loss window. Task 10's "mirror INDEX before popping the stash" fixes exactly this; the fault-injection acceptance (`spec.md:70`, raise before first replace leaves both unchanged) covers it. (Note: the seam is `Path.replace` inside `write_text_atomic`, per Tests `tests.md:13` — the fault-injection must patch `Path.replace`, not `os.replace`. I concur with that precision.)
- **Silent no-INDEX-match no-op (`:108-111`) — CONFIRMED.** `_mirror_confidence_to_index` loops `idx.get("features", []) or []` (`:108`) and mutates only matching `feature_id` entries (`:109-110`), then **unconditionally** re-writes `INDEX.json` (`:111`). On zero matches it rewrites unchanged and returns `None` — no flag, no log, no signal — leaving `feature.json` at AMBIGUOUS with no corresponding INDEX row: a silent persistence inconsistency. Task 10's "surface a no-match signal" targets it. (Architecture flagged my `:108-111` cite as "past meaningful code"; the file is exactly 112 lines and the no-match loop is `:108-110` with the unconditional rewrite at `:111` — my range is correct, not past EOF. Tests independently anchors the loop at `:108-110` — concur, cosmetic.)
- **Missing `sort_keys` on BOTH dumps — CONFIRMED.** `:56`/`:91` = `json.dumps(payload, indent=2)`; `:111` = `json.dumps(idx, indent=2)` — **none** pass `sort_keys=True`. `feature.json`/`INDEX.json` are committed artifacts, so insertion-ordered keys can churn bytes across writers. Task 10's "`sort_keys=True` on every persisted dump" covers it. Hold.

### 4. Strengthening asks — **DEFEND both (4a HARDENED with schema evidence; 4b held, Tests concurs)**

I **hold** both strengthening asks; 4a is now backed by the open-schema evidence and the build accumulation order.

- **(a) Hyperedge sort key must be TOTAL — DEFEND, with evidence the schema is open.** `export/graph.py:36` emits `data["hyperedges"]` unsorted while nodes (`:40`) and links (`:41`) are sorted and the dump is `sort_keys=True` (`:43`) — so hyperedges are the sole unsorted collection (Task 5 correctly scoped). The order is **input-dependent**: `build/__init__.py:126` accumulates via `combined["hyperedges"].extend(ext.get("hyperedges", []))` across extractor outputs in iteration order. And the element schema is **open**: `build/validate.py:81-82` — "Schema 1.2 introduces flow hyperedges with extra fields; … all extras are optional" — and `:94-96` validates a list of dicts with arbitrary extra keys. A single named sort field therefore **does not totally order** the collection (ties on that field leave the extend-order nondeterministic across machines). Only a **total key** — `json.dumps(h, sort_keys=True)` — gives a deterministic order over the open schema. The bug is **latent today** (committed `symbol-graph.json` has `hyperedges = []`, re-verified — hence I keep this `low`/`info`, not `high`), but Task 5 should pin the total key now, before flow hyperedges populate. `spec.md:58` says "sorted by a **stable key**" and must be tightened to "total key" to be testable. **Hold.**
- **(b) `id_remap` immutability test must capture the cached-dict reference BEFORE build — DEFEND; Tests concurs.** Re-verified the aliasing: `extract/__init__.py:177` does `all_nodes.extend(result.get("nodes", []))` — `all_nodes` holds the **same dict objects** as the cache-hit results in `per_file` (`:165` `per_file.append(cached)`, where `cached` is `load_cached`'s fresh `json.loads` dict, `cache.py:75`), no copy. Then `:190-192` mutates `n["id"]` in place on those shared objects. The aliasing is provable-hazardous because `per_file` is consumed **again** afterward: `py_results`/`java_results` derive from it (`:201,210`) and the `raw_calls` resolver re-iterates it (`:224-225`), so a later pass observes post-remap ids on the same objects. The on-disk cache file is NOT corrupted (no rewrite on hit — `save_cached` only fires on miss, `:168-169`), but the in-process invariant is broken. **The test trap:** `load_cached` already returns a *fresh* copy per call (`cache.py:75`), so asserting on a **re-`load_cached`** would compare a brand-new dict and **miss the aliasing entirely** (false pass). The test MUST capture the very object identity that enters the build — i.e. patch/spy `load_cached` to return a sentinel dict and assert *that captured object's* `id` is untouched after `extract` (Tests names exactly this mechanism, `tests.md:37`: "test patches `load_cached` to return a sentinel dict and asserts its `id` field is untouched post-`extract`"). `spec.md:55` says only "the object returned by `load_cached` is unmutated" without pinning *which* call's object — must specify "the object captured from the call that feeds the build," not a re-load. **Tests concurs; hold.**

### Status of my round-0 findings

- `export/graph.py:36` (Task 5 outstanding, hyperedges-only) — **info** — **CONFIRMED / unchanged.** Re-verified against `graph.py:36,40-41,43`; Architecture and Correctness concur.
- `export/graph.py:36` (hyperedge sort key must be **total**) — **low** — **DEFENDED / HARDENED.** Backed this round by open-schema evidence (`validate.py:81-82,94-96`) + extend-order accumulation (`build/__init__.py:126`); latent today (committed `hyperedges = []`). Ask to pin `spec.md:58` to "total key" stands.
- `analysis/cluster.py:11,42,52,109` (single-backend determinism landed) — **info** — **CONFIRMED / unchanged.** Correctness and Architecture concur (`correctness.md:21`, `architecture.md:27`).
- `analysis/cluster.py:34-47` (CROSS-backend nondeterminism real & open; GATE correct; `3d7d539` overstates C2) — **high** — **DEFENDED / unchanged.** All four peers concur the GATE is genuinely still-open (no injectable seam).
- `cluster.py:42,120` / `features/builder.py:76` (GATE strategy (c) is a breaking contract change) — **high** — **DEFENDED / HARDENED.** Consumer re-proven live at `builder.py:76` reading the committed dict (`builder.py:46`); `spec.md:83` precondition resolves FALSE; consequence = (c) needs an unscoped `builder.py` re-route to an on-demand cluster call; (a)/(b) keep the field safe. **Primary GATE input — see item 1.**
- `cluster.py:113-128` (`_split_community` second pass compounds cross-backend divergence) — **medium** — **CONFIRMED / unchanged.** No peer disputes; ask that the GATE test exercise the split path stands.
- `spec.md:35` / `plan.md:39` (Tasks 5 & community = one-time bytes change; compare two post-change rebuilds; `plan.md:39` "5/5" typo) — **info** — **CONFIRMED.** Tests independently verifies no committed-golden assertion breaks (`tests.md:29,50`); the "5/5"→"5/7" typo fix stands (cosmetic).
- `confidence.py:56-60,91-93` (non-atomic two-file write) — **high** — **DEFENDED / unchanged.** Re-verified full-file; Security concurs (`security.md:27`), fix honestly best-effort.
- `confidence.py:89-93` (pop-before-mirror data-loss ordering) — **high** — **DEFENDED / unchanged.** Sharpest defect; `:90` pop → `:91` write → `:93` mirror confirmed; Security + Tests concur; seam is `Path.replace` (Tests precision adopted).
- `confidence.py:108-111` (silent no-INDEX-match no-op) — **medium** — **DEFENDED / unchanged.** Loop `:108-110` + unconditional rewrite `:111` confirmed; my range is in-bounds (file = 112 lines), Architecture's "past code" note rebutted; Tests anchors `:108-110` — concur cosmetic.
- `confidence.py` Task-10 missing `sort_keys` — **medium** — **CONFIRMED / unchanged.** `:56,:91,:111` all lack `sort_keys=True`; verified.
- `confidence.py` Task-10 honesty (best-effort, single-replace) — **info** — **CONFIRMED.** Security concurs the scope is correctly bounded (`security.md:27`).
- `cache.py:97` (`sort_keys` missing on `save_cached`) — **high** — **CONFIRMED / unchanged.** `:97` = `json.dumps(result)` (no indent, no sort_keys); gitignored artifact so lower blast radius; Task 3d fixes determinism + schema.
- `cache.py:60-77` (accept-superset schema won't mass-invalidate; `id`-check nodes-only) — **medium** — **CONFIRMED / unchanged.** Security concurs it's anti-corruption not anti-poisoning (`security.md:23`); Tests confirms the positive control is writable (`tests.md:41`).
- `cache.py:10-17` (`_body_content` loose `\n---` match → stale-cache hazard) — **high** — **CONFIRMED / unchanged.** No peer disputes; ask for a `---hack` (non-bare) regression case stands.
- `extract/__init__.py:189-197` (`id_remap` in-place mutation of cache-returned dicts) — **high** — **DEFENDED / HARDENED.** Aliasing re-proven via `:177` extend (shared refs) + `:201,210,224-225` second consumption of `per_file`; immutability-test seam ask reinforced — see item 4b. Tests concurs (`tests.md:37`).
- `render.py:62-66` vs `atomic_io.py:11-24` (Task 2 unification is safety win, not bug fix) — **low** — **CONFIRMED.** Security + Architecture confirm byte-equivalence (`security.md:27`, `architecture.md:31`). Note: Correctness adds the `__init__.py:74` re-export site (`correctness.md:17`) and Architecture the `render.py:16-17` in-file caller (`architecture.md:13`) — I **concede** Task 2's edit list is wider than my round-0 cross-file note implied (3 sites: def, `confidence.py:16` import, `__init__.py:74` re-export, plus `render.py:16-17` callers); out of my lane but I record the concession so the panel folds it in.
- `verify.py:144` (read-oracle line-count is a READ, no persistence consequence) — **info** — **CONFIRMED / deferred to security.** Tests confirms `Path.open` at `:144` is the real sink (`tests.md:17`); Security holds the trust-boundary finding (`security.md:15`). No data-integrity consequence — unchanged.
