## tests findings

> Lane: test/acceptance quality of the **plan** (`.context/proposals/outstanding-audit-fixes/{plan,spec,checklist}.md`), judged against the CURRENT post-`c574d41` source. Every spy/mock boundary the plan names was checked against the real call site. Verified: `c574d41 --stat` (reality_check split + 3 new test files confirmed), graspologic **ABSENT** in the test venv, `extract/__init__.py` cache lines, all `Path.read_*` sites, both `latest_status` bodies, every reality-check/determinism/resumption test that could break.

### Spy-boundary mismatches (named acceptance test cannot be written as specified)

- `spec.md:77` — **high** — The P2 read-counter spy boundary is **not constructible as named**. The spec says "a spy wrapping **every** read site (`Path.read_bytes`, `Path.read_text`, the hash reader)" and "the incremental `--changed` hash read is **excluded** from the budget". But the three reads it wants to discriminate collapse onto **two `Path` methods**: the extractor read (`generic.py:42` `source = path.read_bytes()`) and the cache "hash reader" (`cache.py:38` `raw = p.read_bytes()`) are **the same method** `Path.read_bytes` — there is no separate "hash reader" method to wrap; and the `--changed` hash (`incremental.py:341 file_hash → cache.py:38`) is *also* `Path.read_bytes`. A spy on `Path.read_bytes` cannot tell "extract" from "hash" from "the excluded `--changed` hash". The "≤2/file **and** exclude the `--changed` hash" assertion therefore requires **path-keyed or call-stack discrimination the plan never names** — evidence: `grep read_bytes cache.py generic.py incremental.py` → all three route through `Path.read_bytes`. Suggested fix: re-word the P2 acceptance to a **path-keyed** counter (`defaultdict[Path,int]` in the wrapper, asserting `counts[f] <= 2` per source path) and define "exclude" as "the `_hash_files` pass over a path already counted does not push it past 2", OR have Task 12 introduce a single named read seam (e.g. `read_source_bytes(path)`) that all three consumers call so the spy has one wrap point.

- `spec.md:62`, `spec.md:83` — **high** — GATE strategy **(b)** acceptance ("a spy asserts the **committed path never calls Leiden**") is **non-constructible in the actual test env**: `graspologic` is **ABSENT** (verified `importlib.util.find_spec('graspologic')` → None), and `cluster.py:35` does `from graspologic.partition import leiden` inside `try/except ImportError`. With graspologic absent there is **nothing to spy on** — the import already raises and falls to Louvain (`:46-56`), so a "Leiden never called" assertion passes **vacuously / for the wrong reason** (it was never importable), proving nothing about the committed path. The spy seam only exists when graspologic is installed; the plan does not state that strategy (b)'s test requires graspologic-present in CI. Suggested fix: if (b) is chosen, the test must `monkeypatch graspologic.partition.leiden` with an installed graspologic and assert non-invocation **under a present backend** — and the plan must pin that env; otherwise prefer strategy (a)/(c) whose tests are env-robust (see next).

- `spec.md:61` — **medium** — GATE strategy **(a)** ("monkeypatch the import to raise; assert exit/raise") **is** constructible (graspologic-absent already exercises the fallback; forcing the require-path to fail-fast is testable), but it is **environment-coupled**: the require-graspologic behaviour can only be *positively* tested (build succeeds with Leiden) where graspologic is installed, which this venv is not. Plan does not flag that (a)'s happy-path needs a backend present. Suggested fix: name the two halves — fail-fast when absent (env-robust) vs. Leiden-used when present (needs the optional dep) — so build doesn't tick (a) on a half-tested assertion.

- `plan.md:3`, plan high-leverage check #1 — **low** — Atomic-mirror fault-injection seam is mis-described as `os.replace`. The reused writer `write_text_atomic` (`atomic_io.py:11-24`) ends with `tmp.replace(path)` (`:24`) — that is **`Path.replace`**, not `os.replace`. The "raise **before the first replace**" test must monkeypatch `pathlib.Path.replace` (or inject at the `write_text_atomic` call), not `os.replace`. (`cache.py:99` *does* use `os.replace`, but that is the cache writer, not the confidence mirror.) Constructible either way; flag is precision so the test author picks the right symbol. Suggested fix: name the seam `Path.replace` (or "the `write_text_atomic` call") in Task 10's acceptance.

### Spy boundaries that ARE the real call site (recorded so they aren't re-disputed)

- `verify.py:143-146` — **info** — The reality-check line-count read **is** `Path.open`: `line_count = sum(1 for _ in resolved.open("rb"))` (`:144`). The spec's "spying `pathlib.Path.open` (the real read call site)" (`spec.md:65`) is **correct** — and it is the single read for *every* `_resolve_cited_path` return branch, since the branches return a `Path` and the open happens once back in `_verify_claim`. Caveat (low): `resolved.is_file()` (`:141`) stats the target *before* the open, so an out-of-root path is `stat`'d even though never `open`ed — the assertion "Path.open never invoked for an out-of-root path" still holds, but "never touches the target" is slightly stronger than the code delivers. None needed.

- `spec.md:30`, `extract/__init__.py:163,169` — **info** — Cache trust-channel citations are exact: `load_cached(path, cache_root or root)` at `:163`, `save_cached(...)` at `:169`; and `runner.py:111` wraps extraction in `cache_dir_override(cache_target)`, confirming `build_all` feeds the cache dir through the **trusted** channel, not the raw env-var read (`cache.py:51`). Task 3a's split is sound and testable as written. None needed.

### "Existing tests pass unchanged" — risks the plan does/doesn't flag

- `tests/context/build/test_no_cache_leak.py:47-57` — **medium** — Task 3a (confine the `DUMMYINDEX_CACHE_DIR` env-var channel to in-root) interacts with a **pre-existing test the plan never names**: `test_cache_env_var_is_restored` pre-sets `DUMMYINDEX_CACHE_DIR=/tmp/some_prior_value` (an **out-of-repo** path) before `build_all`. It survives *only* because `cache_dir_override` (`runner.py:111`) overwrites the env var with the in-repo target *during* the run and restores it after — the test asserts restoration, not where the cache landed. The plan's acceptance (`spec.md:48`) tests the env-var-channel ignore with a *fresh* fixture but does not call out that this existing test exercises the same code path with an out-of-root value; a naive Task-3a implementation that confines at `cache_dir()` *and* fails/raises on an out-of-root override (instead of silently falling back) would break it. Suggested fix: add an explicit "Task 3a must **silently fall back**, never raise, on an out-of-root env var; `test_cache_env_var_is_restored` is the regression guard" note to Task 3.

- `tests/context/domains/test_reality_check.py` (whole file, 1-579) — **low** — Wave-1 Task 2 (retire `render.py:_atomic_write`, point `confidence.py:16` at `write_text_atomic`) and the `sort_keys=True` additions in Tasks 10/11 are **safe against this suite**: every assertion uses `json.loads(...)` (semantic), e.g. `:255`, `:467-468`, `:483-486` — none asserts feature.json/INDEX.json **bytes**, so re-ordering keys won't break them. Confirms the plan's "existing reality-check tests pass unchanged" claim. None needed.

- `tests/export/test_graph_determinism.py:40-75` — **low** — Tasks 5 (sort `hyperedges`) and the C2/C3 determinism fix (`community`) do **not** risk this `c574d41` golden: the fixture builds nodes/links only (no `hyperedges`, `_COMMUNITIES` is a literal `:36`) and asserts **run-to-run equality** of two freshly-written files (`out_forward.read_bytes() == out_reverse.read_bytes()`, `:49`,`:59`), never against a committed file. So Task 5's "two consecutive rebuilds emit byte-identical hyperedges" (`spec.md:58`) needs a **new fixture carrying hyperedges** — the plan does name a new test, good — but the *existing* determinism test gives Task 5 no coverage and is not a breakage risk. None needed.

- `tests/context/build/test_output_hygiene.py:104,158`, `tests/context/build/test_graph.py:47` — **info** — No existing test asserts committed `symbol-graph.json` **bytes**: hygiene reads it as text from a freshly-`built_context` and asserts a `.gitattributes` line; `test_graph.py` asserts `community_count >= 1`, not exact community IDs. Confirms `spec.md:35`'s "one-time committed-bytes change… expected, not a regression" — **no committed-golden assertion breaks**. None needed.

- `tests/context/domains/{test_council_cli.py:214-223, audit/test_audit_domain.py:295}` — **info** — Task 9 dedup is safe: the two `latest_status` have **incompatible signatures** (`council.py:332` `(features_dir, feature_id, stage, agent)` matching `entry.stage/agent`; `audit/log.py:166` `(workspace, round_num, persona)` matching `entry.round/persona`) and read different logs. Resumption tests call the **wrappers** (`latest_status(...)`), not the proposed shared `last_matching`. The plan's "both siblings import it; signature takes no domain object; resumption tests pass unchanged" is correct — each wrapper reads its own log + builds its own predicate. (Note: the current docstrings at `council.py:339-344` / `log.py:169-175` say the two are "**deliberately NOT** extracted… cross-domain dependency"; Task 9 reverses that, sidestepping the objection via a *new common parent* `log_scan.py` — the rationale is sound but the plan should note it is overriding an explicit in-code decision.) None needed.

### Acceptance ↔ testability gaps (risky new logic; is the named test writable?)

- `plan.md:17` (Task 4b), `extract/__init__.py:216-221` — **medium** — `global_label_to_nid` collision disambiguation: acceptance (`spec.md:54`, "two same-named symbols in different files… disambiguated or skipped, never bound to the last node") is **realistically writable** and the citation is exact (`global_label_to_nid[normalised.lower()] = n["id"]` at `:221`, consumed at `:229`). But the assertion is **outcome-shaped, not seam-shaped**: the test must construct two files whose symbols normalise to the same `.lower()` key, run `extract`, and assert the `calls`/`uses` edge is **absent or correctly targeted** — there is no exposed map to inspect, so the test reads the *edge set* of the result. Writable, named adequately. Suggested fix: none (acceptance already specifies the observable); optionally have Task 4b expose the collision count for a sharper assert.

- `plan.md:17` (Task 4c), `extract/__init__.py:189-197` — **medium** — `id_remap` immutability: acceptance (`spec.md:55`, "the object returned by `load_cached` is unmutated after `extract`") is writable but the **named seam is awkward**. The current in-place mutation is `n["id"] = id_remap[n["id"]]` (`:192`) on dicts that came from `load_cached`. To prove non-mutation the test must (a) seed a cache entry, (b) capture/`deepcopy` the dict `load_cached` returns, (c) run `extract`, (d) assert the captured dict is unchanged — i.e. it must spy/patch `load_cached` to grab the very object identity that gets remapped. Constructible but fiddly; the plan names the assertion, not the mechanism. Suggested fix: add to Task 4c "test patches `load_cached` to return a sentinel dict and asserts its `id` field is untouched post-`extract`" so the author isn't left to reverse-engineer the object identity.

- `plan.md:24` (Task 7), `verify.py:376-384`, `verify.py:63` — **medium** — Nested-`.context/` fixture for `_repo_root_from_meta`: acceptance (`spec.md:67`) demands "the **nested-`.context/`** fixture (where `context_dir.parent` is not the repo root)". The hazard the test must hit is `verify.py:63`: `repo_root=Path(_repo_root_from_meta(context_dir) or context_dir.parent)` — today `_repo_root_from_meta` (`:376-384`) returns `meta["root"]` **verbatim, ungated** (no git-toplevel check exists yet), and falls back to `context_dir.parent`. The existing `fake_context` fixture (`test_reality_check.py:47-110`) sets `meta["root"] = str(root)` where `root == context_dir.parent`, so it **cannot** exercise the nested case. A genuinely new fixture (subtree where `meta["root"]` ≠ `context_dir.parent` ≠ git-toplevel) is required and must include a real git toplevel for the trusted-anchor branch. Writable, but the plan should note the existing fixture is insufficient and the fixture needs an actual `.git` (or a monkeypatched `is_git_repo`) to test the positive control at `spec.md:67`. Suggested fix: name "new fixture + `.git` dir (or patched `is_git_repo`/`resolve_git_dir`)" in Task 7b.

- `plan.md:15` (Task 3b), `cache.py:60-77` — **low** — "A valid cache entry still hits" positive control (`spec.md:49`) is writable and well-specified: feed three malformed shapes + one real `save_cached` entry, assert miss×3 + hit×1. The seam is clean (`load_cached` is the public function, `save_cached` produces the "real entry"). Note the test must `save_cached` *through the current code* (not hand-write JSON) so it proves "no mass invalidation" of genuinely-produced entries. None needed.

- `plan.md:31` (Task 10), `confidence.py:90-93,108-111` — **low** — Mirror-before-pop + no-match-signal: the line citations drift by one. The stash pop is `del payload[DEMOTED_FROM_KEY]` at **`:90`** (plan says `:91-93`), the feature.json write at `:91`, the INDEX mirror at `:93`. The "no INDEX entry matched" silent no-op is the `for entry in idx.get("features", [])` loop at **`:108-110`** that simply doesn't find a match (plan says `:108-111`). Both are real and the fix is constructible (fault-inject `Path.replace` before the first replace; assert both files unchanged). Citations are ~1 line off — cosmetic. Suggested fix: re-anchor Task 10 to `:90` (pop) and `:108-110` (no-match loop).

- `plan.md:23` (Task 7c CLI guard), `cli/reality_check.py:39` — **low** — `--feature ../../escape` rejection at the CLI boundary: `feature_id = parsed.get("feature")` is at `:39`; the guard slots between `:45` (required-check) and `:57` (the `reality_check_feature` call). Acceptance (`spec.md:66`, "rejected… right after parsing `--feature`… no write outside `.context/features/`") is writable via `dispatch([...,"--feature","../../escape",...])` asserting exit≠0 and no file outside `features/`. The "same guard at `verify.py:30`" is also reachable (`feat_dir = context_dir / "features" / feature_id` at `:30` would escape on `..`). Citations accurate. None needed.

### Net assessment

- **Two HIGH** plan-quality defects: the P2 read-counter spy (`Path.read_bytes` shared by 3 named-distinct reads → "exclude the `--changed` hash" not expressible at the method level) and GATE strategy (b)'s "spy Leiden never called" being **vacuous with graspologic absent** (the test env). Both make the *as-written* acceptance test either unwritable or a false pass.
- The plan's **byte-change safety** claims (Tasks 5/7 one-time `symbol-graph.json`/`hyperedges`/`community` rewrite) **hold** — no existing committed-golden assertion breaks (verified across `test_graph_determinism`, `test_output_hygiene`, `test_graph`, `test_reality_check`).
- The "existing tests pass unchanged" claims **hold** for reality-check (semantic asserts) and resumption (`latest_status` wrappers untouched), with **one unflagged interaction**: `test_cache_env_var_is_restored` constrains Task 3a to silently fall back, not raise.
- Most spy targets are **correct** (`Path.open` at `verify.py:144`; cache `cache_root` lines exact); the atomic seam is `Path.replace` not `os.replace` (precision).

## Round 1 — rebuttals (tests)

> Positions taken against all four peer files (`correctness.md`, `security.md`, `architecture.md`, `data-integrity.md`), grounded in re-run code. New evidence this round: I executed `_FILE_LINE_RE` against every headline + extension-bearing proof string, re-confirmed `graspologic` absent, traced `file_hash`→`cache.py:38 p.read_bytes()`, read both `latest_status` NOTE blocks verbatim, and confirmed `atomic_io.py:24,45` is `tmp.replace`. The code still wins.

### 1. CONCUR (emphatically) — Security's "vacuous proof strings" HIGH (`security.md` HIGH at `extract.py:32-34`). This is the round's key cross-check and it survives empirical test.

I ran `_FILE_LINE_RE = re.compile(r"`([\w./\-]+\.[A-Za-z0-9]{1,6}):(\d+)`")` (`extract.py:32-34`, read fresh) against the spec's named acceptance strings (`spec.md:65`, `checklist.md:29`) and against Security's proposed replacements. Result, verbatim:

- **Headline strings — all REJECT:** `` `../../../../etc/passwd:1` `` → REJECT; `` `/etc/passwd:1` `` → REJECT; `` `../../.git/config:1` `` → REJECT; `` `/home/u/.ssh/id_rsa:1` `` → REJECT. None carries a `.<ext>` segment before `:line`, so the grammar never produces a `FILE_LINE` claim, so `_resolve_cited_path` (`verify.py:157-202`) is never entered, so the `resolved.open("rb")` sink at `verify.py:144` is **never reached**.
- **Extension-bearing escapes — all MATCH and reach the sink:** `` `../../secrets.env:1` `` → groups `('../../secrets.env','1')`; `` `/tmp/x.json:1` `` → `('/tmp/x.json','1')`; `` `../../../config/database.yml:1` `` → match; `` `/etc/cron.d/evil.sh:1` `` → match; and the headline target with an extension appended, `` `../../../../etc/passwd.bak:1` `` → match. Each becomes a `FILE_LINE` claim whose `path_str` flows through the un-gated `on_disk = repo_root / path_str` absolute-join branch (`verify.py:181-183`) to `resolved.open("rb")` at `:144`.

**Verdict:** The spec's headline proof strings MUST be replaced with extension-bearing escapes for the named acceptance test (`spec.md:65` / `checklist.md:29`) to actually exercise the confinement. As written, "spy `Path.open`, assert it is never called for `/etc/passwd:1`" is satisfied by the **current unpatched code** — a false pass that proves nothing about Task 7a/d. This is strictly worse than an unwritable test: it is a *green* test over a *live* hole.

Two cross-checks I add to Security's finding (both reinforce, neither weakens):
- The reachable surface is even slightly wider than Security's list: appending any 1–6-char alnum extension to an otherwise-rejected target (`/etc/passwd.bak:1`, `/etc/shadow.0:1`) re-admits it. So the regex is not a *containment* boundary at all — it is an *extension filter*, and "extensionless ⇒ safe" is incidental, not designed. The acceptance suite should keep one extensionless control **explicitly labelled "grammar-rejected, NOT confinement-rejected"** (Security's own recommendation) so a future regex relaxation doesn't silently turn the control into an un-noticed live escape.
- This interlocks with Security's `_repo_root_from_meta` HIGH (`verify.py:376-384`, `:63`): the extension-bearing escape is only filesystem-relative because `repo_root` is attacker-shippable via committed `meta.json`. The two HIGHs are one exploit chain (poisoned `root` × extension-bearing citation), and the proof-string fix is what makes the chain *testable*. My Round-0 `info` at `verify.py:143-146` already certified `:144` as the correct single sink; I now additionally certify that the **inputs** the spec feeds that sink are wrong.

### 2. DEFEND both my Round-0 HIGHs — held, with fresh evidence.

**(a) P2 read-counter spy is non-constructible as named — HELD.** Re-traced this round: `generic.py:42` `source = path.read_bytes()`; `cache.py:38` `raw = p.read_bytes()` (this IS the "hash reader" — `file_hash` at `cache.py:35-40` reads via `p.read_bytes()`, there is no separate hash-read *method*); the `--changed` budget-excluded read is `incremental.py:341 out[rel] = file_hash(p, root)` → the **same** `cache.py:38 p.read_bytes()`. So the spec's three "named-distinct" reads (`Path.read_bytes`, `Path.read_text`, "the hash reader") collapse to **one method, `Path.read_bytes`** for two of the three, and the third (`Path.read_text`) does not even appear on the extract/hash hot path I was asked to budget. A method-level spy on `Path.read_bytes` cannot discriminate "extract" from "cache-hash" from "the excluded `--changed` hash" — they are byte-identical call sites differing only by the *path argument* and the *caller*. The "≤2/file **and** exclude the `--changed` hash" assertion (`spec.md:77`) is therefore **not expressible at the method level**. This is corroborated independently by Correctness (`correctness.md` MEDIUM at `extract/__init__.py:268-293`: the read sites are `generic.py:42`/`references.py:79`, not the walk) and Architecture (`architecture.md` HIGH at `extract/__init__.py:1-306`: "grep `read_bytes|read_text|open(` in `extract/__init__.py` returns zero hits"). **Constructive fix (restated):** re-word P2 acceptance to a **path-keyed** counter — `defaultdict[Path,int]` in the wrapper asserting `counts[resolved_path] <= 2`, and define "exclude the `--changed` hash" as "the `_hash_files` pass over a path already counted (`incremental.py:341`) does not push that path's count past 2" — OR have Task 12 introduce a single named seam (`read_source_bytes(path)`) that all three consumers route through, giving the spy exactly one wrap point with a path argument to key on. Either makes the assertion expressible; the method-level wording does not.

**(b) GATE strategy (b) "spy asserts Leiden never called" is vacuous — HELD.** Re-confirmed `importlib.util.find_spec("graspologic")` → `None` in this venv, and `cluster.py:34-35` does `from graspologic.partition import leiden` **inside** `try/except ImportError` (read fresh: the import is at `:35`, the `except ImportError: pass` at `:46-47`, Louvain fallback `:49-56`). With graspologic absent the import raises on first execution and control falls to Louvain **before any Leiden symbol exists to patch or observe** — so "assert the committed path never calls Leiden" passes because Leiden was **never importable**, not because the build chose Louvain deliberately. The assertion proves nothing about the *committed path's choice*; it certifies the *test environment's dependency set*. Data-integrity (`data-integrity.md` HIGH at `cluster.py:34-47`) and Architecture (`architecture.md` info at `checklist.md:15`) both independently confirm the backend is selected by import-success inside the function with **no injectable seam** — which is precisely why a present-backend spy is the only way to exercise (b). **Constructive fix (restated):** if (b) is chosen, the test MUST install graspologic, `monkeypatch graspologic.partition.leiden` (or wrap it to record invocation), assert *non-invocation under a present backend*, and the plan MUST pin graspologic-present in that CI lane. Absent that pin, (b) is a tautology; prefer (a) fail-fast-when-absent (env-robust) or (c) — but see my concession to Data-integrity on (c) below.

> Concession that does **not** retract (b): Data-integrity (`data-integrity.md` HIGH at `cluster.py:42,120`/`builder.py:76`) shows strategy **(c)** "exclude `community` from committed bytes" is a breaking contract change — `builder.py:76` reads node `community` to scaffold features. I **concede** (c) is not a drop-in omission and concur it is the least safe of the three. This sharpens, not softens, my (b) defence: with (c) compromised and (b) vacuous-without-a-pinned-backend, strategy **(a)** is the only env-robust *and* consumer-safe option, and the plan's GATE must say so. My lane's contribution: even (a) is only *half*-testable here — fail-fast-when-absent is env-robust, but "Leiden-used-when-present" needs the optional dep (my Round-0 `medium` at `spec.md:61`, held).

### 3. CONCUR — Task 9 reverses an explicit in-code decision; and CONFIRM the dedup is test-safe.

I read both NOTE blocks verbatim this round. `council.py:332-344`: *"The two are deliberately **not** extracted into a shared helper … sharing would create a cross-domain dependency that `conventions/folder-organization.md` warns against — and the loop is too small to justify a new cross-cutting module. This copy is load-bearing for resumption; keep its semantics exact."* `audit/log.py:166-178` carries the byte-identical NOTE (minus the resumption sentence). Task 9 (extract into `context/domains/log_scan.py`) **reverses this**. I concur with Correctness (`correctness.md` HIGH at `log.py:166-181`/`council.py:332-350`) that — **regardless of Architecture's ruling** on whether `log_scan.py` is sound — the plan must explicitly **quote and rebut** these two NOTE blocks, not silently reopen the decision; this is the report's lowest-confidence M5, argued down *in the code itself*. Architecture (`architecture.md` info at `pipeline/io/__init__.py`) rules the *layering* clean via the `atomic_io.py` shared-domain-helper precedent (`atomic_io.py:1-5`) — I do not contest that ruling, but a clean layer is not a substitute for acknowledging the override. **My lane's positive contribution:** the dedup is **test-safe**. Both `latest_status` wrappers keep their incompatible signatures (`(features_dir, feature_id, stage, agent)` vs `(workspace, round_num, persona)`), each reads its **own** log (`read_log(features_dir, feature_id)` vs `read_log(workspace)`), and each builds its **own** predicate (`entry.stage==stage and entry.agent==agent` vs `entry.round==round_num and entry.persona==persona`). Resumption tests (`test_council_cli.py:214-223`, `audit/test_audit_domain.py:295`) call the **wrappers**, never the proposed shared `last_matching`/`log_scan` parent. So the extraction is behaviour-preserving and "resumption tests pass unchanged" holds — the objection is to the plan's *silence about the override*, not to test breakage.

### 4. CONCUR/CONFIRM — `Path.replace` precision and the `test_cache_env_var_is_restored` constraint (load-bearing for Security's corrected Task 3a).

- **`Path.replace`, not `os.replace`:** confirmed `atomic_io.py:24` and `:45` both end in `tmp.replace(path)` — that is `pathlib.Path.replace`. The Task-10 atomic-mirror fault seam ("raise before the first replace", `spec.md:70`) must monkeypatch `pathlib.Path.replace` (or inject at the `write_text_atomic` call), **not** `os.replace`. (`cache.py:99` uses `os.replace`, but that is the cache writer, not the confidence mirror — irrelevant to Task 10.) Data-integrity (`data-integrity.md` info at `render.py:62-66` vs `atomic_io.py:11-24`) confirms the two writers are byte-identical (`tmp.replace`), so the Wave-1 retire-and-repoint (Task 2) keeps the seam stable for the Task-10 fault injection. My Round-0 `low` at `plan.md:3` (held): the plan's `os.replace` label is a precision bug the test author must override.
- **`test_cache_env_var_is_restored` constrains Task 3a to silently fall back:** confirmed `test_no_cache_leak.py:47-57` pre-sets `DUMMYINDEX_CACHE_DIR="/tmp/some_prior_value"` (out-of-repo), runs `build_all`, asserts `os.environ["DUMMYINDEX_CACHE_DIR"] == "/tmp/some_prior_value"` — i.e. asserts **restoration**, never landing site. A Task-3a implementation that confines the env-var channel and **raises** on an out-of-root value would crash inside `build_all` before restore → test fails. So Task 3a **must silently fall back, never raise**. This is load-bearing for Security's corrected Task 3a mechanism: Security's BLOCK (`security.md` BLOCK at `common.py:130-141`/`cache.py:51-55`) re-frames the channel and notes `cache_dir_override`'s only two real callers point in-repo (`runner.py:109`, `enriched_refresh.py:100`) — so confinement *can* be made non-breaking — **but only if** it degrades to silent fallback rather than a raise, exactly the regression-guard constraint this test imposes. I **concur** with Security's corrected mechanism *and* supply the test that pins "silent fallback" as the non-negotiable behaviour. (I do not adjudicate Security's "channel is inverted" claim — that is the correctness/architecture lane — but my regression guard holds under *either* the plan's framing or Security's corrected one: whoever implements Task 3a, this test is the tripwire for "raise vs fall back.")

### Status of my round-0 findings

- `spec.md:77` — P2 read-counter spy non-constructible (**high**) — **HELD.** Re-traced; `generic.py:42`/`cache.py:38` both `Path.read_bytes`, `file_hash`→same; method-level spy cannot discriminate or exclude the `--changed` hash. Independently corroborated by correctness + architecture. Constructive fix (path-keyed counter / single named seam) restated.
- `spec.md:62`,`:83` — GATE (b) "Leiden never called" vacuous with graspologic absent (**high**) — **HELD.** `find_spec` → None re-confirmed; import inside try/except at `cluster.py:35`. Fix (pin graspologic-present + monkeypatch leiden) restated. Strengthened by conceding (c) is breaking (`builder.py:76`), leaving (a) as the only robust+safe strategy.
- `spec.md:61` — GATE (a) happy-path needs a present backend (**medium**) — **HELD.** Unchanged; reinforced by the (c)-is-breaking concession (raises (a)'s importance).
- `plan.md:3` — atomic seam is `Path.replace` not `os.replace` (**low**) — **HELD / CONFIRMED.** `atomic_io.py:24,45` `tmp.replace`. Cross-confirmed by data-integrity.
- `verify.py:143-146` — `Path.open` at `:144` is the real sink (**info**) — **HELD / UPGRADED in relevance.** Now the linchpin of item 1: the sink is correct, but the spec's *inputs* to it (`/etc/passwd:1`) are grammar-rejected, so the named test never reaches `:144`. Concur Security's vacuous-proof-strings HIGH.
- `extract/__init__.py:163,169` — cache trust-channel citations exact (**info**) — **HELD as citations**, but **scope-narrowed.** My Round-0 note said "Task 3a's split is sound and testable as written"; I now **partially concede** to Security's BLOCK that the *mechanism framing* (which channel `cache_dir_override` uses) is contested. My testability claim survives only via the `test_cache_env_var_is_restored` constraint (item 4): the test is writable *iff* Task 3a falls back silently. Citation accuracy unchanged; "sound as written" downgraded to "sound *if* re-derived per Security's correction + silent-fallback."
- `test_no_cache_leak.py:47-57` — Task 3a must silently fall back, not raise (**medium**) — **HELD / ELEVATED.** Now doubly load-bearing: guards both the plan's framing and Security's corrected mechanism.
- `test_reality_check.py` (whole) — Tasks 2/10/11 safe (semantic asserts) (**low**) — **HELD.** Unchanged.
- `test_graph_determinism.py:40-75` — Task 5 needs a new hyperedge fixture; existing test no breakage (**low**) — **HELD.** Concur data-integrity's add: the new fixture's sort must use a **total** key (`json.dumps(h, sort_keys=True)`), else the hyperedge determinism test under-verifies on partial-key dicts.
- `test_output_hygiene.py`/`test_graph.py` — no committed-bytes golden breaks (**info**) — **HELD.** Unchanged.
- `test_council_cli.py`/`test_audit_domain.py` — Task 9 dedup test-safe (**info**) — **HELD / promoted to item 3.** NOTE blocks read verbatim; wrappers read own logs + own predicates; resumption tests untouched. Plan must still acknowledge the override (concur correctness HIGH).
- `spec.md:54` (Task 4b global_label collision) — outcome-shaped, writable (**medium**) — **HELD.** Unchanged.
- `spec.md:55` (Task 4c id_remap immutability) — writable, awkward seam (**medium**) — **HELD.** Concur data-integrity's sharpening: the test must capture the cached dict **from the first `load_cached` call** (re-`load_cached` returns a fresh copy and would NOT catch the aliasing) — name that in Task 4c.
- `spec.md:67` (Task 7 nested-`.context/` fixture) — needs new fixture + real `.git`/patched `is_git_repo` (**medium**) — **HELD.** Concur security + architecture that `meta["root"]` anchoring is the right fix; my fixture-insufficiency note stands.
- `spec.md:49` (Task 3b cache schema positive control) — writable, clean seam (**low**) — **HELD.** Concur data-integrity: schema check must NOT require `id` on **edges** (edges use `source`/`target`); `id`-string check is nodes-only (`plan.md:15` already scopes it so).
- `confidence.py:90-93`/`:108-110` (Task 10 line drift) — citations ~1 line off (**low**) — **HELD.** All three lanes (mine, correctness, architecture, data-integrity) agree the region is real and ~1 line mis-anchored; re-cite to `:90` (pop) + `:108-110` (no-match loop). Fault-inject `Path.replace` (per item 4), not `os.replace`.
- `cli/reality_check.py:39` (Task 7c CLI guard) — writable at parse boundary (**low**) — **HELD.** Concur security that the parse-time guard closes both the read-oracle and the `--feature` write-primitive before `demote_feature_on_contradiction`.
