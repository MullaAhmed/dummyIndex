## architecture findings

> Scope: the plan/spec/checklist of `outstanding-audit-fixes` vs the REAL post-split source under `dummyindex/`. THE CODE WINS. Timeline verified: `c574d41` split `reality_check.py`→package + fixed determinism/dead-code/perf/CLI; `d7d2e74` recouncilled. The plan cites POST-split code. Every bullet cites `path:Lstart-Lend`.

### Wave / dependency soundness (the plan's core structural claim)

- `dummyindex/pipeline/extract/__init__.py:1-306` — **high** — **Task 4(a)'s read-time symlink-containment anchor does not exist in the named file.** Task 4(a) (plan.md:18) says "containment-check leaf targets with `resolve_under_root` **at read time** (just before `read_bytes`/`_read_text_safely`) … (`:268-293`)". A repo-wide grep for `read_bytes|read_text|open(` in `extract/__init__.py` returns **zero hits** — this module never reads file bytes. `extract()` (`:118`) only calls `collect_files` (path discovery) + per-language extractors; the cited `:268-293` is inside `collect_files` (`:252-306`), the `os.walk(followlinks=True)` *path-list* loop (`:278`), not a read site. The actual byte reads are in `generic.py:42` (`source = path.read_bytes()`) and `cache.py:38` (via `file_hash`→`load_cached` at `extract/__init__.py:163`). **Consequence:** to implement read-time containment, Task 4 must edit `generic.py` (and/or `cache.py`) — files NOT in Task 4's scope (`extract/__init__.py` only) but `generic.py` **is** in Task 12's scope (Wave 3). So the only place to put a read-time guard collides with Task 12, or forces an out-of-scope edit. Suggested fix: either (a) re-scope Task 4(a) to a containment filter inside `collect_files` (reject leaf paths whose `realpath` escapes `root` — doable in `extract/__init__.py`, honest to the file list), or (b) move the read-time guard into `generic.py:42` and explicitly serialize it before Task 12.

- `.context/proposals/outstanding-audit-fixes/plan.md:46` — **high** — **Task 12's wave rationale is factually wrong AND a file is missing from its scope.** Rationale: "Task 12 shares the `extract/__init__.py` read seam (Task 4)". Verified: `references.py` does **not** import `pipeline.extract` (grep clean), so there is no file-level dependency on `extract/__init__.py`. The real coupling is data-flow: `structure.py:120` calls `_derive_textual_references(...)`, and Task 12 wants to "thread the bytes the extractor already read" — but `extract()` currently returns only `{nodes, edges, …}` (`extract/__init__.py:90-95`), never the raw bytes. To thread bytes, Task 12 must edit **`extract/__init__.py`** (expose/return the bytes) and **`structure.py`** (pass them through) — **neither is in Task 12's file list** (`references.py`+`generic.py`+`incremental.py`). Suggested fix: add `pipeline/extract/__init__.py` and `pipeline/build/structure.py` to Task 12's scope, correct the rationale to "Task 12 edits `extract/__init__.py` (same file as Task 4) → genuine collision, serialize after Wave 2", and confirm the serialization the plan already imposes (Wave 3 after Wave 2) actually covers it.

- `.context/proposals/outstanding-audit-fixes/plan.md:42-46` — **medium** — **Wave 2 intra-wave disjointness holds; Wave 3 serialization is sound but justified by the wrong reason.** Verified Wave-2 file sets are pairwise disjoint: `cache.py` (T3) / `extract/__init__.py` (T4) / `export/graph.py` (T5) / `analysis/cluster.py` (T6) / `verify.py`+`cli/reality_check.py` (T7) / `cli/query.py`+`cli/help.py` (T8) / `audit/log.py`+`council.py`+`log_scan.py` (T9) — **no overlap, safe to fan out.** Cross-wave reuse is correctly serialized by the wave gate: `render.py` (T2→T11), `confidence.py` (T2→T10), `cli/reality_check.py` (T7→T11), and the `extract/__init__.py` seam (T4→T12) are all in *earlier* waves than their re-touch, so no two parallel agents ever hold the same file. The serialization is *adequate*; only its stated mechanism for T12 is wrong (prior bullet). Suggested fix: none for the gating itself; fix the rationale text.

- `.context/proposals/outstanding-audit-fixes/plan.md:17` — **low** — Task 2's "retire `render.py:_atomic_write`" understates the edit. `render.py:_atomic_write` (`render.py:62-66`) is **also used** by `write_report` at `render.py:16-17`, not only re-exported to `confidence.py:16`. Deleting it requires repointing `render.py:16-17` to `write_text_atomic` too (the plan implies this with "point both `render.py` and `confidence.py`", but the task title says only "Delete … and point both" — the in-file call sites must not be missed). Suggested fix: spell out "repoint `render.py:16-17` + `confidence.py:16`, then delete the def" so the implementer doesn't leave a NameError.

### `paths.py` layering (Task 1)

- `dummyindex/pipeline/io/__init__.py:1-44` — **info** — **`pipeline/io` is genuinely the lowest layer and the new primitive creates no cycle — claim verified.** `pipeline/io/__init__.py` imports only `.cache`/`.git`/`.detect` (stdlib/third-party beneath). `context/__init__.py:64` already does `from dummyindex.pipeline.io import is_git_repo` (and `:105` re-exports it), so the legal `context → pipeline.io` arrow the spec cites (spec.md "Contracts") is real. Adding a pure `paths.py` re-exported from `__init__.py` (`:14-26` pattern) introduces no upward import. `tests/pipeline/io/` exists (`__init__.py`, `test_git.py`) and is ready for `test_paths.py`. Suggested fix: none.

### "Trusted root per call site" coherence (Task 3 / Task 7)

- `dummyindex/pipeline/io/cache.py:51-57` — **info** — **The cache trust-split is architecturally coherent and verified.** `cache_dir()` reads `DUMMYINDEX_CACHE_DIR` (`:51`) with no containment today; the trusted `cache_root` is threaded `extract()`→`load_cached(path, cache_root or root)` (`extract/__init__.py:163,169`); `cache_dir_override` (`context/build/common.py:130`) sets that env var to a resolved out-of-repo temp dir legitimately. Confining only the env-var read while leaving `cache_root` unconfined is a defensible "trusted-channel vs untrusted-channel" split — BUT note: `cache_dir_override` sets the **same env var** the confinement targets (`common.py:130` `os.environ[key] = str(target.resolve())`), so the implementation must distinguish "env var set by the trusted override context-manager" from "env var set by an attacker." The plan's stated mechanism (spec.md Contracts: "`cache_dir_override` keeps working by passing its target through the trusted `cache_root` channel") implies re-plumbing `cache_dir_override` to NOT use the env var — that is a real, unstated code change to `common.py:129-140`. Suggested fix: make Task 3 explicit that `cache_dir_override` must be re-routed off the now-confined env var onto the `cache_root` channel, or the confinement will break it.

- `dummyindex/context/domains/reality_check/verify.py:63,376-384` — **medium** — **The reality-check `repo_root` weakness is real and the fix anchor is correct.** `reality_check_feature` builds `repo_root=Path(_repo_root_from_meta(context_dir) or context_dir.parent)` (`verify.py:63`); `_repo_root_from_meta` (`:376-384`) returns whatever string `meta.json`'s `root` holds with **no validation**, and the fallback is `context_dir.parent` ("any ancestor" → admits `/` for a top-level `.context/`). Task 7(b) correctly targets this. The trust anchor the plan names (git toplevel via `is_git_repo`/`resolve_git_dir`, both present at `pipeline/io/git.py:24,38`) is the right one. Suggested fix: none — well-specified.

### GATE escalation (Task 6)

- `.context/proposals/outstanding-audit-fixes/checklist.md:15` — **info** — **GATE is consistently marked and correctly scoped — verified.** `**GATE**` appears in plan.md:21 (task body), plan.md:44 (rationale), checklist.md:15, and spec.md:59/83 (Open question). Critically, the GATE is NOT moot despite `c574d41`: that commit already added same-machine determinism (`cluster.py:11` `_RANDOM_SEED=42`, `:42` `leiden(G, random_seed=_RANDOM_SEED)`, `:109` `key=lambda c: (-len(c), sorted(c)[0])`). The GATE addresses the *residual* cross-machine graspologic-vs-Louvain partition divergence (`cluster.py:35,46` `from graspologic.partition import leiden` inside `try/except ImportError` — no injectable seam), a genuinely still-open contract problem. The spec correctly demands a single-backend test (cross-backend equality is non-constructible). Suggested fix: none.

### Reuse-not-reimplement claims (spec Contracts)

- `dummyindex/context/domains/atomic_io.py:11` — **info** — `write_text_atomic` exists at `atomic_io.py:11` as claimed; `confidence.py:16` does `from .render import _atomic_write`; `render.py:62-66` defines it. The Wave-1 unification target is real and correctly located. Verified.

- `dummyindex/cli/query.py:60-68` — **low** — **Task 8 is partially redundant against `c574d41`; only part is net-new.** The audit's C4 ("trailing `--top-k` swallowed") was **already fixed** by `c574d41`: `query.py:60-68` now `print(f"error: {a} requires an integer value"); return 2` for a trailing value-flag. So Task 8's "route `query`'s four exit-2 paths through `usage_error`" is a *refactor* of existing exit-2 code (lines 37,44,51,58,67 already `return 2`), NOT new behavior; only the unknown-`--flag` rejection (currently `query.py:69-71` folds it into `leftover`) and the USAGE doc are genuinely new. The plan's citation `query.py:69-71` for "unknown flag folds into search string" is **accurate** (`:69-71` = `else: leftover.append(a); i += 1`). Suggested fix: re-scope Task 8 to "convert the 5 existing raw-print/return-2 sites to `usage_error` + add unknown-flag arm + USAGE doc" so it's not mistaken for adding the trailing-flag guard that already exists.

- `dummyindex/context/build/manifest.py:105` + 5 sites — **info** — **The spec's "~6 inline `resolve().relative_to()` guards" list resolves to real code.** Verified each: `cli/common.py:43` (`scope_resolved.relative_to(cwd)`), `context/reconcile_gate.py:67` (`sub.relative_to(root)`), `context/build/incremental.py:110` (`p.resolve().relative_to(root)`), `context/build/manifest.py:105` (`fp.resolve().relative_to(root).as_posix()`), `cli/check.py:79` (`p.resolve().relative_to(out_root.resolve())`), `cli/equip/discover.py:258` (`resolved.relative_to(project_root.resolve())`). The consolidation premise (net-new symbol, not net-new logic) is sound. Only `incremental.py` is migrated in-scope (Task 1, also touched by Task 12) — the rest deferred as a follow-up note, which is internally consistent. Suggested fix: none.

### Citation precision (low-impact drift)

- `dummyindex/context/domains/reality_check/confidence.py:90-94` — **low** — Task 10 cites `confidence.py:91-93` for "mirror INDEX before popping the stash" and `:108-111` for "no-match signal". The file's relevant logic is at `:90` (`del payload[DEMOTED_FROM_KEY]`) and `:93` (`_mirror_confidence_to_index(...)`) inside `promote_feature_on_clean`; the mirror **already follows** the pop in `promote` (`:90` then `:93`) and **precedes** it in `demote` (`:58` mirror after `:55`-ish stash) — the "mirror before pop" invariant is about ordering across both functions and the `:91-93`/`:108-111` ranges are slightly off / past meaningful code (file is ~111 lines, `_mirror_confidence_to_index` def at `:97`). The intent is sound; the line anchors drift by a few lines. Suggested fix: re-cite to `confidence.py:85-94` (promote) + `:55-61` (demote) so the implementer lands on the real ordering points.

- `dummyindex/export/graph.py:36,40-41` — **info** — Task 5 is correctly scoped and needed: `graph.py:36` emits `data["hyperedges"]` **unsorted** while nodes/links ARE sorted (`:40-41`, added by `c574d41`), and the dump is `sort_keys=True` (`:43`) — so hyperedges are the sole remaining unsorted collection. The plan's "matching the nodes/links sort (`:40-41`)" is accurate. Verified.

### Summary

- **Two high-severity scope defects** that would corrupt or stall parallel work: (1) Task 4(a)'s read-time symlink guard has **no anchor** in its only in-scope file (`extract/__init__.py` reads no bytes) and the real read sites are in `generic.py`/`cache.py` (the former is Task 12's file); (2) Task 12's rationale is wrong and it is **missing `extract/__init__.py` + `structure.py`** from its file list despite needing to edit them to "thread the extractor's bytes."
- **Two unstated code-changes** hidden behind "reuse" framing: re-routing `cache_dir_override` off the now-confined env var (Task 3), and repointing `render.py:16-17` (Task 2).
- **One redundancy**: Task 8's trailing-flag guard already shipped in `c574d41`.
- **Everything else is sound**: Wave-2 intra-wave disjointness holds, the layering for `paths.py` is clean and cycle-free, the GATE is consistent and genuinely still-open, the cache/reality-check trust anchors are coherent, and the 6-guard consolidation list + all major reuse symbols verify against real code.

## Round 1 — rebuttals (architecture)

> I argue three items in my lane against the round-0 peer findings, grounded in the real post-`c574d41` source (the code wins). Each position cites `path:range`. The pivotal call is Task 9 — it is mine to settle.

### 1. RULING on Task 9 (the cross-domain-dependency question) — **the extraction is architecturally SOUND; the plan defect is a missing acknowledge+rebut, not an ill-advised design. Held at HIGH, fix = (b) rebut, NOT (a) drop.**

I **concur with Correctness's HIGH** on the *existence* of the defect (the plan silently reverses a documented in-code decision) but I **dispute its framing that dropping Task 9 is a co-equal remedy** — only fix (b) is correct, because the extraction does not in fact violate the rule the NOTEs invoke. Reasoning, from the real code and the real convention:

- **What the rule actually forbids (read precisely).** `folder-organization.md:69-73` states the test verbatim: *"a module that merely *uses* a domain is not *part* of it — if a sibling domain with the same need would want it, it is cross-cutting and goes top-level. `export/graph.py` lives top-level because it is transport-shaped (graph → bytes), though both `context/build/graph.py` and the features scaffolder consume it."* The rule does **not** prohibit shared peer modules — it **prescribes** them for exactly the "two sibling domains, same need" case. What it forbids is one domain importing *another domain's internals*. A `last_matching(entries, predicate, attr="status")` helper that takes **no domain object** (plan.md:27) is the textbook cross-cutting extraction the rule endorses, not the cross-domain coupling it warns against.

- **`atomic_io.py` is an exact, live precedent — verified, not asserted.** It physically sits at `context/domains/atomic_io.py` as a **top-level peer module** directly under `domains/` (confirmed: `ls domains/` shows `atomic_io.py` as a sibling `.py` alongside the `audit/`, `memory/`, `proposals/`, `equip/` dirs, inside *none* of them). Its own docstring (`atomic_io.py:1-5`) states the precise principle Task 9 reuses: *"Kept in the `domains` package so any domain store can import without creating cross-domain dependencies."* And it is imported across **≥9 distinct domains/layers** — `audit/workspace.py:22`, `memory/{roll,nudge,breadcrumb,store}.py:14/6`, `proposals/store.py:18`, `equip/lifecycle/{evolve,status}.py:15/36`, plus `cli/debt.py:135`, `cli/plan_update.py:21`, `context/reconcile_gate.py:31`, `context/build/{graph,common}.py`, `context/output/claude_md.py:21` (verified by grep). So a domain-neutral peer helper under `domains/` is **the established pattern**, and `log_scan.py` is structurally identical to it: peer module, no-domain-object signature, each domain keeps its own wrapper + predicate. The NOTE's stated blocker — *"no common parent module"* (`log.py:172-175`, `council.py:340-343`) — is dissolved precisely by creating the sanctioned common parent, exactly as `atomic_io.py` did for the store modules.

- **Why it is nonetheless a HIGH defect (the code wins).** The two NOTE blocks are **live, committed code** added by `c574d41` (`audit/log.py:169-175`, `council.py:337-344`) that explicitly say the helpers are *"deliberately **not** extracted"* and *"keep its semantics exact."* Under this repo's own governing rule (CLAUDE.md, HOW_TO_USE: when `.context`/code disagree, the code wins), a plan that **reverses a documented in-code decision without quoting and rebutting it** is defective irrespective of whether the reversal is right. The plan (plan.md:27) cites `folder-organization.md:54` only for the *filename* rule (no leading underscore) and never engages the extraction objection sitting in the two docstrings. That silent reversal is the HIGH — not the design.

- **Required fix (precise).** Task 9 must (i) quote both NOTE blocks, (ii) rebut them on the `atomic_io.py:1-5` precedent above (shared domain-neutral peer ≠ cross-domain dependency), (iii) **delete/rewrite both stale NOTEs as part of the task** so code and decision re-converge, and (iv) preserve each wrapper's exact predicate — `council.latest_status` keys on `(stage, agent)` (`council.py:348`), `audit.latest_status` keys on `(round, persona)` (`log.py:179`); the shared helper must remain a pure fold so neither wrapper's semantics shift (Tests confirms the resumption tests call the wrappers, not `last_matching` — `tests.md` §"existing tests pass unchanged"). With (i)–(iv), Task 9 ships safely.

**Net:** I **defend my round-0 architecture finding's spirit** (the plan hides a real decision-reversal) and **converge with Correctness at HIGH**, but **rule the design itself architecturally OK** — so the severity attaches to *plan incompleteness*, and the remedy is rebut-and-delete-the-NOTE, not drop-the-task. I **concede** nothing material; I **sharpen** Correctness's "(a) drop OR (b) rebut" to "**(b) only**."

### 2. Defending Task 4(a) HIGH and Task 12 HIGH against Correctness's milder take — **both HELD at HIGH; they are scope-incompleteness, not wording.**

**Task 12 — HELD HIGH (dispute Correctness's LOW "imprecise prose").** Re-verified the mechanism is a missing-file scope gap, not a phrasing nit:
- `extract()` returns only `{nodes, edges, …}` (`extract/__init__.py:90-95`) — it **never surfaces the raw bytes** it read. Task 12's own invariant ("thread the bytes the extractor already read into the textual-reference pass," plan.md:35) is therefore **unimplementable without editing `extract/__init__.py`** to expose/return those bytes.
- The consumer seam is `structure.py:120` calling `_derive_textual_references(...)` (the cross-file/textual pass that today does an independent `path.read_text` via `references.py:_read_text_safely` at `:164-176`). Threading bytes through **requires editing `structure.py`** to pass them along.
- **Neither `extract/__init__.py` nor `structure.py` is in Task 12's file list** (plan.md:35 = `references.py`, `generic.py`, `incremental.py`). An implementer who edits only the three listed files **cannot satisfy the invariant** — they would either re-read disk (the bug) or reach outside their declared scope mid-build. That is a genuine scope-incompleteness that stalls/corrupts the parallel contract, exactly the class of defect a wave plan exists to prevent. Correctness's own §"internal-consistency" bullet concedes "the prose overstates direct file overlap" and offers "add `extract/__init__.py` to Task 12's list" as a reconcile option — i.e. it already agrees the file is missing; it merely under-rates the consequence. **HELD at HIGH.** Required fix: add `pipeline/extract/__init__.py` + `pipeline/build/structure.py` to Task 12's scope and correct the Wave-3 rationale (plan.md:45) from the false "shares the `extract/__init__.py` read seam" to "Task 12 *edits* `extract/__init__.py` (same file as Task 4) → real collision, serialize after Wave 2."

**Task 4(a) — HELD HIGH (Correctness independently rated this MEDIUM "location mismatch"; I keep HIGH and we converge on the seam).** Re-verified:
- `extract/__init__.py` contains **zero byte-read sites**: `grep read_bytes|read_text|open(` over the file → **no hits**. `extract()` (`:118`) calls only `collect_files` (path discovery) + per-language extractors. The plan's cited `:268-293` is inside `collect_files` (the `os.walk(followlinks=True)` *path-list* loop), **not** a read.
- The actual leaf reads are out-of-file: `generic.py:42` (`source = path.read_bytes()`) and the cache hash read `cache.py:38` reached via `load_cached`/`file_hash` at `extract/__init__.py:163`. So a *read-time* `resolve_under_root` guard (the task's stated, TOCTOU-correct intent — plan.md:17) **has no honest home inside Task 4's only in-scope file.** Its options collapse to: (i) put the guard in `generic.py:42` — but `generic.py` is **Task 12's** file (Wave 3), forcing an out-of-scope edit or a hidden cross-wave collision; or (ii) redesign it as a **containment filter at `collect_files` leaf-emission** (reject leaf paths whose `realpath` escapes `root` before they enter the file list) — doable inside `extract/__init__.py`, honest to the file, but it is a *walk-time* guard, which is weaker than the "read time, robust to TOCTOU and Task-12 caching" the task promises.

This is why it stays HIGH, not MEDIUM: it is not merely a citation that points at the wrong line — it is that **the task's stated guarantee cannot be delivered from its declared scope without either colliding with Task 12 or weakening the guarantee.** Severity tracks "can the parallel agent complete this in its lane as written?" — and the answer is no.

**Convergence on the single recommended seam (Security + Correctness + me).** Security (`security.md` MEDIUM on `collect_files`) notes the read at `:163` (cache hash) touches the out-of-tree file *before any check* and concludes the gate must wrap the path **before `load_cached`/`extractor(path)` at `:163,167`, or be pushed into `collect_files` leaf emission.** Correctness names the read sites `extract/__init__.py:167` + `generic.py:42` + `references.py:79/164-176`. I concur with the **`collect_files` leaf-emission filter** as the seam that (a) keeps Task 4 inside `extract/__init__.py`, (b) covers the `:163` cache-hash read that a `generic.py`-only guard would miss, and (c) avoids the Task-12 collision. The residual TOCTOU gap (post-enumeration symlink swap) should be closed by *also* gating the `generic.py:42` read **as part of Task 12** (which already owns that file and threads the bytes), with the plan stating the two-layer placement explicitly. **Single recommended seam: containment-filter at `collect_files` leaf emission for Task 4(a); read-sink gate in `generic.py:42` folded into Task 12.** This is the one placement all three lanes can sign.

### 3. Security's Task 3a severity — **I CONCUR it converges to HIGH (Security's BLOCK is calibration-justified; on plan-quality impact we meet at HIGH), and I CONCUR with the decisive in-repo-callers fact.**

My round-0 finding called this "an unstated code change behind reuse framing" and rated the cache trust-split coherent-but-under-specified. Security's round-0 went further and showed the framing is not merely unstated but **inverted**, and I now **concede that sharpening** after re-verifying the code:
- `cache_dir_override` uses the **env var**, not the `cache_root` channel: `common.py:134` `os.environ[key] = str(target.resolve())` (verified). The plan's "passes its target through the trusted `cache_root` channel" (plan.md:15, spec.md:30) describes a path the code does not take.
- Inside `cache_dir()` the **env var takes precedence over** the threaded `root`: `cache.py:51-55` returns on `if override:` before `root` is consulted. So at every production call site that wraps `cache_dir_override` (`runner.py:111`, `enriched_refresh.py:102`) the env var wins and the "trusted `cache_root`" escape hatch the plan leans on is dead there. My round-0 already flagged "the implementation must distinguish env-var-set-by-trusted-override from env-var-set-by-attacker" — Security's finding is the stronger, correct statement of the same defect: the plan's *named mechanism does not exist in the code.*
- **Decisive fact — I CONCUR and independently verified it.** Both real callers point the override at an **in-repo** target: `runner.py:109` and `enriched_refresh.py:100` set `cache_target = context_dir / "cache"` (i.e. `<out_root>/.context/cache/`, under the root). So confining the env-var value would pass `resolve_under_root` cleanly for the real callers and **would not break them** — which **contradicts the plan's own justification** for leaving the env var unconfined ("so `cache_dir_override` still works with an *out-of-repo* target"): there is **no in-repo caller** that passes an out-of-repo target. The plan defends an escape hatch nothing uses, at the cost of leaving the env-var channel open. The fix is simpler than the plan's justification assumes — Security's option (i) "confine inside `cache_dir()` and drop the false `cache_root` framing" is correct and breaks nothing real.

On severity: Security's BLOCK is defensible **as a plan-load-bearing-fix defect** (the headline Task-3a mechanism is non-existent in the code, so the acceptance bullet spec.md:48 passes vacuously). For cross-lane ranking I record my position as **HIGH** (the fix is real, scoped, and breaks no real caller — it is not a ship-stopping security hole given the local-tool trust model Security itself calibrates), and I note BLOCK-vs-HIGH is a labeling difference, not a substantive disagreement: **we converge that the plan's stated `cache_root` channel is inverted and must be rewritten, and that the in-repo-callers fact makes the confinement strictly safe.** I defer to synthesis on the BLOCK/HIGH label.

### Status of my round-0 findings

- **Task 4(a) "read-time guard has no anchor in its in-scope file" (high)** → **HELD at HIGH; defended.** Converged on the seam (`collect_files` leaf-emission filter for T4; `generic.py:42` read-sink gate folded into T12). Disputes Correctness's MEDIUM.
- **Task 12 "rationale wrong + missing `extract/__init__.py`+`structure.py`" (high)** → **HELD at HIGH; defended.** Genuine scope-incompleteness (extractor returns no bytes at `:90-95`; threading requires editing both missing files). Disputes Correctness's LOW.
- **Wave-3 serialization sound-but-wrong-reason (medium)** → **HELD at MEDIUM.** Gating is adequate; only the T12 rationale text (plan.md:45) is wrong. Unchanged.
- **Task 2 "retire `_atomic_write` understates the edit / repoint render.py:16-17" (low)** → **HELD at LOW; reinforced.** Correctness adds the `reality_check/__init__.py:74` re-export as a third site I missed — I **concede + adopt** that: the clean retirement touches `render.py` def + `confidence.py:16` import + `__init__.py:74` re-export + `render.py:16-17` call sites.
- **Cache trust-split "unstated code change behind reuse framing" (info, common.py:129-140)** → **ESCALATED to HIGH; conceded to Security's sharper finding.** The framing is not merely unstated, it is inverted (`cache_dir_override` uses the env var, not `cache_root`; `cache.py:51-55` env wins). Real callers are in-repo (`runner.py:109`, `enriched_refresh.py:100`) so the fix breaks nothing.
- **`paths.py` layering clean/cycle-free (info)** → **HELD; unchallenged by any peer.**
- **Reality-check `repo_root` weakness + anchor correct (medium)** → **HELD at MEDIUM; corroborated** by Security (HIGH on `meta["root"]` being committed/attacker-shippable) and Tests (nested-`.context/` fixture needs a real `.git`). I adopt Security's "reject `/` explicitly / equality against *resolved* git toplevel" as an implementation rider.
- **GATE consistent + genuinely still-open (info)** → **HELD; strongly corroborated** by Data-integrity (live consumer `features/builder.py:76` reads node `community`, so strategy (c) is a breaking change) and Tests (strategy (b)'s "spy Leiden never called" is vacuous with graspologic absent). No change to my position; these strengthen the GATE rationale.
- **Task 8 partially redundant vs `c574d41` (low)** → **HELD at LOW; corroborated** by Correctness (MEDIUM, same stale-premise) — the trailing-flag guard already shipped (`query.py:60-68`); only unknown-flag + USAGE doc are net-new.
- **Task 10 citation drift `:91-93`/`:108-111` (low)** → **HELD at LOW; corroborated** by Correctness and Tests (both re-anchor to `:90` pop / `:108-110` no-match). Cosmetic.
- **`export/graph.py` hyperedge sort needed (info)** → **HELD; corroborated** by Data-integrity, which adds a real rider: the sort key must be **total** (`json.dumps(h, sort_keys=True)`), not a single field, given the open hyperedge schema. I adopt that as a refinement.
- **6-guard consolidation list resolves to real code (info)** → **HELD; corroborated** by Correctness (notes 2 of 6 don't `.resolve()` the candidate, so `resolve_under_root` must handle already-resolved + not-yet-resolved uniformly — a fair implementation rider).

### RULING on Task 9

**Task 9 is architecturally SOUND** — a domain-neutral `log_scan.py` peer module is the same sanctioned shape as `context/domains/atomic_io.py` (`atomic_io.py:1-5`, imported across ≥9 domains) and is endorsed, not forbidden, by `folder-organization.md:69-73`; the only defect is that the plan must **quote, rebut, and delete the two "deliberately NOT extracted" NOTEs** (`audit/log.py:169-175`, `council.py:337-344`) rather than silently reverse them — **fix (b) rebut, NOT (a) drop; severity HIGH for the unrebutted decision-reversal, not for the design.**
