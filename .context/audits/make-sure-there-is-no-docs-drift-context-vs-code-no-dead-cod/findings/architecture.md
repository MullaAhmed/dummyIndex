## architecture findings

- `.context/features/cli-dispatch/spec.md:L56` — **high** (high) — DRIFT: spec says `ContextSubcommand` is "a `str, Enum` of 39 members from `INIT` through `STATUSLINE`", but the live enum has **43 members** (`dummyindex/context/enums.py:47-86` — 40 lines, INIT@47…STATUSLINE@86, 43 `NAME = "..."` declarations confirmed by count). Same "39" claim repeats at `plan.md:43` ("39 members"), `plan.md:90` ("39-verb CLI"), and `concerns.md:27,29,39,43`; `plan.md:89` says "other 38" (should be 42). — Curated feature-doc drift. Fix: edit the four docs to "43 members" / "42 others" in-session.

- `.context/features/cli-dispatch/spec.md:L17-L18` — **high** (high) — DRIFT: spec states "the two former business-logic modules (`context/domains/council.py`, `dev_pick.py`) were removed from the cluster" — but both modules still exist (`dummyindex/context/domains/council.py`, `dummyindex/context/domains/dev_pick.py`) AND are live: `cli/__init__.py:27,30` import the `council`/`dev_pick` CLI handlers, `_HANDLERS` maps `COUNCIL_LOG`→`council.run` (`cli/__init__.py:102`) and `DEV_PICK`→`dev_pick.run` (`:110`), and `dev-pick` is in the help reference (`cli/help.py:224`). The "removed" claim is false against the code. — Curated feature-doc drift. Fix: rewrite the sentence to reflect that council/dev_pick are present and dispatched (the code wins).

- `dummyindex/cli/__init__.py:35,88` — **high** (high) — DRIFT: the `hooks` subcommand (`ContextSubcommand.HOOKS = "hooks"` at `enums.py:51`; handler `cli/__init__.py:88` → `hooks.run`; module `dummyindex/cli/hooks.py`, an install/uninstall/status/defer-check verb) is entirely undocumented in cli-dispatch `spec.md`/`plan.md` — the SessionStart drift hook flagged `cli/hooks.py` for exactly this feature. The spec's handler-shape and examples sections never mention it, and the `Examples` list stops at the older verbs. — Curated feature-doc drift. Fix: add `hooks` to the spec's enum/handler description and (optionally) an example line; it is a real member of this feature's alphabet.

- `.context/features/cli-dispatch/council/02-architect-notes.md:L18` — **medium** (medium) — DRIFT: council note asserts it "Verified … exactly 39 members (`INIT`@47 → `STATUSLINE`@86)" — the @47/@86 line anchors are still correct but the count is stale (43, not 39), so the recorded verification no longer holds. — Curated council artefact drift. Fix: correct the count in the note, or leave a dated reconcile annotation (council artefacts are historical, lower priority than spec/plan).

- `.context/features/tree-enrich/plan.md:L96-L100` — **info** (info) — DRIFT (benign / already-acknowledged): the drift hook flagged `tests/cli/test_hooks_cli.py` for tree-enrich, but the plan's Open-questions already documents this exact "membership noise" — `test_hooks*` clusters here via shared test-harness call edges, not the enrich domain, and the spec/plan are deliberately scoped to the two real domain files (`context/domains/enrich.py`, `cli/enrich.py`). All cited `enrich.py` ranges verify accurate (build_plan@90, write_plan@180, apply_updates@202, ApplyResult@189≈doc `:188-199`, SCHEMA_VERSION@25; cli/enrich.py run_plan@10, run_apply@58). — No substantive doc-vs-code divergence; the flag is the known surplus-test cluster. Fix: none (resolve by the human placement pass the plan already calls for; not a real drift).

- `dummyindex/pipeline/` (whole subtree) — **info** (info) — Layering invariant HOLDS: grep for `from dummyindex.(context|cli|analysis)` under `pipeline/` returns nothing, and `analysis/` imports nothing from `context`/`cli` — matching the stated one-way arrows `context → analysis → pipeline` (`.context/conventions/folder-organization.md:25-28`). No lower-layer-imports-higher violation found. — Evidence: clean grep. Fix: none.

- `dummyindex/context/domains/` (whole subtree) — **info** (info) — Reverse-arrow invariant HOLDS: `context/domains/*` never imports `cli` (grep for `dummyindex.cli` / relative `..cli` under `context/` is empty), so the documented "cli is a one-directional sink; the world does not depend on cli" boundary (`cli-dispatch/plan.md:26-30`, `folder-organization.md:27-28`) is intact in code. — Evidence: clean grep. Fix: none.

- `dummyindex/cli/check.py:19`, `dummyindex/cli/refresh.py:5`, `dummyindex/cli/reconcile_gate.py:12`, `dummyindex/cli/statusline.py:28` — **low** (low) — The four `cli.<sub>` → `cli.<sub>` sibling imports flagged as a stated-rule exception in `folder-organization.md:77-83` all verify true at the cited lines (`from .rebuild import run as run_rebuild`, `from .migrate import migrate_legacy_layout`, `from .memory import _read_hook_stdin, _resolve_transcript`, `from .plan_update import badge_cache_path`). This is the documented narrow exception, not new erosion — but `reconcile_gate.py:12` reaches into two *underscore-private* names of a sibling (`_read_hook_stdin`, `_resolve_transcript`), a leaky-abstraction reach-through, not a public-entrypoint reuse like the other three. — Evidence: the private-name import crosses a sibling's privacy boundary. Fix: promote those two helpers to a non-underscore name in `cli/common.py` (or `cli/memory.py`'s public surface) if the reuse is intended; else keep but note the leak.

- `.context/features/cli-dispatch/spec.md:L34-L36,L65-L77` and `cli/common.py`/`cli/help.py` — **info** (info) — Spot-check PASSES: every load-bearing `path:range` citation in the cli-dispatch docs verifies against source — `usage_for`@`cli/help.py:427`, `resolve_context_root`@`cli/common.py:13`, `usage_error`@`:47`, `_FLAGS_TAKING_VALUE`@`:64`, `pull_repeatable_flag`@`:77`, `parse_path_and_root`@`:103`, `parse_kv_flags`@`:182`, `_wants_help`@`__init__.py:57`, `dispatch`@`:127-145`, `_HANDLERS`@`:83-124`. The structural/architecture documentation is accurate; only the count + "removed" + missing-`hooks` claims above are stale. — Evidence: all ranges match. Fix: none.

## Round 1 — architecture rebuttals
- **A1** → confirmed (with correction) — DRIFT is real but Round-0 magnitude was wrong: live `ContextSubcommand` is **40** members not 43 (`context/enums.py:47-86`, INIT@47 → STATUSLINE@86, 40 `NAME = "..."` lines incl. `HOOKS`@51); docs say **39** at `cli-dispatch/spec.md:56`, `plan.md:43,90`, `concerns.md:27,29,39,43`. The off-by-one the docs missed is the `HOOKS` verb (39→40), not a +4.
- **A2** → confirmed — `spec.md:16-18` "the two former business-logic modules (`context/domains/council.py`, `dev_pick.py`) were removed" is provably false: both files exist on disk AND are live, imported by `council_batch.py`, `reality_check.py:58`, `features/ops.py:331`, `equip/generate/adopt.py:30`; the cli verbs `cli/council.py:16,83` & `cli/dev_pick.py:15` dispatch into them (wired at `cli/__init__.py:102,110`).
- **A3** → confirmed — `HOOKS = "hooks"` is a real member (`context/enums.py:51`) with handler `ContextSubcommand.HOOKS: hooks.run` (`cli/__init__.py:88`, import @35), yet the spec/plan member-count prose (39, INIT→STATUSLINE) never names it — a true documentation omission.
- **A4** → confirmed — council `02-architect-notes.md:18` asserts "exactly 39 members (`INIT`@47 → `STATUSLINE`@86)"; the range is right but the count is stale by one (`HOOKS`@51 uncounted). Same root cause as A1.
- **A8** → confirmed (downgrade-resistant LOW) — `cli/reconcile_gate.py:12` imports `_read_hook_stdin, _resolve_transcript` from `cli/memory.py` (defined `memory.py:22,41`), reaching across a sibling's **underscore-private** boundary; the other three sibling imports (`check.py:19`, `refresh.py:5`, `statusline.py:28`) all reuse *public* names — so this is a genuine leaky reach-through, not the documented public-entrypoint exception. Stays LOW; minimal fix = promote both helpers to a non-underscore name.
- **C1/C2/C3 (cross-exam)** → strengthen — determinism is an explicit architectural invariant of this project (the committed `.context/` artefacts must be reproducible byte-for-byte across rebuilds); a cache-hit collapsing edge confidence to 1.0 (`export/graph.py:32-35`), non-deterministic community IDs (`cluster.py:103-104`), and unsorted graph JSON (`graph.py:37-38`) each corrupt the reproducibility contract of a *committed* artefact, so these are architecture-relevant, not merely local correctness nits.
