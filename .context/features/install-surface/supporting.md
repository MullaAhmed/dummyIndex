# Supporting

<!-- dummyindex:merged:begin -->
### Merged from `community-21`

The deterministic cluster `community-21` is folded into `install-surface`. Its
three files are members of this feature, and it contributes no flow documents:
`feature.json` carries an empty `flow_ids` and this feature directory has no
`flows/` subdirectory. The curated description of this material lives in
`spec.md` (reviewed default plugins, declaration, and materialisation) — this
section records only the merge provenance.

**Files involved:**

- `dummyindex/context/default_plugins.py`
- `dummyindex/installer/install.py`
- `tests/context/test_default_plugins.py`

**Entry points carried over (all still listed in `feature.json`):**

- `default_plugins_claude_available`
- `default_plugins_tail`
- `test_default_plugins_enabled_plugins`

<!-- dummyindex:merged:end -->
