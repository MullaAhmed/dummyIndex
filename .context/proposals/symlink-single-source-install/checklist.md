# Checklist — Symlink-aware single-source skill install: one real .agents/skills tree, .claude/skills symlinked

> Derived from plan.md tasks + spec.md Acceptance, grouped into
> dependency-ordered waves. Items inside one wave touch disjoint files and
> are mutually independent. Tick `- [x]` only after verifying.
> Link mode is the DEFAULT, `--platform` defaults to `both`, and migration
> (duplicated AND claude-only layouts) is FORCED (user decision
> 2026-07-24): one flagless `dummyindex install` / `/dummyindex-update`
> makes the repo work in Claude Code, Codex, Cursor, and Gemini CLI.
> `--copy` opts out; `--link` is strict; `--platform claude|agents` narrow.

## Wave 1 — foundations (disjoint: common/repair/uninstall hoists vs args/__main__)

- [x] Hoist `_remove_owned_tree_no_follow` + ownership-evidence trio (`_read_stamp`, `_has_legacy_codex_heading`, `is_owned_copy`, `_VERSION_STAMP_NAME`, `_LEGACY_CODEX_HEADING_RE`) into dummyindex/installer/common.py with re-exports from repair.py/uninstall.py, and add `LinkMode(str, Enum)` AUTO/LINK/COPY to common.py; full suite passes with zero test edits
- [x] Add `--link`/`--copy` flags + default-both flip: dummyindex/installer/args.py (11-tuple resolving `LinkMode`, `platform` default `"claude"`→`"both"`, usage text, parse-time exit 2 for `--link --copy` and `--link --platform agents`) + dummyindex/__main__.py forwarding + install()/uninstall() signature defaults + parse tests in tests/test_install.py (default both+AUTO, each flag, both rejections; claude-default assertions updated as the documented compatibility break)

## Wave 2 — link primitives (single item; everything downstream depends on it)

- [x] Implement dummyindex/installer/link.py + tests/test_install_link_primitives.py (unit-marked): `FamilyLinkState(str, Enum)` 6 states, frozen `FamilyLinkClassification`/`LinkResult`, `classify_family_link` (parent-chain rule, scope-root rule, readlink-parts comparison, fail-closed OSError→FOREIGN, positive-ENOENT dangling), `relative_link_value`, `family_link_target`, `create_family_links` (temp-link-first rename dance, stamp-required replacement + hand-edits caveat, `target_is_directory=True`, per-family `FileExistsError` continue, EPERM abort + Windows hint + uncovered-family names, post-create resolution check + dotfiles hint, DI `symlink_fn`), `verify_family_links`, `remove_dangling_family_links` (pre-unlink parent re-check), `run_link_install` (AUTO/LINK/COPY tri-state, capability pre-probe → AUTO whole-run copy fallback / strict-LINK exit 1, pinned sequencing); imports common.py ONLY; full classification + creation TDD matrix from plan task 3

## Wave 3 — wire the three consumers (disjoint files: install.py / repair.py / uninstall.py)

- [x] Preflight admission + default-link install + forced migration in dummyindex/installer/install.py + tests/test_install_link.py: admit OURS_*/MATERIALIZED family dirs in every LinkMode, FOREIGN + deeper refusals byte-identical (assert message), `_install_skill_family` never writes through a family-dir symlink, direct-write loop classifies first (dangling→report under COPY, no `FileExistsError` crash), sequencing plan→direct-write→execute_repairs→create_family_links, Claude-side family write skipped when linking; AUTO matrix (both = flagless default): both→linked by default + duplicated proven Claude families converted (`migrated ->` + hand-edits caveat per family, equal-stamp copies converted) + claude-only layouts migrated to universal (.agents written real, .claude linked, AGENTS.md written), claude (explicit)→link-to-existing when proven current `.agents` exists else copy, agents (explicit)→never touches `.claude/**` + duplication reported; strict LINK exit-1 gates (+`--force-downgrade` when newer/unknown); link-mode still writes /tokens + CLAUDE.md registration + hooks + auto-init; `--copy` characterization (COPY never calls create_family_links, linked layout left as-is)
- [x] Repair classification in dummyindex/installer/repair.py + tests/test_install_repair.py: OURS_HEALTHY → `linked -> .agents (current)` report (never rewrite; staleness handled on codex row), OURS_DANGLING/MATERIALIZED → report (healed same-run under AUTO/LINK, remediation under COPY), proven real Claude copy beside proven `.agents` → `migration candidate` report, FOREIGN message unchanged, no link writes in repair, `_find_duplicate_families` excludes only resolves-into-other-root pairs (user-real + project-link stays reported, both directions tested), dedupe removes link-only on linked side + sweeps dangling links after codex-family removal
- [x] Uninstall sweep in dummyindex/installer/uninstall.py + tests/test_install.py: `remove_dangling_family_links` after codex-family removal on agents/both, claude-platform link removal locked, foreign links untouched

## Wave 4 — surfaces (disjoint: check.py / help+README+update-skill)

- [x] check --versions labels in dummyindex/cli/check.py + tests/cli/test_check_versions.py: ` (linked)` and ` (materialized link)` + remediation
- [x] Split BOTH over-threshold installer modules into packages (import-transparent, `__init__.py` re-exports every currently-imported name incl. the underscore helpers the tests import, full suite ZERO test edits): (a) dummyindex/installer/link.py (1274 lines after the security-fix pass) into `dummyindex/installer/link/` — `families.py` / `models.py` / `classify.py` / `create.py` / `sweep.py` / `orchestrate.py`; (b) dummyindex/installer/install.py (1019 lines) into `dummyindex/installer/install/` — `orchestrate.py` (`install()`), `family_write.py` (`_install_skill_family`/`_symlinked_skill_install_directory`), `link_dispatch.py` (`_claude_narrowing_link_gate` + the two report helpers), `project_init.py` (auto-init); and (c) collapse `install._agents_family_stamp_state` + `repair._parse_version`/`_compare_stamp` into ONE stamp comparator in `common.py` (the layer below both; all inputs already there; repair re-exports — task-1 pattern) so the two version comparators can't drift. GATED on the link.py security audit's fixes having landed with the suite green (decision 2026-07-24: rule is aspirational — 9+ files already exceed 600 — so this is opportunistic correction at the point of least risk, not an emergency; the plan's "install.py gains only a dispatch call" understated reality by ~296 lines of install-boundary code — preflight admission, direct-write classification, result presentation — which cannot live in link.py without breaking its no-print/no-exit seam) — `families.py` (enumeration + temp-artifact naming + dotfiles hint), `models.py` (`FamilyLinkState`/`FamilyLinkClassification`/`LinkResult`/`LinkInstallResult`/`LinkCapabilityError`), `classify.py` (`relative_link_value`→`verify_family_links` incl. the parent-chain rule), `create.py` (the replacement dance), `sweep.py` (`remove_dangling_family_links`), `orchestrate.py` (`_probe_symlink_capability`/`run_link_install`); `__init__.py` re-exports exactly the 12 public names so every `from .link import X` resolves unchanged — full suite passes with ZERO test edits (import-transparent). GATED on the link.py security audit's fixes having landed with the suite green (decision 2026-07-24: rule is aspirational — 9 files already exceed 600 — so this is opportunistic correction at the point of least risk, not an emergency)
- [x] Docs: dummyindex/cli/help.py install section (link-by-default, `--copy`/`--link` tri-state, direction rationale) + README.md (linked default, update migrates duplicated repos automatically, `--copy` opt-out, Windows core.symlinks/Developer-Mode caveat + AUTO copy-fallback, MATERIALIZED recovery) + dummyindex/skills/update/SKILL.md step-3 note (update preserves linked layouts AND converts duplicated ones — expect `migrated ->` lines first time)

## Wave 5 — integration

- [x] E2E lifecycle in tests/test_install_link.py (integration-marked, in-process, capability-guarded): flagless install (universal linked layout, CLAUDE.md + AGENTS.md) → idempotent rerun (LinkResult 0/0, inode+mtime unchanged) → forced migration duplicated (seeded both-real equal stamps → flagless rerun converts, `migrated ->` lines asserted) → forced migration claude-only (seeded .claude-only → flagless rerun writes .agents + links + AGENTS.md) → stale-stamp repair (links untouched) → dangling heal via AUTO rerun → materialized replace → EPERM pre-probe AUTO copy-fallback (DI raiser) → Nth-call mid-loop failure (survivors + uncovered names) → crash-window recovery (temp artifact + renamed-aside tree → rerun converges) → dedupe → uninstall agents sweep → flagless uninstall (both) → clean tree; user-scope lifecycle + dotfiles-divergent fallback

## Wave 6 — verification + acceptance

- [x] Run the full suite + ruff on the branch and report the output — via /dummyindex-verify
- [x] One-time copy-path evidence: `install --skill-only` from merge-base (git worktree, default) vs HEAD (`--copy`), `diff -r` the `.claude/` + `.agents/` trees, paste the empty diff into the PR
- [x] Acceptance: fresh FLAGLESS `dummyindex install --scope project --dir <repo>` yields 8 enumerated real families under .agents/skills (stamped) + 8 Claude-side symlinks with readlink parts `../../.agents/skills/<family>` + CLAUDE.md + AGENTS.md; no enumerated family is a real dir under .claude/skills
- [x] Acceptance: duplicated repo + flagless install converts every proven Claude family (`migrated ->` + caveat), skips heading-only/unproven with reports; claude-only repo + flagless install becomes universal (.agents real, .claude linked, AGENTS.md); explicit `--platform claude` links-to-existing; explicit `--platform agents` never touches .claude and reports; parse exit-2 pairs (`--link --copy`, `--link --platform agents`); strict `--link` exit-1 gates name `--force-downgrade` when newer/unknown
- [x] Acceptance: plain reinstall on linked layout succeeds, reports `linked -> .agents (current)`, never re-duplicates; `--copy` leaves linked layout as-is; cross-scope user-real + project-link duplicate still reported
- [ ] **GATE** Live-host verification: scratch repo with linked layout — fresh Claude Code session lists and invokes /dummyindex; fresh codex session lists/invokes $dummyindex from .agents/skills (or Cursor picker shows the family); then run the forced migration on the real BOS-Mono/backend repo and confirm both hosts still work; record observed evidence in the PR
- [ ] Reconcile: fold link-default + forced migration into .context/features/install-surface/spec.md and advance the anchor (`dummyindex context reconcile-stamp`); reconcile report clean for install-surface — via /dummyindex
