# Plan — Implement outstanding audit + council findings

> Ordered, file-path-naming tasks. Source: the `concerns.md` of `extraction-pipeline`, `reality-check`, `cli-dispatch` + the audit `report.md`. Revised after the critique panel (wave collisions fixed, trust-roots named), then again after the `/dummyindex-audit` meta-audit (`audits/audit-plan-and-results/report.md` — cache trust-model corrected, symlink guard re-anchored, Task-12 scope completed, three false-pass/non-constructible acceptance tests fixed, GATE-(c) confirmed breaking). Reuses `write_text_atomic` (`atomic_io.py:11`), `usage_error` (`cli/common.py:47`), `is_git_repo`/`resolve_git_dir` (`pipeline/io/git.py`). Implementation tasks are untagged (build routes them to the generated `python-implementer` / `dummyindex-security-specialist` by keyword); verification is `— via /dummyindex-verify`.

## Tasks

### Wave 1 — shared primitives (prerequisites for the fan-out)

1. **Create `dummyindex/pipeline/io/paths.py`** — consolidate the path-confinement logic both feature fixes reuse. Add `resolve_under_root(candidate, root) -> Path | None` (pure; resolved iff under `root.resolve()`; catches `../` and absolute-join escapes; must accept **both already-resolved and not-yet-resolved** candidates — 2 of the 6 existing guards pass already-resolved paths) and `is_safe_read_target(path, *, max_bytes) -> bool` (does `os.lstat`/`os.stat`; rejects symlink/non-regular/oversize). Re-export both from `pipeline/io/__init__.py`. This deduplicates ~6 existing inline `resolve().relative_to()` guards (`cli/common.py:41-45`, `context/reconcile_gate.py:64-68`, `context/build/incremental.py:108-112`, …) — migrate the in-scope sites (`incremental.py`, touched by Task 12) to it; leave the out-of-scope sites as a follow-up note. Add `tests/pipeline/io/test_paths.py` (in-root / `../`-escape / absolute-escape / symlink / oversize / FIFO / positive control).

2. **Unify the reality-check atomic writer** (`dummyindex/context/domains/reality_check/render.py`, `…/confidence.py`, `…/__init__.py`). `_atomic_write` is used at **three** sites — the in-file `write_report` call (`render.py:16-17`), the import in `confidence.py:16` (`from .render import _atomic_write`), and the **re-export in `reality_check/__init__.py:74`**. Repoint `render.py:16-17` + `confidence.py:16` at `write_text_atomic` (`atomic_io.py:11`), drop the `__init__.py:74` re-export, then delete the def (else a `NameError` / dangling re-export). `write_text_atomic` and `_atomic_write` are byte-for-byte equivalent today, so this is a pure refactor — existing reality-check tests must pass unchanged. This stabilizes the writer **before** Tasks 10/11 fan out (they both otherwise edit the writer seam).

### Wave 2 — extraction + cli + reality-check confinement + maintainability (disjoint files; consume Wave 1)

3. **Harden the cache** (`dummyindex/pipeline/io/cache.py`, `dummyindex/context/build/common.py`). (a) **Trust-channel (corrected after the meta-audit — the original "split" was inverted).** `cache_dir_override` today sets the **env var** `DUMMYINDEX_CACHE_DIR` (`common.py:134`), and `cache_dir()` reads that env var with **precedence over** the threaded `root` (`cache.py:51-55` returns in the `if override:` branch before `root` is consulted) — so the env var, not `cache_root`, is the channel that decides. Fix: **re-route `cache_dir_override` off the raw env var** onto a trusted in-process cache-dir setting (so the override never flows through the confined path), then **confine only the ambient `DUMMYINDEX_CACHE_DIR`** inside `cache_dir()` — accept iff under the repo root (`resolve_under_root`), else **silently fall back** to `<root>/.context/cache/` and **never raise** (honor `tests/context/build/test_no_cache_leak.py::test_cache_env_var_is_restored`). `DUMMYINDEX_CACHE_DIR` is a **documented user-facing opt-out** (`cache.py:47-49` "put the cache anywhere"; `CHANGELOG.md:1095`) — either preserve it by distinguishing the trusted override write from an ambient value, or deprecate the out-of-repo opt-out as an **intentional breaking change** and update the docstring + CHANGELOG. (Both real internal callers — `runner.py:109`, `enriched_refresh.py:100` — already target the in-repo `<root>/.context/cache/`, so they are unaffected either way.) (b) **Minimal accept-superset schema** on `load_cached` (`cache.py:60-77`): miss unless payload is a dict with list-typed `nodes` + `edges` and every **node** has a string `id` (edges use `source`/`target`, not `id`); ignore unknown keys (so existing entries still hit — no mass invalidation). This is **anti-corruption, not anti-poisoning** — a well-formed poisoned graph still passes; the real lever is the dir confinement in (a). (c) Anchor `_body_content`'s `.md` frontmatter close to `^---\s*$` (`cache.py:10-17`) — the current `text.find("\n---")` is a **loose substring** match that truncates at a non-bare `---hack`, not only a horizontal rule — preserving the whole-file fallback when there is no closing fence. (d) `sort_keys=True` on `save_cached` (`cache.py:97`).

4. **Harden the extraction walk** (`dummyindex/pipeline/extract/__init__.py`). (a) **Symlink escape:** `extract/__init__.py` performs **no byte reads itself** — the real reads are `generic.py:42` (extractor) and `cache.py:38` (cache-hash, via `load_cached` at `:163`). Put the containment filter at **`collect_files` leaf emission** (`:289-293`) — the single in-scope chokepoint covering **both** read sinks: reject any leaf path whose `realpath` escapes `root` before it is emitted, under `follow_symlinks=True`. **NOTE: this is walk-time, not read-time** — a post-enumeration symlink swap is a documented residual; a true read-time guarantee would have to live in `generic.py` (Task 12's file) and is deferred there. (b) `global_label_to_nid` collision (`:216-221`): when a normalized label maps to >1 node, disambiguate by `source_file`/stem or skip the `calls`/`uses` resolution rather than binding to the last node. (c) `id_remap` immutability (`:190-197`): build new dicts (`{**n, "id": …}`) instead of mutating the `load_cached`-returned dicts in place (they are aliased into `all_nodes` at `:177` and re-consumed by later passes).

5. **Sort `hyperedges` in the graph export** (`dummyindex/export/graph.py:36`) — sort `data["hyperedges"]` by a **total** key (e.g. `json.dumps(h, sort_keys=True)` — a single field is insufficient given the open hyperedge schema, `validate.py:81-82`) before `json.dump`, matching the nodes/links sort (`:40-41`).

6. **GATE — clustering backend determinism** (`dummyindex/analysis/cluster.py`). Single-backend determinism (seed + content tiebreak) **already shipped in `c574d41`**; this GATE is the **residual cross-backend** gap (graspologic-Leiden vs NetworkX-Louvain produce different partitions, no injectable seam). Implement the strategy chosen for the spec Open-questions GATE; add the **single-backend** test the spec's GATE-dependent acceptance bullet names (cross-backend equality is non-constructible). **Strategy (c) is confirmed breaking** (feature-grouping re-reads the committed `community` — see spec Open questions) and **strategy (b)'s "Leiden never called" spy is vacuous when graspologic is absent** — prefer (a). **Do not implement until the GATE decision is recorded** — this item is `**GATE**` in the checklist so build escalates to the main session.

7. **Confine reality-check path resolution** (`dummyindex/context/domains/reality_check/verify.py`, `dummyindex/cli/reality_check.py`). (a) `_resolve_cited_path` (`verify.py:157-202`): route **every** return branch (`on_disk` + both basename-candidate branches) through `resolve_under_root(resolved, repo_root)`; on escape return not-found/ambiguous without opening. (b) `_repo_root_from_meta` (`verify.py:376-384`): trust `meta["root"]` only if it equals the **resolved** git toplevel (`is_git_repo`/`resolve_git_dir`) or the resolved `--root`; else fall back — never "any ancestor". (c) feature-id injection: reject `feature_id` containing `/`,`\`,`..`,NUL **at the CLI boundary** (`cli/reality_check.py:39`, right after parsing) with a non-zero exit before any read/write, plus the same guard at `verify.py:30`. (d) gate the line-count `open` (`verify.py:143-146`) behind `is_safe_read_target`. Golden test: this repo's own in-repo citations still verify + a **new** nested-`.context/` fixture **carrying a real `.git`** (the existing `fake_context` can't reach the nested case). **Acceptance proof strings must be extension-bearing** (`../../secrets.env:1`, absolute `/tmp/x.json:1`) — `_FILE_LINE_RE` (`extract.py:32-34`) grammar-rejects extensionless paths like `/etc/passwd:1` before they reach the verifier, so a `/etc/passwd:1` test is a **false pass** (see spec Acceptance).

8. **Fix the `query` arg surprises** (`dummyindex/cli/query.py`, `dummyindex/cli/help.py`). **The trailing `--top-k`/`--budget` guard already shipped in `c574d41` (`query.py:60-68`, exits 2) — do NOT re-add it; cover it with a regression test only.** Net-new: route `query`'s existing exit-2 sites (`:37,44,51,58,67`) through `usage_error` (`cli/common.py:47`) for the `--help` hint; emit a usage error on an unknown `--flag` instead of folding it into the search string (`query.py:69-71`); document the no-match exit-1 in `query`'s USAGE (`help.py`).

9. **Deduplicate `latest_status`** (`dummyindex/context/domains/audit/log.py:166`, `…/council.py:332`, new `dummyindex/context/domains/log_scan.py`). **This reverses an explicit in-code decision: `c574d41` added NOTE blocks to both bodies (`audit/log.py:169-176`, `council.py:337-344`) stating they are "deliberately NOT extracted". Quote-and-rebut those NOTEs in the task and delete them** — rebuttal: a domain-neutral `log_scan.py` peer is the same sanctioned shape as `context/domains/atomic_io.py` (a shared-domain helper imported across many domains), which `folder-organization.md:69-73` prescribes; a no-domain-object `last_matching(entries, predicate)` does not reach into another domain's internals. Extract a pure `last_matching(entries, predicate, attr="status")` helper into `context/domains/log_scan.py` (no leading underscore — `folder-organization.md:54`); both siblings import it; signature takes no domain object. Resumption tests pass unchanged (each wrapper keeps its own log + predicate; the council copy's exact resumption semantics are preserved).

### Wave 3 — reality-check mutation/report + P2 (depend on Wave 1/2; serialize the shared `cli/reality_check.py` after Task 7)

10. **Atomic confidence mirror** (`dummyindex/context/domains/reality_check/confidence.py`). Build both payloads, write both `.tmp`, replace back-to-back via `write_text_atomic` (Wave-1 writer); **mirror INDEX before popping the stash** — the current code pops first (`del payload[DEMOTED_FROM_KEY]` at `:90`, write at `:91`, mirror at `:93`); fix the ordering in **both** `promote` (`:85-94`) and `demote` (`:55-61`) so a failure can't lose the stash; surface a "no INDEX entry matched" signal instead of a silent no-op (the zero-match loop at `:108-110`); `sort_keys=True` on every persisted dump. Fault-injection test: a raise before the first replace (monkeypatch **`Path.replace`** — `atomic_io.py:24` — not `os.replace`) leaves both unchanged; document the single-replace window as best-effort.

11. **Report the `--demote` confidence delta** (`dummyindex/cli/reality_check.py`, `dummyindex/context/domains/reality_check/render.py`). **`demote_feature_on_contradiction`/`promote_feature_on_clean` currently return a bare `bool` the CLI discards (`confidence.py:26,61,64,94`) — a bool cannot distinguish the three transitions or carry the prior value. Widen the return to `(transition, from_value, to_value) | None`** (or read `feature.json` in the CLI). Emit the `demoted/restored/unchanged` line **in the CLI** (`cli/reality_check.py`, where the widened return value + the report are both in scope — `render_report_md(report)` alone can't see the transition). Add `sort_keys=True` to the `_reality-check.json` write (`render.py:16`). `capsys` test asserts all three transition strings.

12. **P2 — read each file at most twice** (`dummyindex/pipeline/build/references.py`, `dummyindex/pipeline/extract/generic.py`, `dummyindex/context/build/incremental.py`, **`dummyindex/pipeline/extract/__init__.py`**, **`dummyindex/pipeline/build/structure.py`**). `extract()` returns only `{nodes,edges}` today (`extract/__init__.py:90-95`) and `references.py` does **not** import `pipeline.extract`, so threading the extractor's bytes into the textual-reference pass requires **extending `extract()`'s return** and **passing the bytes through `structure.py:120`** — both added to scope above. Make the cross-file resolver consume the cached parse instead of re-reading disk (closing the cache-hit stale-node/new-byte hole, `extraction-pipeline/concerns.md`); **preserve the `re.escape`+lookahead matcher** (`references.py:106-125`) — do not re-introduce an alternation under a quantifier (ReDoS). Invariant: within one `build_all`, each file read once, all passes see the same bytes. Ordered **after** Task 4 (both edit `extract/__init__.py` — genuine collision, serialized by the wave gate). Golden: `symbol-graph.json` byte-identical; **read-counter is a path-keyed `defaultdict[Path,int]` asserting ≤2 per source path** (`generic.py:42`, the cache-hash `cache.py:38`, and the `--changed` hash all route through `Path.read_bytes`, so a method-level spy can't discriminate them), or introduce one named `read_source_bytes(path)` seam all consumers call; a mid-build file mutation yields one consistent byte-state.

### Wave 4 — verification

13. **Verify the whole change** — `python -m pytest tests/ -q` green, all new determinism/confinement/golden/fault-injection tests pass, and `symbol-graph.json` rebuilds byte-identically **across two post-change rebuilds** (not vs the pre-change committed file, which the hyperedge sort (Task 5) and the GATE `community` decision (Task 6) intentionally change once). — via /dummyindex-verify

## Dependency / wave rationale

- **Wave 1** (Tasks 1–2) is shared scaffolding every later task consumes: the `paths.py` primitive and the unified atomic writer. Their files (`paths.py`, `render.py`+`confidence.py`+`__init__.py`) are disjoint from each other.
- **Wave 2** (Tasks 3,4,5,6,7,8,9) touch disjoint files — `cache.py`+`common.py` / `extract/__init__.py` / `export/graph.py` / `analysis/cluster.py` / `verify.py`+`cli/reality_check.py` / `cli/query.py`+`help.py` / `log.py`+`council.py`+`log_scan.py` — and only read the Wave-1 primitive. Task 6 is `**GATE**`.
- **Wave 3** (Tasks 10,11,12) is serialized after Wave 2 because Task 11 re-touches `cli/reality_check.py` (Task 7) and Task 12 **edits `extract/__init__.py`** (the same file Task 4 touches — a genuine collision) plus `structure.py`. Within Wave 3, Task 10 (`confidence.py`), Task 11 (`cli/reality_check.py`+`render.py`), and Task 12 (`references.py`+`generic.py`+`incremental.py`+`extract/__init__.py`+`structure.py`) are disjoint.
- **Wave 4** is the final whole-suite verification.
