# Checklist — equip-auto-vendor-skills

> Wave N groups are mutually independent (disjoint files) and dispatch in parallel;
> waves run strictly in order. Tick `- [x]` only after the item's test is observed green.

## Wave 1 — pure capability-gap core (no I/O)
- [x] T1 — `context/domains/equip/generate/gaps.py`: `covered_capabilities(manifest)`, `required_capabilities(profile, *, proposal_capabilities=())`, `capability_gaps(*, profile, manifest, proposal_capabilities=())` (pure, deterministic `Capability`-order). Export from `equip/__init__.py`. + `tests/context/domains/equip/test_equip_gaps.py` (covered-subtraction, deterministic order, empty-manifest, generic-stack, proposal-scoped caps) — 11 tests green
- [x] T1b — rewrite `cli/equip/discover.py:_needed_caps` to delegate to `capability_gaps` (manifest-aware via `read_manifest`; an unreadable manifest degrades to a stack-only gap with a stderr warning, mirroring `_record_native`); tests in `tests/context/domains/equip/test_equip_discover_cli.py` (covered caps excluded; stack gap surfaced; corrupt-manifest degrade). **Scope note:** bare `discover` is stack+manifest only — proposal-scoped *specialist* gaps (security/db/…) are threaded in by the plan-time caller in **Wave 4**, not here.

## Wave 2 — pinned-ref enumeration + fetch (impure leaf; depends on W1 only for exports)
- [x] T2 — `context/domains/equip/plugins/sources.py`: added `resolve_ref(repo, *, runner)` (`gh api repos/{repo}/commits/HEAD` → sha; `SourceError` on undecodable), `ref=` kwarg on `fetch_file` (contents `?ref=`, back-compat default `None`), `_list_dir` + `list_skills(repo, *, ref=None, runner)` (tries `skills/<name>/` then repo root, skips files/hidden dirs, sorted-by-name deterministic), and a frozen `SkillRef`. Exported from `__init__`. + 10 `test_equip_sources.py` tests (ref resolve/none/undecodable, ref-pinned fetch endpoint, enumeration + root fallback + hidden-skip + empty + ref-carry) — 19 green

## Wave 3 — VENDOR install branch (CLI boundary; depends on W1+W2)
- [x] T3 — `cli/equip/discover.py`: stopped `continue`-skipping `is_collection` seeds in `_collect_catalogs`; `_collection_catalog` enumerates a collection's skills (`list_skills`) into `PluginEntry`/`Candidate`s. Reserved-name + trust + usage-doc gates intact (`is_collection`→`build_install_plan`→VENDOR mechanism).
- [x] T3b — `cli/equip/discover.py:run_install`: branch `if pi.mechanism is InstallMechanism.VENDOR:` → `_run_vendor_install` → `resolve_ref` → `fetch_file(..., ref=sha)` → `stamp_vendored` → `_vendor_write_ok` never-clobber guard (`is_safe_to_write` + `VENDORED_SENTINEL`) → `write_text_atomic('.claude/skills/<name>/SKILL.md')` → `_record_vendored` (`vendored_item`, `origin_ref=sha`, `mechanism=vendor`). Native path unchanged. + 6 vendor tests in `test_equip_discover_cli.py` (file written+stamped, manifest item vendored w/ 40-hex ref, no settings written, ref-pinned fetch, untrusted needs `--yes`, never-clobber user file, re-vendor own file). **638 green.**
- [x] T3-split — extracted the install verb + helpers (`run_install`, `_run_vendor_install`, `_record_native`, `_record_vendored`, `_vendor_write_ok`, `_write_back_wired`, `_settings_path_for_scope`, `_validate_usage_doc`, `_parse_capabilities_flag`, `_already_enabled_in`) into `cli/equip/install.py` (491 lines); `discover.py` back to **375** lines. `install.py` reaches the runner + shared discovery helpers via `from . import discover` → `discover._RUNNER`/`discover._collect_catalogs`/… so the `monkeypatch …discover._RUNNER` surface is unchanged; only one genuinely-moved symbol test (`discover.write_config`→`install.write_config`) updated. `dispatch.py` imports `run_install` from `.install`. **638 green.**

## Wave 4 — lifecycle parity + build-loop signal + skill prose (depends on W3)
- [x] T4 — lifecycle parity confirmed: `is_vendored_file`/`classify_item`/`status`/`uninstall` already include vendored items, so **no code change needed**; added `test_equip_vendor_lifecycle.py` (vendored PRISTINE; user-edit→USER_MODIFIED frozen; uninstall removes PRISTINE / keeps USER_MODIFIED; `refresh` leaves vendored untouched — re-fetch is via re-`install` at a new pinned ref, not refresh). **Spec corrected:** refresh does NOT re-fetch vendored (it re-renders GENERATED only).
- [x] T4b — `cli/build_loop/waves.py:_entry_for` emits an additive `missing_capability: [<cap>]` on a **true** specialist fallback (`choice.fallback` + `capabilities_from_text(text)` non-empty); threaded through `do_next` JSON (`do_next_wave` already spreads the full entry). `_dispatchable` unchanged. Exported `capabilities_from_text`. + 2 build-loop tests.
- [x] T4c — `equip/SKILL.md` corrected (vendor path is **live**, pinned-ref, never-clobber, trust-gated — no longer "next slice"); `build/SKILL.md` documents consuming the `missing_capability` signal via the gated discover→vendor flow. Plus a path-safety guard on the vendored skill name + test.

## Wave 5 — reconcile + acceptance GATE
- [x] T5 — **GATE** full suite green: **2145 passed, 1 skipped** (unrelated `graspologic` optional dep). Updated `.context/features/equip/{spec,plan,concerns}.md` — retired both open questions, documented the vendor path + `gaps.py` + pinned-ref + `install.py` split; annotated the moving-HEAD concern as partially-resolved for vendored skills.
- [x] Acceptance: `test_install_vendors_skill_from_trusted_collection` — stamped `.claude/skills/<name>/SKILL.md` + vendored manifest item with 40-hex `origin_ref`, no settings written.
- [x] Acceptance: `test_install_vendor_untrusted_collection_requires_yes` (+ `…_with_yes_vendors`); `test_uninstall_keeps_user_modified_vendored_skill` + `test_refresh_leaves_vendored_skill_untouched`; `test_uninstall_removes_pristine_vendored_skill`.
- [x] Acceptance: `test_needed_caps_excludes_capabilities_already_covered`; `test_cli_next_json_missing_capability_signal_on_specialist_fallback`.

## Wave 5b — security-review fixes (vendor path; folds the Wave 2+3 `python-reviewer` BLOCKs)
- [x] R1 — **silent clobber on reinstall** (BLOCK): `install.py:_run_vendor_install` now reads the prior manifest and, when a prior vendored item exists, refuses (rc 1) if `is_user_owned(classify_item(project_root, prior_item))` — the same origin-hash oracle refresh/uninstall use. The sentinel-only `_vendor_write_ok` is kept solely for the no-prior-record (foreign-file) branch. So a hand-edited vendored skill is never silently overwritten by a re-`install`; PRISTINE/MISSING still re-vendor. + `test_install_vendor_reinstall_refuses_edited_file`; existing unedited-reinstall test still green.
- [x] R2 — **uncaught `SourceError`** (BLOCK): `resolve_ref(...)` and the pinned-ref `list_skills(...)` in `_run_vendor_install` are now inside one `try/except SourceError: print(...); return 1`, mirroring the existing `fetch_file` handling — an undecodable gh-api response is a clean rc 1, not a crash. + `test_install_vendor_resolve_ref_source_error`, `test_install_vendor_list_skills_source_error`.
- [x] R3 — **path-traversal defense-in-depth** (BLOCK, mitigation hardened): `sources.py:_is_safe_skill_name` rejects empty / hidden (`.`-prefix → also `.`/`..`) / separator (`/`, `\`) names, and `list_skills` filters on it so `discover` never even surfaces a traversal segment; `install.py` keeps its explicit boundary guard as belt-and-suspenders. + `test_list_skills_drops_unsafe_path_names` (sources) and the existing end-to-end `test_install_vendor_rejects_unsafe_skill_name`.
- [x] Doc: `skills/equip/SKILL.md` vendored-freeze prose corrected — a hand-edited vendored copy is now genuinely frozen across a re-`install` (was only true for refresh/uninstall before R1).
- [x] R4 — **uncaught `EquipError` from R1's manifest read** (new BLOCK, introduced by R1, found on re-review): the prior-item `read_manifest` at `install.py` is now wrapped in `try/except EquipError` and **fails closed** (clean `error:` + rc 1) — a corrupt `equipment.json` can no longer crash a vendor install with a traceback, and we never overwrite when the never-clobber oracle is unreadable. + `test_install_vendor_corrupt_manifest_refuses`.
- [x] R5 — **uncaught `SourceError` in discovery** (sibling, same defect family, flagged on re-review): `discover.py:_collection_catalog` now wraps `list_skills` in `try/except SourceError` → warns + returns `None`, so a collection that returns an undecodable listing contributes no candidates instead of crashing the whole `discover`/`install` run. + `test_discover_collection_undecodable_listing_degrades`.
- [x] GATE: full suite **2151 passed, 1 skipped** (unrelated `graspologic`); ruff check + format clean on all touched files. Re-review verdict: 3 original BLOCKs RESOLVED; R4/R5 close the two follow-ups the re-review surfaced.
